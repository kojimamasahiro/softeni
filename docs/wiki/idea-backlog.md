# Idea Backlog 索引

Idea Backlog（発展候補アイデア）専用の索引ページ。責務は「アイデアがどこにあるか」を1ページで
把握できることだけに絞る。各アイデアの詳細行・状況・目的は各エリアページの
「発展候補アイデア一覧（Idea Backlog）」節が正（source of truth）。このページはそこへの
所在地インデックス＋一言サマリのみを持つ（重複管理はしない）。

## 使い方（idea-backlogスキルからの参照先）

- **アイデアのサマリーを知りたいとき**: まずこのページの索引表を見て、関連しそうなエリアページに飛ぶ。
- **新しいアイデアを作成したとき**: 対象エリアページの表に行を追加した後、このページの該当行の
  一言サマリも同期する。
- **既存アイデアを更新したとき**: 対象エリアページのrawファイル/表を更新した後、状況が変わって
  いればこのページの一言サマリも同期する（例: 発散フェーズ→実装済み）。
- **新しいエリアページに初めて「発展候補アイデア一覧」節を作ったとき**: このページの索引表に
  新しい行を追加する。

## 索引

| エリア | ページ | 一言サマリ（最終更新: 2026-08-01） |
|---|---|---|
| score機能 | [score-general-availability.md](./score-general-availability.md) | **2026-08-03: 差別化の核決着**——主軸は「重要局面別の分析エンジンによる成長のヒント発見」（論文等が根拠）、大会DB接続・普及エンジンは副次的。**2026-08-04: サイト全体比較は現状データでは棄却**（母集団5人・順位不安定、実データ検証済み）、当面は自己比較を優先、データ増加時に再検証予定 / 希少イベント検知→SNS投稿・金星バッジ（実装済み）/ プレースタイル分析・ランキング較正（検討/実装済み混在）/ シングルス整理（実装済み）/ 記録半自動化＋収集UX調査（発散フェーズ）/ 選手の主要大会実績の勲章カード＋全国大会優勝SEO（P0実装済み、残はGSC効果測定）/ 大会結果の希少性検知B系統（無敗優勝・◯年ぶり優勝、P1実装済み・first-regionは不採用・残はP2 win-streak） |
| 高校カテゴリ | [highschool.md](./highschool.md) | 強豪校ランキングページ（M2実装済み、残M4 GSC検証は2026年8月中旬予定）/ 地区大会結果の掲載（9地区の個人戦ダブルス・団体戦とも実データ投入済み。東海は打ち切り運用new設。高校カテゴリへは統合しない。残はneedsReview精度・ドキュメントwrite-back）/ 地区大会結果とnewsプレビューの連携（**一部実装済み**。「前哨戦・再戦」文脈ブロックを個人戦・団体戦の両方で実装し、プレビュー／年度別結果／大会ハブ／選手結果ページの4面へ展開。ペア単位照合なら誤マッチ実質ゼロと実測で確認。旧④`recentAchievers`案の団体戦未対応も2026-07-30に解消（校単位照合＋local_index.json連結）。残はhead-to-head本体（通算対戦成績））/ 開催中の全国大会を既存ページで受ける会期中SEO（**2026-08-01実装済み**。会期中に唯一順位が付いていた大会ハブが訪問者を大会公式サイトへ逃がしていたのが出発点。新規URLは間に合わないため作らず、ハブ1枚＋都道府県94枚＋学校243枚の既存インデックス済みページを更新。会期後は自動で「歴代」インテントへ戻る。残は実機`npm run build`とGSC効果測定） |
| 公開ページ | [public-pages.md](./public-pages.md) | トーナメント表の作り直し（**実装済み・`npm run build` 未検証**。本命Aは「結果未入力でも全ラウンドと接続線が描かれ勝ち上がりを目で辿れる」こと＝**状態管理不要の描画問題**と確定。ブラケット復元は `lib/bracketLayout.ts` にあり全データ検証で209大会・20,571試合が一致／不一致0件。検証の副産物として不具合2件を修正（席ずれによる誤ラウンド表示10大会／bye無し2冪ドロー36大会の取りこぼし）。描画用ツリー `buildBracketTree` は実装・検証済み（209大会・20,571試合が正しいラウンドに配置、欠落0。IH2026の結果未入力状態で512枠・9ラウンドが揃う）。分割・レイアウトの基準も確定（1枚64枠＝ベスト64が1枚／山シート＋ベスト64シート／左右から中央へ収束／拡大可／**両端に名前・内側は線だけ**で64枠が602×682pxに収まる）。`splitBracketSheets` 実装済み・モックは `npm run bracket:preview`。**`TournamentBracket.tsx` の差し替えまで実装済み**（復元できれば新描画 `BracketSheets`、できなければ従来描画）。`type` 逆算スクリプトで19大会を修復し新描画が229大会に。年度別結果ページのOGPを**ベスト16のトーナメント表**にする画像生成も追加（311件）。残は `npm run build` での確認） |
| データモデル | [data-model.md](./data-model.md) | Knowledge Graphによるデータ設計・UX統合（発散フェーズ、次の一歩は候補ビュー試作）/ 大会メタデータ基盤＝会場・施設・日程・要項のエンティティ化（発散フェーズ。SEOは会場名literalで足り、エンティティ化は履歴の蓄積待ち。いまはデータ収集のみ着手） |
| SNSストーリー生成基盤 | [sns-story-platform.md](./sns-story-platform.md) | 大会・学校・選手・試合データ→構造化ストーリー（JSON/YAML、LLM入力可）→SNS/SEO/通知等へ再利用する基盤（発散フェーズ、要件定義済み。ADR-005のLLM不使用方針からの転換点）/ 「展望記事のドローの厳しさ（山の偏り）」は未来予測寄りで弱いとの反省から、**方向性を「過去の事実ベース5分類（継続・逆転・成長・衰退・再会）＝大会インサイト」へ転換**し、**2026-08-01に全データ実測で設計決着**（未解決7項目すべて）。名寄せは氏名のみが正解（誤合流0.07%＜所属込みで切れる1,223件）、閾値は選手3年・学校4年、候補は出すぎず**不足**のため記事単位を「大会×年」に確定・供給源は一般カテゴリ中心（高校は選手でなく学校単位の継続で代替）、掲載先は**サイト新規ページなし＝年度別結果ページ「注目ポイント」の拡張でADR-010非抵触**。**サイト掲載も実装済み**で、ADR-005の「本文はテンプレのみ・LLM不使用」は[ADR-012](../adr/ADR-012-llm-authored-insights-with-machine-verification.md)が「機械照合を通った散文に限り可」へ改めた（公開条件はprebuildが照合し直して強制）。**YAML生成`scripts/generate-story-yaml.mjs`と機械照合`scripts/verify-story-text.mjs`は実装済み**（`npm run story:generate` / `story:verify`。生成物は`docs/story-yaml/`）。**学生カテゴリは学校を主役にする**方針も決定（インターハイの個人戦は混成ペア0.00%で「◯◯高が3連覇」と書ける／高校は選手が3年で卒業するので経年ストーリーの器が学校しかない）。残るのは実装順序・工数の計画 |
| プロジェクト運用/メタ | [open-questions.md](./open-questions.md) | AIとの共同探索と探索プロセスの属人性（発散フェーズ・中断中） |
| 選手ページ | [players-pages.md](./players-pages.md) | 試合結果データだけから選手の「戦評」を編み出す（発散フェーズ・実データ一次検証済み2026-08-06。接戦度はsplit-half信頼性検証で棄却、勝率調整済み圧勝度の残差が唯一検証を通った候補指標） |

## 関連

- [docs/wiki/index.md](./index.md) — Wiki全体の入口
- [AGENTS.md](../../AGENTS.md) — LLM Wiki運用ルール全般（ドキュメント同期・クロスリンクの一般規約）
