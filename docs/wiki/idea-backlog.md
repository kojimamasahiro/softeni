# Idea Backlog 索引

Idea Backlog（発展候補アイデア）専用の索引ページ。責務は「アイデアがどこにあるか」を1ページで
把握できることだけに絞る。各アイデアの詳細行・状況・目的は各エリアページの
「発展候補アイデア一覧（Idea Backlog）」節が正（source of truth）。このページはそこへの
所在地インデックス＋一言サマリのみを持つ（重複管理はしない）。

## 使い方（idea-backlogスキルからの参照先）

- **アイデアのサマリーを知りたいとき**: まずこのページの索引表を見て、関連しそうなエリアページに飛ぶ。
- **新しいアイデアを作成したとき**: 対象エリアページの表に行を追加した後、このページの該当行の
  一言サマリも同期する。
- **既存アイデアを更新したとき**: 対象エリアページのrawファイル/表を更新した後、状況が変わって
  いればこのページの一言サマリも同期する（例: 発散フェーズ→実装済み）。
- **新しいエリアページに初めて「発展候補アイデア一覧」節を作ったとき**: このページの索引表に
  新しい行を追加する。

## 索引

| エリア | ページ | 一言サマリ（最終更新: 2026-08-15） |
|---|---|---|
| score機能 | [score-general-availability.md](./score-general-availability.md) | **2026-08-03: 差別化の核決着**——主軸は「重要局面別の分析エンジンによる成長のヒント発見」（論文等が根拠）、大会DB接続・普及エンジンは副次的。**2026-08-04: サイト全体比較は現状データでは棄却**（母集団5人・順位不安定、実データ検証済み）、当面は自己比較を優先、データ増加時に再検証予定 / 希少イベント検知→SNS投稿・金星バッジ（実装済み）/ プレースタイル分析・ランキング較正（検討/実装済み混在）/ シングルス整理（実装済み）/ 記録半自動化＋収集UX調査（発散フェーズ）/ 選手の主要大会実績の勲章カード＋全国大会優勝SEO（P0実装済み、残はGSC効果測定）/ 大会結果の希少性検知B系統（無敗優勝・◯年ぶり優勝、P1実装済み・first-regionは不採用・残はP2 win-streak） |
| 高校カテゴリ | [highschool.md](./highschool.md) | 強豪校ランキングページ（M2実装済み、M4 GSC検証は**2026-08-15第1回チェック実施**——インデックス・カニバリ問題なし、強豪/ランキング系クエリは維持〜向上（ヘッドクエリ「ソフトテニス 高校」のみ順位悪化）。残は2026年9月末の平常期チェック）/ 地区大会結果の掲載（9地区の個人戦ダブルス・団体戦とも実データ投入済み。東海は打ち切り運用new設。高校カテゴリへは統合しない。残はneedsReview精度・ドキュメントwrite-back）/ 地区大会結果とnewsプレビューの連携（**一部実装済み**。「前哨戦・再戦」文脈ブロックを個人戦・団体戦の両方で実装し、プレビュー／年度別結果／大会ハブ／選手結果ページの4面へ展開。ペア単位照合なら誤マッチ実質ゼロと実測で確認。旧④`recentAchievers`案の団体戦未対応も2026-07-30に解消（校単位照合＋local_index.json連結）。残はhead-to-head本体（通算対戦成績））/ 開催中の全国大会を既存ページで受ける会期中SEO（**2026-08-01実装済み**。会期中に唯一順位が付いていた大会ハブが訪問者を大会公式サイトへ逃がしていたのが出発点。新規URLは間に合わないため作らず、ハブ1枚＋都道府県94枚＋学校243枚の既存インデックス済みページを更新。会期後は自動で「歴代」インテントへ戻る。実機`npm run build`は2026-08-07確認済み・問題なし。**GSC効果測定は2026-08-15第1回チェック実施済み**（詳細は[raw/2026-08-15-m4-gsc-review.md](../raw/2026-08-15-m4-gsc-review.md)）。残は2026年9月末の平常期チェック） / **2026-08-15: 学校ページの回遊強化（横リンク）を新規追加**（発散フェーズ）。SEO一巡後の次テーマ。目的はPV増＝AdSense収益。`out/`4,243枚の実測で `hs:school → hs:school` の横リンクが0本・学校ページ698枚の68%が本文リンク10本以下と判明。在庫は**同県の他校のみが全ページで成立**（上限5校で3,293本）、**団体戦の直接対戦は52%が0件で棄却**。実装は既存`getPrefectureTopSchools`再利用で新規URLゼロ。未決は絞り方のみ。**計測手段は[回遊検証ランブック](./circulation-verification.md)として整備済み**（主指標は同意状態に依存しないモジュールCTR、対照群は選手結果ページ、最終判定はAdSense収益総額。ベースラインは平常期＝2026年9月末にM4の平常期チェックと同時実施） |
| 公開ページ | [public-pages.md](./public-pages.md) | **2026-08-14: 大学カテゴリは保留**（データの下ごしらえのみ適用済み・**公開ページとコードは未着手＝サイトの表示は変わっていない**。再開の入口は[節の整理の設計](./public-pages.md)の案A/B/C決定）。方針としては「進路接続を先にやる」へ転換済み。8/11に「大学は都道府県0.5%で向かない」と棄却した判断を深さ未測定だったため見直し、**大学は中学の真逆＝チーム数は少ないが1チームが濃い**と判明。中学カテゴリの実装を踏まえ、**ツリーではなく進路接続を先に**やる方針へ転換した——`prefecture`が`日本学連`/`学連`で**地域軸が本当に無く**ツリーは135ページの平屋にしかならない一方、進路なら**新規URLは索引2枚**で残りは高校の既存721ページへの節追加になり、**高校×性別グループ300は中学の111の2.7倍**（高校ページの42%に付く）。中学の学びから**ランキングも大会軸ページも作らない**。**3段接続（中学→高校→大学）は現時点0人**だが**インカレ2026の結果投入で初成立**する。チーム名の下ごしらえは**適用済み**（186→163チーム、中央値20→25、試合5未満25.8%→16.6%） / **2026-08-11: 成年（社会人・一般）カテゴリの都道府県ページ・強豪チームランキング**を検討したが、試合数分布を実測した結果**高校型のフル横展開はデータ不足と判明**（中央値4試合、57.3%が5試合未満、mapping カバー5%）。上位12%程度に絞ったランキング型なら見込みありだが未検討。今回はSTリーグ出場チームの「メンバー」節・選手名リンクのみ実装（`st-league.md`）/ トーナメント表の作り直し（**実装済み・`npm run build` 確認済み**（2026-08-07・問題なし）。本命Aは「結果未入力でも全ラウンドと接続線が描かれ勝ち上がりを目で辿れる」こと＝**状態管理不要の描画問題**と確定。ブラケット復元は `lib/bracketLayout.ts` にあり全データ検証で209大会・20,571試合が一致／不一致0件。検証の副産物として不具合2件を修正（席ずれによる誤ラウンド表示10大会／bye無し2冪ドロー36大会の取りこぼし）。描画用ツリー `buildBracketTree` は実装・検証済み（209大会・20,571試合が正しいラウンドに配置、欠落0。IH2026の結果未入力状態で512枠・9ラウンドが揃う）。分割・レイアウトの基準も確定（1枚64枠＝ベスト64が1枚／山シート＋ベスト64シート／左右から中央へ収束／拡大可／**両端に名前・内側は線だけ**で64枠が602×682pxに収まる）。`splitBracketSheets` 実装済み・モックは `npm run bracket:preview`。**`TournamentBracket.tsx` の差し替えまで実装済み**（復元できれば新描画 `BracketSheets`、できなければ従来描画）。`type` 逆算スクリプトで19大会を修復し新描画が229大会に。年度別結果ページのOGPを**ベスト16のトーナメント表**にする画像生成も追加（311件）。残タスクなし） |
| データモデル | [data-model.md](./data-model.md) | Knowledge Graphによるデータ設計・UX統合（発散フェーズ、次の一歩は候補ビュー試作）/ 大会メタデータ基盤＝会場・施設・日程・要項のエンティティ化（発散フェーズ。SEOは会場名literalで足り、エンティティ化は履歴の蓄積待ち。いまはデータ収集のみ着手）/ **2026-08-08: 全中ブロック大会の掲載**を追加検討（高校地区大会と同型。既存インフラはgeneration非依存でコード変更不要と確認済み、東北ブロックのみ北海道の扱いが高校と異なり`blocks.json`追加が必要。中学専用カテゴリは作らず通常の大会として登録する方針で合意、9ブロック分の2026年度データ投入済み） / **2026-08-12: 中学カテゴリの公開ページ（/highschool型フル横展開）を方針決定**（8/8の「中学専用カテゴリは作らない」を上書き）。実測でページ候補は高校の1.9倍・データ量0.45倍と判明。ユーザーの懸念のうち**都道府県のまとまりは杞憂**（県判明率100%）、**真のボトルネックは表記ゆれ9.9%＝高校の20倍**（全中は略称・ブロックPDFは正式名称）。前提条件は名寄せ層の構築＋掲載閾値5以上（283校/約435ページ・47県非空）＋「学校」でなく「チーム」設計。次の一歩はコード着手前の名寄せ層構築とページ規模確定。**同日追記: 「高校と違う中身」を4候補検証**——ダブルスペアの所属一致率（高校IH/全中0.00%に対しブロック18.65%・県対抗38.89%）から**高校の「学校が主役」設計を持ち込む必然性は無い**と判明。①進路の接続（中学→高校547人・ペア継続48組）が最有力で既存の高校ページにも還流、②県対抗は47県完全均等でサイト内最も規則正しいデータ、③**地域移行トラッカーは2026-08-12に実装済み**（全中の大会ハブに節を追加・新規URLなし。確定値は2022年度1団体→2023年度10→2024年度21→2025年度39＝36.8%で、中体連が特例を設けた2023年度と跳ね上がりが一致。分類は「クラブと断定できる証拠がある場合のみ」の下限カウント方式＝`lib/clubTransition.ts`、検証は`npm run club:verify`。残は`npm run build`完走とGSC効果測定）、④選手中心は棄却（中学単独で5回以上は0.1%）。**2026-08-12: 前提条件だった名寄せも実施済み**——新層は不要で既存パイプラインが使えたが、alias表が文脈を持たず`日高→日高中学校`のように高校を壊すため[ADR-013](../adr/ADR-013-scoped-team-name-aliases.md)で大会スコープを導入。中高一貫7校の中学出場107件を分離（全日本ジュニアのu17/u20 36件は高校のまま）。ヘルスチェックの要対応シグナルは72→42、別名残存4→0。**同日: ページ規模も確定**——閾値5・ブロック大会込みで283団体/約432ページ（高校289団体・409ページと同等）。ブロック大会は掲載判定には使うが集計には使わない（県別出場数が北海道65対福岡4でブロック構成の差が出るため）。閾値5の内訳は学校117/クラブ116で**クラブが約4割**あり「チームページ」設計が実測で確定。**2026-08-12: 実装完了**——URLは `/secondaryschool/`、階層は中学向けに再設計（性別をURLに入れない／「学校」でなく「チーム」／大会軸ページを作らない／ランキングは当面なし）で計328ページ。都道府県ページの主役は「県そのものの成績」（候補2）、チームページには「中学→高校の進路」（候補1、184件/83チーム）を実装。仕様は[secondaryschool.md](./secondaryschool.md)。残は teamId のローマ字読みの目視確認280件・`npm run build` 完走・GSC効果測定 |
| SNSストーリー生成基盤 | [sns-story-platform.md](./sns-story-platform.md) | 大会・学校・選手・試合データ→構造化ストーリー（JSON/YAML、LLM入力可）→SNS/SEO/通知等へ再利用する基盤（発散フェーズ、要件定義済み。ADR-005のLLM不使用方針からの転換点）/ 「展望記事のドローの厳しさ（山の偏り）」は未来予測寄りで弱いとの反省から、**方向性を「過去の事実ベース5分類（継続・逆転・成長・衰退・再会）＝大会インサイト」へ転換**し、**2026-08-01に全データ実測で設計決着**（未解決7項目すべて）。名寄せは氏名のみが正解（誤合流0.07%＜所属込みで切れる1,223件）、閾値は選手3年・学校4年、候補は出すぎず**不足**のため記事単位を「大会×年」に確定・供給源は一般カテゴリ中心（高校は選手でなく学校単位の継続で代替）、掲載先は**サイト新規ページなし＝年度別結果ページ「注目ポイント」の拡張でADR-010非抵触**。**サイト掲載も実装済み**で、ADR-005の「本文はテンプレのみ・LLM不使用」は[ADR-012](../adr/ADR-012-llm-authored-insights-with-machine-verification.md)が「機械照合を通った散文に限り可」へ改めた（公開条件はprebuildが照合し直して強制）。**YAML生成`scripts/generate-story-yaml.mjs`と機械照合`scripts/verify-story-text.mjs`は実装済み**（`npm run story:generate` / `story:verify`。生成物は`docs/story-yaml/`）。**学生カテゴリは学校を主役にする**方針も決定（インターハイの個人戦は混成ペア0.00%で「◯◯高が3連覇」と書ける／高校は選手が3年で卒業するので経年ストーリーの器が学校しかない）。残るのは実装順序・工数の計画 |
| プロジェクト運用/メタ | [open-questions.md](./open-questions.md) | AIとの共同探索と探索プロセスの属人性（発散フェーズ・中断中）/ **2026-08-14: skillをローカルLLMで代替する検討**（設計→実データ検証済み。3skillのうち venue-data は実装済み・pdf-to-players はLLM不要で、insightが本命。**照合の実効カバレッジが26.4%**しかなく、**記事の主語「今年誰が優勝したか」は24本すべて未照合(0/24)**と判明。抽出パターン(a2)追加で23/24に修正済み。優先順位を「禁止語チェック」から**「照合の網の拡張」へ変更**。`scripts/insight-agent/lint.mjs`と(a2)は実装済み。**同日、最優先の「未知の固有名詞の検出」も実装**——記事の語彙が固有名詞を除くと93文字しかないことを利用し、正解コーパス24本で誤検出0・架空名は検出。次の一歩は「初優勝」を照合器の主張型に足すこと。**A・Bのレビューも実施し、Bの前提の誤りを発見**——pdf-to-playersは「未実装」ではなく`scripts/pdf/`に中核資産(座標抽出・initialPlayers生成・座標校正)が既にあり、**SKILL.md側に`tempId`の項目数と`COL_SPLIT`の実在について2つの誤記**がある。**Bは実装済み（`scripts/pdf-to-players/`）**——団体戦は人が作った正解データと49件全項目一致・回帰32/32 pass、LLMは「列の意味づけ」1点のみで既定オフ（Ollama無しで通る）。**全中2024のドロー表で個人戦も検証済み**（ブラケット表対応のため列検出を「行ごとの投票」方式に変更、4ページとも32エントリーを正しく抽出、回帰64/64 pass）。団体戦ブラケットも対応済みで**全6ページ・回帰78/78 pass**。SKILL.mdの2つの誤記も修正版を受け渡し済み。**さらに「未知の様式への融通」「自力で直せること」の指摘を受け、パラメータの自動調整層と実施手順書RUNBOOK.mdを追加**（人がパラメータを振る運用をやめ、抽出結果を決定的に採点して自動選択。LLMは点数が低いときだけの切り札に変更）。**インカレも通し、大会プロファイル方式を導入して回帰103/103 pass**（詰まりは知能差でなく通常のバグ＋採点の設計誤りだった）。モデル選定は保留） |
| 選手ページ | [players-pages.md](./players-pages.md) | **2026-08-08: 選手一覧ページ（/players）を全面改修**（「同姓同名選手の一覧」という名乗りが実装の誤解だったと判明し入口として定義し直し。内部リンク97→1,917本、DOM要素数-68%、検索インデックス334KB(gzip)の無条件fetchを廃止、JSON-LDのビルド日偽装を除去。姓頭文字での分割ページは薄いページ量産のため不採用。残はGSC効果測定・読み仮名データ・実機ビルド）/ **2026-08-07: 結果ページのセクション階層化を実装**（主要タイトル常時表示／「スタッツ」1見出しの下にチップ＋`<details>`「詳細を見る」に統一、旧・関連選手は重複解消でチップへ統合、他機能導線2種と大会結果を分離。段階的開示P3適用、情報削除なし）/ 試合結果データだけから選手の「戦評」を編み出す（発散フェーズ・実データ一次検証済み2026-08-06。接戦度はsplit-half信頼性検証で棄却、勝率調整済み圧勝度の残差が唯一検証を通った候補指標） |

## 関連

- [docs/wiki/index.md](./index.md) — Wiki全体の入口
- [AGENTS.md](../../AGENTS.md) — LLM Wiki運用ルール全般（ドキュメント同期・クロスリンクの一般規約）
