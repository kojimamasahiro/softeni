# SNSストーリー生成基盤

## ステータス

Draft（2026-07-22 起票）。まだ意思決定していない発散フェーズ。要求・要件定義は完了、
設計フェーズは未着手。一次ソース:
[docs/raw/2026-07-22-sns-story-platform-requirements.md](../raw/2026-07-22-sns-story-platform-requirements.md)。

## 概要

大会・学校・選手・試合データから発信価値のあるストーリーを抽出し、JSON/YAML形式の
構造化データとして出力する共通基盤。出力はLLMへそのまま入力できる形式にし、
X・Threads・SEO記事・プッシュ通知・ホーム画面・メール等への横展開を狙う。
年内Xフォロワー1,000人がゴールの一つ。

要件の要点（詳細は一次ソース参照）:

- ストーリー抽出結果はJSON/YAML、機械可読な構造化データ（自然文ではない）
- 推測・主観・評価表現を含めず、DBから導出可能な事実のみを保持
- 分類情報（例: Ranking / Record / Comparison / Milestone / Player / School / Trivia）を持ち、将来拡張可能
- 生成の根拠となる対象データ（大会/試合/学校/選手/ランキング）を保持
- 発信優先度判断のための評価情報（希少性/季節性/話題性/保存価値）を保持。算出方法は実装非依存
- 実装方式・LLM・大会種別に依存しないインターフェース

## 既存の近縁の取り組み

同種の「データ→イベント抽出→再利用」構造を持つ先行実装・検討が複数ある。
新規実装するか統合するかの判断材料として整理する。

- **ADR-005 / [news-context-blocks.md](./news-context-blocks.md)**: 大会データ→
  イベント抽出→文脈ブロック（一次成果物）→大会/選手/ランキング/記事への再利用という
  骨格がほぼ同型。ただし「本文はテンプレートのみ・LLM不使用」を明示決定しており、
  本アイデアはそこからのLLM方針転換にあたる（LLMに本文自動生成させるのではなく、
  LLM入力可能な構造化データを用意するところまでがスコープ）。
- **[rare-events.md](./rare-events.md)**: score機能のポイントデータ限定で、分類・根拠・
  評価・再利用をすでに持つ先行実装（試合詳細ページ表示＋X投稿テンプレ生成）。
  対象は大会・学校単位のデータには及ばない。
- **抽出ロジックの分散**: `lib/milestones.ts` / `lib/careerRecord.ts` /
  `lib/tournamentRecords.ts` / `lib/majorTitles.ts` 等、大会データからイベント・関係を
  再構築するロジックが機能ごとに個別実装されている
  （[data-model.mdのKnowledge Graphアイデア](./data-model.md)で重複実態を確認済み）。

## 発展候補アイデア一覧（Idea Backlog）

まだ発散フェーズ。

| アイデア | 状況・目的（1行） | 詳細 |
|---|---|---|
| SNSストーリー生成基盤（構造化データ→LLM入力） | 発散フェーズ（2026-07-22起票）。要件定義済み。実装は新規が第一候補（既存milestone等との統合は重複コスト次第）、分類は既存へ無理にマッピングせず独立実装でよい方針 | [アイデア](../raw/2026-07-22-idea-sns-story-platform.md) / [要件定義](../raw/2026-07-22-sns-story-platform-requirements.md) |
| 展望記事: ドローの厳しさ（山の偏り）ストーリー | 発散フェーズ（2026-07-31起票）。本基盤の最初の具体的ストーリー種別候補。`/news`展望記事の拡充相談から派生し、「優勝予想」ではなく「なぜこの山は厳しいか」を数値事実で語る方向へ。個人の生レートは非公開のまま、山（4分割）単位の「上位establishedのうち上位10%が何組集中しているか」を実測（全日本選手権2023男子で期待2.5組に対し実際6組＝2.4倍等）。平均レートでの比較は薄まって使えないことも確認済み。generationId `'all'`（一般カテゴリ）中心が対象になりそう（インターハイ等はestablished coverageが11〜33%と薄く成立しにくい）。ただし未来予測寄りで弱いとの反省から、下記アイデアへ主軸が移った | [アイデア](../raw/2026-07-31-idea-news-draw-difficulty-story.md) |
| 大会インサイト: 過去の事実ベース5分類ストーリー（継続・逆転・成長・衰退・再会） | **設計決着**（2026-08-01）。未来予測は弱い・過去の事実は強いという反省から着想。10具体例中5個（連覇・番狂わせ・雪辱・王者敗退・前年決勝の再戦）は既存実装そのもの。残り5個も「同一大会の過去開催のみ」にスコープを絞ることで名寄せブロッカーを回避でき、ロジックのみ実装可能と確認済み。**全335ファイル実測で7つの未解決を決着**: (1) 名寄せキーは**氏名のみ**が正しい（同姓同名の誤合流は0.07%＝35/49,429件、対して氏名＋所属は表記ゆれ・移籍で1,223件のstreakが不当に切れる。姓のみデータの選手と掲載年に欠落のある大会はガードで除外）、(2) 閾値は選手3年連続・学校4年連続・自己最高はベスト8足切り（足切りの根拠は希少性ではなく、ベスト8未満が「N回戦敗退」でドロー規模依存＝年度間比較不能なこと）、(3) 候補は出すぎるどころか**不足**（大会×種目×年で中央値1件・39%が0件）→ 記事単位を「大会×年（全種目束ね）」に確定、供給源は一般カテゴリがほぼ唯一（16組で0件なし・中央値8.5件、高校は選手単位の継続が3年卒業で構造的に成立せず0.04件/組 → **学校単位の継続**で代替）。ただし記事本数は掲載大会数に律速され一般カテゴリで年4本ペース＝ペースを上げたいなら掲載大会・年度を増やす方が効く、(4) `related`は機械的に決まる4語彙のみ（same-subject/same-match/same-entry/counterpart）、筋の判断はLLM側、「該当なし」は`notes.absence`へ吸収、(5) 選定はロジック一次選定でLLMは執筆に専念（候補が少なくコンテキストが問題にならないため）＋出力の機械照合を追加、(6) **掲載先はサイト新規ページを作らない**＝既存の年度別結果ページ`ResultContextBlocks`（注目ポイント）の拡張。ADR-010の想定内で非抵触、LLM散文はSNSへ出すのでADR-005の「サイト本文はテンプレのみ」も維持、(7) 「データ発見」枠は廃止しストーリー／記録の2分類（ドローの厳しさは大会前の情報なので`/news`側が正しい置き場所）。残るのは実装順序・工数の計画。**検証ステップ1として実データからSNS投稿案5本を作成**（実装前にコード変更ゼロで「読まれるか」を測る）。この作業で設計の穴3つを発見: 個人戦の混成ペアがあるため`team-best8-streak`を「学校が◯連覇」と書くと誤り（主語は「所属選手がベスト8以上に残った年数」に限定）、所属は年をまたぐ本文に書けない（丸山海斗はUp Rise→one team）、「同一ペアの継続」が`pair-continuity`としてkind未設計。あわせて試作YAMLに事実誤り3件が見つかり、**機械照合チェックは必須**と確定 → `scripts/verify-story-text.mjs`（`npm run story:verify`）として**実装済み**。照合先は生データにしてYAML生成のバグとLLMの捏造を同時に捕まえる。既知の誤り4パターンを検出し、正しい投稿案5本に誤検知ゼロ。**学生カテゴリは学校を主役にする**方針も決定（インターハイの個人戦は混成ペア0.00%＝0/3,792なので「◯◯高が3連覇」と書いてよい。混成ペアの制約は一般カテゴリ限定。高校は選手が3年で卒業し経年ストーリーの器が学校しかない）。本命は`school-both-events`＝同一年に団体・個人の双方でベスト8以上（男女各3校と希少で、選手単位では原理的に表現できない）。連続出場は6年連続が48校と希少性ゼロで不採用。**YAML生成も実装済み**（`scripts/generate-story-yaml.mjs` / `npm run story:generate`。7kind・天皇賜杯2025で25件、インターハイ2025で18件を生成し、いずれも照合スクリプトを不一致0で通過）。生成側にも姓のみ選手の除外・暦年での連続判定・混成ペア時の主語切替のガードを実装。**LLMへの渡し方も確定**（`docs/story-yaml/PROMPT.md`）: YAMLは13〜18KB＝生データの1/260なので丸ごと渡す（抜粋するとrelatedの筋が切れる）、`records`は執筆対象でないので`--no-records`で外す、出力にstory idを併記させて検証時の追跡を可能にする、評価語（強豪・名門・悲願等）を明示的に禁止する。インターハイ2025で実施し照合を不一致0で通過。**サイト掲載も実装済み**（[ADR-012](../adr/ADR-012-llm-authored-insights-with-machine-verification.md)）: ADR-005の「本文はテンプレのみ・LLM不使用」を「**機械照合を通った散文に限り可**」へ改めた（LLM不使用の根拠だった「捏造を機械的に検出できない」が照合スクリプトの実装で崩れたため）。年度別結果ページの「注目ポイント」に表示し新規URLは作らないのでADR-010とは非抵触。公開条件はprebuildの`check-tournament-insights.mjs`が**その場で照合し直して**強制する（verifiedAtの値は信用しない＝本文改竄でビルドが落ちることを確認済み）。**初稿が「時系列の羅列でストーリーになっていない」と指摘され原因を特定**: 書き方ではなく**YAMLに「今年何が起きたか」が無かった**（生成が経年ストーリーしか出さず、当年の優勝者が1度も登場しない）。`title-won`（当年の優勝・決勝/準決勝の相手とスコア・初優勝か、priority 1）と`recurring-matchup`を追加し、**団体戦の対戦カード検出も修正**（キーが選手名でメンバーが毎年替わる団体戦では再戦を1件も検出できていなかった→学校名キーに変更し「東北対高田商の準決勝が5年連続」を検出）。PROMPT.mdに「今年何が起きたか→勝ち上がりの具体→過去との関係」の構成指示を追加。**進行中大会の日次まとめも実装**（`--in-progress`）: 本当の制約は件数でなく**順位が準々決勝まで確定しないこと**（男子ダブルスは315試合中ベスト8以上のラベルが付くのは最後の7試合＝大会の98%は既存kindが1つも計算できない）。途中で言えるのは勝ち残り／敗退だけで、注目主体を「前年ベスト8以上」に絞ると敗退イベントが362件→10件、3回戦終了時点で全4種目23件と**1日1本に収まる**ため追加の絞り込みは不要。個人戦は前年ベスト8の16人中翌年出場が4人しかおらず絞りすぎると枯れるので勝ち残り側も出す。**インターハイ2026女子ダブルス（3回戦終了時点）で初運用**し、選手単位では2件しか出ないこと（前年ベスト8以上16人中2026年出場は3人＝卒業）を確認。学校単位に広げる必要があるがペア単位では23件でノイズのため`watched-school-progress`として**学校ごとに集約**（4件）。これにより「就実は勝ち残りが無くなった」という記事の軸になる事実が出た（ペア単位のままでは学校として消えたことが読み取れない）。本文は照合16件・不一致0で通過し公開済み | [アイデア](../raw/2026-08-01-idea-news-fact-based-story-categories.md) / [投稿案(全日本)](../raw/2026-08-01-sns-post-drafts-zennihon-championship-2025.md) / [投稿案(インターハイ)](../raw/2026-08-01-sns-post-drafts-highschool-championship-2025.md) / [機械照合](../raw/2026-08-01-story-text-verification.md) / [YAML](../story-yaml/README.md) |

## Open Questions

- 抽出ロジックを新規実装するか、既存（milestone/career-record/tournamentRecords等）と
  統合するか（判断材料は重複コストの実測）。
  → **5分類ストーリーについては既存統合で決着**（`lib/milestones.ts` に新kindを4つ足す形。
  2026-08-01）。基盤全体の方針としては未決。
- ストーリー分類の詳細語彙・粒度（要件定義は「例」止まり）。
  → **5分類ストーリーの範囲では決着**（`best8-streak`/`team-best8-streak`/`self-best`/
  `streak-stopped` の4kindを追加し、`KIND_IMPORTANCE` の既存優先度の下に挟む。2026-08-01）。
- 評価情報（希少性・季節性・話題性・保存価値）の算出方法。
  → **希少性は実測の出現頻度で代替できると判明**（kind別に0.09〜0.86件/組を実測）。閾値設計に
  使えば別立ての評価スコアは当面不要。季節性・話題性・保存価値は未決。
- JSON/YAMLの詳細スキーマ、根拠データ（対象データの型・ID体系）の参照方法。
  → **`related` フィールドの語彙とID採番規則は決着**（2026-08-01）。スキーマ全体は未確定。
- LLM入力後の実際の投稿本文への組み替え方法（プロンプト設計・媒体別テンプレ）は
  今回のスコープ外だが、将来的な接続イメージの整理は必要。
  → **出力先はSNS（X/Threads）でサイトには載せないと決定**（2026-08-01）。媒体別テンプレと
  投稿頻度は依然未検討。
- （新規）高校・中学カテゴリの供給不足。学校単位の継続で一部は埋まるが、高校14組中3組は
  依然0件になる。検出網の拡張が必要か、一般カテゴリに集中するかの判断が残る。

## 関連

- [ADR-005](../adr/ADR-005-news-context-block-architecture.md)
- [news-context-blocks.md](./news-context-blocks.md)
- [rare-events.md](./rare-events.md)
- [data-model.md](./data-model.md)（Knowledge Graphアイデアとのレイヤ関係）
- [score-general-availability.md](./score-general-availability.md)
