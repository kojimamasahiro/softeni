# 西日本ソフトテニス選手権 2024年度 一般男女 エントリー取り込み

日付: 2026-09-15
入力: `2024_B03_40.pdf`（ユーザー提供、12ページ、テキストレイヤーあり）
対象: 一般男子 p1-4（`一般男子 (1)`〜`(4)`）/ 一般女子 p6-7（`一般女子 (1)`〜`(2)`）。ユーザー指定どおり。
     p5/p8 はスコア記入表、p9-12 はシニアの予選リーグ。
出力: `tools/west-japan-2024/doubles-none-{boys,girls}.initialPlayers.json`（男子143ペア / 女子83ペア）
前提: 東日本の同形式取り込み [2026-09-14-east-japan-2024-pdf-entries-import.md](2026-09-14-east-japan-2024-pdf-entries-import.md)

## 形式・方針

東日本2021〜2024と同じ: 所属は紙面表記のまま、県は正式名称、学連は紙面どおり `学連`、tempId は4項目。
既存の西日本 details は 2025女子が `学連`、2026 が `日本学連`（統一ルールは無い。`docs/wiki/data-import.md`「連盟値の扱いの決定」）。

## 東日本との様式の違い（使い捨てスクリプトを修正）

- 県欄が `広島県` のように**接尾辞つき**で印字される（東日本は `広島`）。辞書照合を両方受けるよう修正。
- 左ブロックの2桁のエントリー番号（10〜17）の2桁目が番号列の判定範囲からはみ出し `1` と読まれた。範囲を広げて解消。
- 所属が3行に折り返される（女子13・27 `SHIROYAMA / HOTEL / kagoshima`）。県がペア共通で所属が3行に分かれるときは1つに結合。
- 上記修正後も東日本2024 p5 の出力が変わらないことを確認。

## 紙面の手書き修正（印刷後の書き込み）

テキスト層に**本文と異なるフォントサイズ（9.2〜9.9pt、本文7.4pt）の氏名**と、
**氏名行に重なる横線**（黒＝選手変更、赤＝棄権）として入っている。赤字の `棄権` も文字として入る。
全対象ページを機械的に走査して6件を検出した。

| 種目 | entryNo | 紙面 | 取り込み |
|---|---|---|---|
| 男子 | 4 | `山下 雅斗` に黒線、`井上寛太` を書き足し | `井上_寛太_ヨシザワ_三重県` |
| 男子 | 139 | `橋野 潤` に黒線、`村田匠` を書き足し。共通所属 `小城ソフトテニス協会` にも線、`TEAM SSP` を書き足し | `村田_匠_TEAM SSP_佐賀県` / 相方 `前川_太一_小城ソフトテニス協会_佐賀県` |
| 女子 | 10 | `庄司 梨乃` に黒線、`甲斐理央奈` を書き足し | `甲斐_理央奈_宮崎商業高校_宮崎県` |
| 男子 | 19 | 赤線と赤字 `棄権`（所属欄） | ペアはそのまま。所属に混入した `棄権` を除去して `広島大学` |
| 男子 | 97 | 赤字 `（棄権）`（ブラケット側） | ペアはそのまま |
| 女子 | 13 | 2人とも赤線 | ペアはそのまま |

- **ユーザー判断: 選手変更は変更後の選手、棄権のペアはそのまま残す**（既存 details の棄権ペアの扱いと同じ）。
  変更前の選手（`山下 雅斗` `橋野 潤` `庄司 梨乃`）は本ノートにのみ残す。
- 139 の `TEAM SSP` は**変更後の選手（村田 匠）の所属**と解釈した。既存データで相方の `前川 太一` は
  `小城協会`、`村田 匠` は `TeamSSP` のため（**Assumption**）。
- 書き足しの氏名はフォントが大きく行の外にあるため、抽出ではどのエントリーにも拾われない（取りこぼしの形で壊れる）。
  `TEAM SSP` も行の窓の外で拾われなかった。いずれも手で反映。

## 検証

- 男子 id 1〜143 / 女子 id 1〜83、欠番・重複・空欄なし。県は全件、辞書または `学連`。
- p1・p6 の全件を紙面と照合してユーザー承認後に全ページを流した。
- `data/tournaments/details/**` との「姓+名」完全一致: 男子286人中265人、女子166人中159人（修正前の抽出値）。
- **分割違いで既存に存在: 男子79 `渡具知 豊`**。既存データ側が `渡具|知豊` と割っている。紙面は姓3文字の
  グリッド（`渡具知` が姓枠）で、沖縄の姓 `渡具知` として紙面側が正しいと判断。既存データは直していない。

## 未了

- `data/tournaments/information/west-japan.json` に 2024 年度の定義が無い（2025/2026のみ）。
- スコア未入力。
- 既存 `west-japan/2025/doubles-none-boys.json` は `prefecture: null` が84人（東日本2025男子と同型）。未着手。
- 既存データの `渡具|知豊` の誤分割。

## Compile Log（2026-09-15）

`docs/wiki/data-import.md` の `zennihon_championship_entries.py` 節の東日本の項の後に、西日本2024の様式差と
手書き修正の検出方法を短く追記した。

**取り込んだもの**
- 県欄が接尾辞つき・所属の3行折り返しという東日本との差
- 手書き修正が「異なるフォントサイズの氏名」「氏名行に重なる横線（黒=変更、赤=棄権）」として検出できること、
  書き足しは抽出で拾われず黙って取りこぼされること

**入れなかったもの**
- 6件の個別一覧: 年度固有。raw に残せば足りる
- 選手変更を変更後の選手にする判断: この年度でユーザーが選んだ扱い。他大会で同じ判断が続いたら wiki の方針に上げる
- `渡具知` の誤分割: 1件の個別事例。修正するときに `name-split-aliases` 側で扱う
- 西日本2025男子の県 null: 未着手の課題。着手時に記録する

---

## 追記（2026-09-15）: 2023年度 一般男女 エントリー取り込み

入力: `2023_B03_40.pdf`（ユーザー提供、21ページ、テキストレイヤーあり）
対象: 一般男子 p1-8（`一般男子 (1)`〜`(8)`）/ 一般女子 p11-14（`一般女子 (1)`〜`(4)`）。ユーザー指定どおり。
出力: `tools/west-japan-2023/doubles-none-{boys,girls}.initialPlayers.json`（男子277ペア / 女子136ペア）
形式・方針は2024年度と同じ（選手変更は変更後の選手、棄権ペアは残す）。

### 2024年度との違い: 変更は赤字で刷り込まれている

取消線や書き足しは無く、**変更後の選手・所属が最初から赤字で印字**されている（男子19件・女子6件）。
赤字も通常の文字としてテキスト層に入るので、そのまま取れば変更後の値になる。赤字の `棄権` や取消線は無し。

- 赤字の行は字間に半角スペースが混ざり文字数が7にならない（`藤  井　 良  哉`）。その行だけ
  **姓名の境界を x 座標で決める**よう使い捨てスクリプトを修正（東日本2024 p5・西日本2024 p1 の出力は不変）。
- **赤字の選手は、棄権（`R`）したペアから回された同一選手であることが多い**:
  `藤井 良哉`（男子17 赤 / 233 R）、`松尾 誠`（男子157 赤 / 126 R）、`山本 花菜`（女子93 赤 / 100 R）。
  方針どおり両ペアとも残した。

### 手で直したもの

| 種目 | entryNo | 抽出 | 修正後 | 根拠 |
|---|---|---|---|---|
| 男子 | 214 | 所属・県が空 | `畔柳 侑汰`=松葉クラブ・愛知県 / `寺浦 励`（赤）=太平洋工業・岐阜県 | 選手行の所属が名前行から数pt離れ行の窓から漏れた。紙面で確認 |
| 男子 | 91 | `日本触媒株` | `日本触媒（株）` | 括弧を区切りとして除去していた |
| 男子 | 271 | `北川ジ|ャン瑠加` | `北川|ジャン瑠加` | 行が字間なしの7文字。既存データと一致 |
| 女子 | 36 | `東鈴|菜` | `東|鈴菜` | 既存データ（神戸松蔭女子学院大学ほか8件） |
| 女子 | 57 | `原華|菜子` | `原|華菜子` | 既存データ（松山東雲女子大学ほか9件） |
| 女子 | 91 | `川井|田悠` | `川井田|悠` | 既存データ（熊本学園大学5件） |
| 女子 | 93 | `山本 花菜` 城山観光・京都府 / 相方の所属が空 | `山本 花菜`=城山観光・**鹿児島県** / `中西 彩夏`=ワタキューセイモア・京都府 | 赤字は氏名と所属だけで、県欄は元ペアの `京都府` のまま。城山観光は鹿児島で、同一人物の女子100・既存データも鹿児島県（**Assumption**） |
| 女子 | 103 | `辻󠄀󠄀倉` | `辻倉` | 異体字セレクタ（IVS）を除去。既存データ20件と一致 |

- 36/57/91 は、4枠グリッドでは `1+2` と `2+1`、`3+1` と `2+2` を**座標で区別できない**
  （`東　鈴　　　菜` は `寺　浦　　　励`＝`寺浦|励` と同じ並び）。既存データに同一人物がいた3件だけ直した。
  **既存データに居ない選手で同じ型の誤分割が残っている可能性がある**（未検出）。

### 検証

- 男子 id 1〜277 / 女子 id 1〜136、欠番・重複なし。修正後に空欄なし。県は全件、辞書または `学連`。
- p1・p11 の全件を紙面と照合してユーザー承認後に全ページを流した。
- `data/tournaments/details/**` との「姓+名」完全一致（修正後）: 男子554人中486人、女子272人中257人。別の位置で割ると一致する例は修正後ゼロ。

### Compile Log（追記・2023年度）

`docs/wiki/data-import.md` の西日本の項に、2023年度は変更が赤字で刷り込まれることと、4枠グリッドでは
`1+2`/`2+1` 等が座標で区別できないことを1行ずつ追記した。

**入れなかったもの**
- 手修正8件の一覧: 年度固有。raw に残せば足りる
- 赤字の選手が棄権ペアから回される傾向: 3件の観察で、一般化するには少ない
- 女子93 の県の判断: 1件の個別判断（Assumption として raw に残す）

---

## 追記（2026-09-15）: 2022年度 一般男女 エントリー取り込み

入力: `2022_B03_40.pdf`（ユーザー提供、16ページ、テキストレイヤーあり）
対象: 一般男子 p1-8（`一般男子 (1)`〜`(8)`）/ 一般女子 p10-11（`一般女子 (1)`〜`(2)`）。ユーザー指定どおり。
出力: `tools/west-japan-2022/doubles-none-{boys,girls}.initialPlayers.json`（男子257ペア / 女子125ペア）
形式・方針は2023/2024年度と同じ。

### 2023/2024年度との違い

- **県欄が略称（`広島` `京都`）で、所属と字間を空けずに印字**される。括弧から一定幅を県とみなす区切りだと
  所属の先頭1文字が県に入った（`広島Ｎ` `学連熊`）。**行の先頭が47都道府県名（接尾辞の有無とも）か `学連` かで
  県と所属を切り分ける**よう使い捨てスクリプトを修正。
- 氏名が字間なしの行（`守屋優人` `中野倖太郎` `竹田真樹` `川口真歩`）や、4文字姓で7文字行の4文字目が埋まる行
  （女子2 `小茄子川　夏月`）は x 座標で姓名を分割。
- 女子は1ページに63ペアが詰まる（行送りが狭い）が、行の取り違えは無し。
- 手書きの書き足し・赤字・取消線は対象全ページで0件。
- 修正後、東日本2021/2022/2024・西日本2023/2024 の計9ページの出力が変わらないことを確認
  （最初の回帰は zsh の `set --` が引数を分割せず空振りしていたため、Python から回し直した）。

### 手で直したもの

- 男子3: 所属 `プラスワン` がペアの下の行寄りに印字され、1人目 `田中 剛士` の所属が空になった。ペア共通なので2人とも `プラスワン`・滋賀県。
- 女子37: 県欄はペアで1つの `岐阜` だけだが、2人目 `岡 美志` の所属は `三重高校`。既存データでは `岡_美志_三重_三重県` が10件。
  **ユーザー判断で `三重県`**（紙面の県欄は1人目 `薮内 祥子`／太平洋工業の県と解釈。**Assumption**）。

### 直していないもの

- 同一選手が2ペアに出る（いずれも片方に `R`＝棄権のマーク）: `峯松 健太朗`（男子42 R / 210）、`森田 新乃祐`（男子108 / 196 R）。方針どおり両ペアとも残した。

### 検証

- 男子 id 1〜257 / 女子 id 1〜125、欠番・重複なし。修正後に空欄なし。県は全件、辞書または `学連`。
- p1・p10 の全件を紙面と照合してユーザー承認後に全ページを流した。
- `data/tournaments/details/**` との「姓+名」完全一致: 男子514人中445人、女子250人中237人。別の位置で割ると一致する例はゼロ。

### Compile Log（追記・2022年度）

`docs/wiki/data-import.md` の西日本の項に、2022年度は県が略称で所属と詰めて印字され、辞書で切り分ける必要があることを1行追記した。

**入れなかったもの**
- 手修正2件・同一選手2件の一覧: 年度固有。raw に残せば足りる
- zsh の `set --` で回帰が空振りした件: この作業のシェル操作の失敗で、データ取り込みの知見ではない
- 女子37 の県の判断: 1件の個別判断（Assumption として raw に残す）

---

## 追記（2026-09-15）: 2021年度 一般男女 エントリー取り込み

入力: `2021_B02_40.pdf`（ユーザー提供、13ページ、テキストレイヤーあり）
対象: 一般男子 p1-4（`一般男子 (1)`〜`(4)`）/ 一般女子 p5-6（`一般女子 (5)`〜`(6)`）。ユーザー指定どおり。
出力: `tools/west-japan-2021/doubles-none-{boys,girls}.initialPlayers.json`（男子188ペア / 女子113ペア）
形式・方針は2022〜2024年度と同じ。

### 年度の確認

ユーザーの依頼文は「2022年の西日本」だったが、PDF のタイトルメタデータは `第75回 西日本選手権大会`、作成日 2021-08-04。
既存の information（2025=第79回、2026=第80回）から逆算して第75回＝**2021年度**。2022年度（第76回）は取り込み済み
（`2022_B03_40.pdf`）のため、2021年度として出力した。ファイル名の大会コードもこの年だけ `B02`（2022〜2024 は `B03`）。

### 2022年度との違い

- **高校のペアは県欄に `高体連` と印字**される（男子16人・女子16人、8校）。
  **ユーザー判断で学校の所在地の都道府県に置き換えた**（`高体連` のまま残す案は採らなかった）。
  校名と `data/highschool/teams.json`・details の既存表記で、いずれも1県に決まった:

  | 学校 | 県 |
  |---|---|
  | 関西中央高等学校 / 奈良育英高等学校 | 奈良県 |
  | 尽誠学園高等学校 | 香川県 |
  | 上宮高等学校 | 大阪府 |
  | 徳島科学技術高等学校 / 徳島県立つるぎ高等学校 / 徳島県立脇町高等学校 | 徳島県 |
  | 和歌山信愛高等学校 | 和歌山県 |

  抽出スクリプトは `高体連` を県欄のトークンとして認識するよう修正（西日本2022〜2024・東日本2021〜2024の計11ページの出力は不変）。
- 変更は2023年度と同じく**赤字で刷り込み**（6ページで19箇所、例: 男子20 `平松 航志朗`／五島市協会、女子2 `栗山 向日葵`）。取消線・書き足しは0件。

### 検証

- 男子 id 1〜188 / 女子 id 1〜113、欠番・重複・空欄なし。置換後の県は全件、47都道府県または `学連`。
- p1・p5 の全件を紙面と照合してユーザー承認後に全ページを流した。
- `data/tournaments/details/**` との「姓+名」完全一致: 男子376人中322人、女子226人中200人。別の位置で割ると一致する例はゼロ。

### 直していないもの

- `岡本 優希乃` が女子51（赤字）と62 の2ペアに出る（いずれも東洋観光）。赤字の選手変更で同一選手が2ペアに入る型。方針どおり両ペアとも残した。

### Compile Log（追記・2021年度）

`docs/wiki/data-import.md` の西日本の項に、2021年度は高校ペアの県欄が `高体連` で、学校所在地の県に置き換えたことと、
ファイル名の年度・依頼文の年度を PDF タイトルの回次で裏取りすることを1行追記した。

**入れなかったもの**
- 8校と県の対応表: 年度固有。raw に残せば足りる
- 依頼文の年度誤り: 1回の個別事例（回次で裏取りする手順だけ wiki に上げた）
- `岡本 優希乃` の2ペア: 既に wiki にある「同一人物が複数エントリーに出る」型

---

## 追記（2026-09-15）: `information/west-japan.json` に 2021〜2024年度を追加

ユーザー依頼「今回追加した大会情報を追加」。PDF に日程・開催地の記載は無いため、**日本ソフトテニス連盟の年度別「大会結果」ページ**
（`https://www.jsta.or.jp/result/result{年度}`）を一次ソースとした。結果 PDF のリンク先
（2021 `2021_B02_40.pdf`、2022〜2024 `{年度}_B03_40.pdf`）が取り込んだファイル名と一致することで対応を確認。

| 年度 | 回次 | 期日 | location | JSTA の開催地表記 |
|---|---|---|---|---|
| 2024 | 第78回 | 2024-06-29〜30 | 沖縄県 | 沖縄市 |
| 2023 | 第77回 | 2023-07-08〜09 | 岡山県 | 備前市・岡山市 |
| 2022 | 第76回 | 2022-07-09〜10 | 三重県 | 鈴鹿市 |
| 2021 | 第75回 | 2021-07-10〜11 | 長崎県 | 長崎市 |

- `source` は `公益財団法人 日本ソフトテニス連盟`、`sourceUrl` は各年度の結果一覧ページ。`categories` は2025/2026と同じ男子一般・女子一般。
- 2024年度は6月開催（他年度は7月）。JSTA の表記どおり。
- `venues` と `guidelineUrl` は未入力（2025/2026 の西日本エントリにも無い）。

### Compile Log（追記・information）

wiki 追記なし。

**入れなかったもの**
- 年度ごとの期日・開催地: データ本体（`information/west-japan.json`）が正。wiki に重複させない
- venues 未入力: 未着手の作業
