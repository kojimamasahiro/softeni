# アイデア: AIが自律的にサービスを改善する仕組み（検知・検証の全体委譲と自己修復）

## 状況

Idea Backlog。発散フェーズだが**方向は決着した**（2026-09-06）。
出発点となるドメイン（PDF取り込み系）と、成立条件（抜き取り監査＋非対称な境界移動）まで合意。
実装は未着手。

## 目的

サービス運用のうち**検知・検証の全体**をAIに委譲し、人間は方向の判断だけを持つ状態にしたい。
背景にあるのは「AIソロプレナー」的な関心と、「自律して目標を立てて内外のデータを取得して
タスクを完了するAIエージェントを駆使できるようになりたい」という関心。

既存の姉妹スレッド2本との関係:

- [AIとの共同探索と探索プロセスの属人性](./2026-07-11-idea-ai-co-exploration-context.md) —
  **人間が主でAIが従**の前提で探索の質を上げる話。本アイデアはその上位（人間が主導権を持たない層）。
- [skillのローカルLLM代替](./2026-08-14-idea-local-llm-skill-replacement.md) —
  **人間が与えたタスクをAIが遂行する**話。本アイデアはその上位（タスクを誰が決めるか）。

本アイデアは「AI自身が何をやるか決める」層で、バックログでは初。

## ユーザーが興味を持った点

原文（時系列）:

- 「AIエージェントなどを使って、AIが自律的にサービスを改善する仕組みを検討したい。いわゆる
  **AIソロプレナー**というものかもしれない。（中略）**自律して目標を立てて内外のデータを取得して
  タスクを完了するAIエージェント**が駆使できるようになることも興味がある」
- (a)サービスの自走 と (b)エージェントを駆使できること の関係について:
  「**(b)をすることで(a)になるのではないか。もしくは、(a)をするために(b)をするのではないか**」
- 任せたいことの正体:
  「**任せたいことはAIの自律性を向上さて "床" を高めて方向を人間が判断できるための土台づくり**」
  「**任せたいことは課題発見かもしれない**」
- 委譲の範囲:
  「**検知や検証の全体を任せていくというのはできないのかどうか。もちろん、最終判断は人間にしたい**」
- 「（検出器の穴を測る案は）**かなり狭い範囲だと感じ、それをすることで仕組み作りにつながる
  イメージがつかない**」（→ Claude の提案の位置づけ誤りを正した指摘。下記「経緯」参照）
- 境界（自動でよい範囲）の決め方について:
  「**自動化したい。ただ、そこにも自己修復できるようにして、間違いがないか、改善する余地は
  ないかを判定して、時には人に判断を任せつつ、自己修復されるようにしておきたい**」

## 想定運用 / なぜこのスコープか

### 「充実」を目的関数に置く（PV・収益は置かない）

ユーザーの見立て:「トップページの『ソフトテニスを「記録」としてたどれる場』が充実していくことで、
収益・PV・掲載データ量・手作業時間の削減というのは説明変数になるのか」。

方向は妥当だが、挙がった4つは**役割が3種類に分かれる**（Claude の整理）:

| 変数 | 役割 | 目的関数に置けるか |
|---|---|---|
| 手作業時間の削減 | 真の説明変数（上流の原因。容量が増える） | ○ |
| 掲載データ量・接続性・正確性 | 「充実」の構成要素そのもの（説明変数ではない） | ○（定義が要る） |
| PV・収益 | 下流の結果（遅行指標） | **×** |

PV・収益を自律エージェントの目的関数に置けない理由は、**本リポジトリに4件の実証がある**。
どれもPVが上がる選択肢を、ミッションに反するとして人間が退けたもの:

1. AdSense全面展開時、本文78文字の空ページにも広告が出る穴を見つけ、
   **本文400文字未満は出さないゲートを足して417枚を自ら除外**した
2. **優勝予想はやらない**（2026-07-31決定。2026-08-31に「事前Elo上位4組＝準決勝4組」と
   実測で当たることを確認してもなお見送り）
3. [ADR-010](../adr/ADR-010-retire-result-articles-consolidate-to-hub.md) で結果記事を廃止しハブへ統合（ページを減らす判断）
4. 選手一覧の姓頭文字分割ページを不採用（薄いページの量産になるため）

**PVを目的関数に持ったエージェントなら4件とも逆の判断をする**。しかも短期的にはどれもPVが
上がるので、指標の上では「改善」に見える（Goodhart）。

一方「記録としてたどれる」は機械で測れる概念で、**断片は既に測られている**が一度も束ねられていない:
網羅性（掲載大会数・年）/ 接続性（内部リンク本数 選手一覧97→1,917本、学校ページ間0本、
`check-orphan-entries` の孤立エントリー）/ 正確性（照合カバレッジ26.4%）/
同一人物として辿れるか（識別ヘルスの要対応シグナル72→42、別名残存4→0）。

### 委譲の単位は「機能の一部」ではなく「1ドメイン丸ごと」

理由は下記「わかっていること」の583件（部分適用が実際に止まった実証）。

## わかっていること

### 1. 完成例が既に1つある — チーム名寄せ

「検知・検証の全体委譲」は、本リポジトリで**既に1ドメインだけ完成している**。

```
① 候補を機械が出す         scripts/build-team-merge-candidates.mjs（コア一致／選手共有の2シグナル）
② 自動でよい分を機械が決める  scripts/apply-auto-merges.mjs
③ 残りを人が判断する道具を機械が作る  scripts/build-team-review-html.mjs
④ 判断を機械が反映する      scripts/apply-team-aliases.mjs
⑤ 起動                     ← ここだけ人
```

③は「チーム名だけでは判断できない」ことを前提に**選手名・年代・主な大会の文脈を添え、
1クリックで判断でき、確認済みはリストから消える**設計。人間は判断だけをする。

**②に「最終判断は人間」が思想でなくコードとして書かれている**のが要:

> 自動OK: 同一グループ内で「同一大会(大会id+年)」に表記が同居しなければ自動OK。
> **ただし signal:"players"（選手共有シグナル）のクラスタは常に自動OKにしない（人手レビュー専用）。**

→ **人間が全部を見なくても「最終判断は人間」は保てる。機械が判断してよい範囲を一度定義すれば、
その外側だけが人間に来る。**

### 2. 部分適用は実際に止まった — 外部データ取得クローラーの583件

```
data/local-sources/detected-documents.json
  総数 583件 / status: 全件 "new"
  ignored 0件 / accepted 0件 / appliedAt を持つもの 0件
  検出日 2026-05-24 〜 2026-06-12
```

`crawl:local-tournaments`（県連サイトを巡回してPDFを検出）と
`apply-accepted-qualifiers.mjs`（accepted を反映）は両方実装済みなのに、
**間の仕分けが人手だったため583件が3ヶ月放置され、accepted が1件も無いので反映側は
一度も仕事をしていない。クローラー自体も2026-06-12以降走っていない。**

→ **①だけ自動化すると人の可処分時間が律速になり、仕組みごと止まる。**
同じ兆候が teamId 目視確認252件にもある。

### 3. 「提案はLLM・採否は機械」の型が3回、独立に発明されている

- `scripts/pdf-to-players/tuning.py` の `repair()` — LLMが4段階の温度で列判定を提案 → 機械が採点 →
  外れた内訳をフィードバックして最大3回直させる。docstring に
  **「採否を決めるのは常に機械（`score_entries`）で、LLMの自己申告は使わない」**
- [ADR-012](../adr/ADR-012-llm-authored-insights-with-machine-verification.md) — 記事は機械照合を通ったものだけ公開（prebuild が強制）
- `npm run ranking:backtest` — 過去データで再生して採点

**自己修復ループの型は既にある。** 今回の要求は「この型を②（境界の決定）自身に適用する」こと。

### 4. ②は既に1箇所で自動化されていて、一度壊れている

`scripts/pdf-to-players/tuning.py`:

```python
GOOD_ENOUGH = 0.75   # これ未満ならLLMに聞く＝機械が自分で「自信がない」と判定する境界
if a.score >= 0.95:  # 早期打ち切り
score *= 0.6         # 退化解釈へのペナルティ
```

2026-08-14のインカレ対応時、**「存在しない項目を減点する設計だったため点数の上限が0.74になり、
目安0.75に到達不能」**という形でこの境界が壊れていた。
→ **境界が誤っていても症状は「なんとなく上手くいかない」としてしか出ない。
②を自動化するなら、境界が正しいかを測る仕組みが同時に要る。**

### 5. 学習材料の実測 — 人の判断は6件しかない

| 材料 | 件数 |
|---|---|
| `data/teams/team-alias-additions-auto-reviewed.json`（人が確定した判断） | **6件** |
| `data/teams/team-name-mappings.json` | 67件 |
| `scripts/pdf-to-players/fixtures/` | **PDF 2本**（うち回帰103アサーション） |

レビューHTMLは判断を `localStorage` に持つため、**押した判断の大半がブラウザ内で消えている**。

一方、**二経路照合による正解データは潤沢**:
PDF抽出 vs 人が作った `initialPlayer.js` = **49件全項目一致** /
インカレ姓名分割 vs 既存選手データ = **796名中793名一致(99.6%)、不一致3件は既存データ側の誤り** /
`bracketLayout` vs 全データ = **209大会 20,571試合一致・不一致0**。

→ **「人の判断を学ぶ」経路は今は成立しない（6件）。「独立した第二経路と突き合わせて
自分を採点する」経路なら今日から成立する。** 後者のほうが設計として優れてもいる——
前者は機械が人間の偏りに収束する（[2026-07-11 の論点1「均質化」](./2026-07-11-idea-ai-co-exploration-context.md)そのもの）。

### 6. 検出器11本はすべて事後に生まれている

`check-*` / `verify-*` の追加日を git で全数確認（2026-09-06時点）:

```
2026-05-19 check-growth-analysis      2026-08-02 check-orphan-entries
2026-06-26 check-identity-health      2026-08-04 check-highschool-pipeline-freshness
2026-07-19 check-tournament-entries   2026-08-26 check-duplicate-placements
2026-07-31 verify-bracket-layout      2026-08-26 check-upcoming-tournaments
2026-08-01 check-tournament-insights  2026-08-29 check-name-splits
2026-08-01 verify-story-text
```

**11本すべてが事後**。ソース冒頭のコメントが例外なく「何が壊れてから作ったか」で始まる。
8月だけで5本＝増加ペースが上がっている（rawノートも 6月10 → 7月35 → 8月65本）。
**人間の発見負荷が増え続けている。**

これは2026-07-11の論点3（**「探索の品質」は既知の失敗モードの不在として負に定義する**）を、
理論として書いた翌月に、11本の検出器として無自覚に実践していたことを意味する。

### 7. 本リポジトリの主要な失敗モードは「静かに間違う」

`check-tournament-entries.mjs` 冒頭:
**「統計エンジンはこれらを『相方不明』として黙って除外するため、表示だけ見ても気付けない」**。
`check-orphan-entries` の70試合消失、`check-duplicate-placements` の二重計上も同型。

### 8. 走る場所が無い

`.github/workflows` が存在せず CI ゼロ。全部ユーザーの Mac で手動 `npm run`。
**仕組みが人の起動を要求する限り、それは仕組みではない。**

## 設計の骨子（合意済み）

### 人間の判断を「キューの処理」ではなく「機械の抜き取り検査」に使う

機械が全件を処理し、**人は無作為に少数（例:20件）だけ見る**。得られるのは処理結果ではなく
**機械の誤り率**で、これが (a) 境界が正しいかの判定材料になり、(b) 不足していた人の判断ラベルにもなる。

**人の時間が件数に比例しなくなる（線形→定数）。** これが「全体を任せる」と
「最終判断は人間」が両立する形。

自己修復はここから自動的に出る:
**誤り率が許容を超えたら境界を自動で締める、下回り続けたら少しずつ緩める。**

### 境界の移動は非対称にする（Claude の提案・ユーザー未確認）

- **締める方向**（人に回る量が増える）→ 安全。自動でよい
- **緩める方向**（機械が決める量が増える）→ **独立した二経路照合で裏が取れたときだけ**

理由は「わかっていること 7」。緩めれば人の負荷は下がり、指標上は改善に見え、誤りは黙って通る。

### 課題発見の3階層（Claude の整理・ユーザー未確認）

ユーザーの「任せたいのは課題発見」は "床" と "方向" の境界上にあるため分解した:

- **(i) 規約違反の発見** — 決めたルールが守られていない箇所を探す。純粋に床。全部任せてよい
- **(ii) 検出器の穴の発見** — 「守っているつもりのルールが機械で守られていない」を探す。
  メタだが保証範囲の内側なので任せられる
- **(iii) 課題設定** — 何を問題とみなすか。任せると均質化が最も強く出る。**人間に残す**

(ii) には本リポジトリに1例だけ実績がある: 2026-08-14の**逸脱注入テスト**（インサイトに8種の逸脱を
注入し、照合器が落とせたのは2種だけ）。ここから「実効カバレッジ26.4%」「記事の主語が0/24で未照合」
という、人間が気付いていなかった課題が出た。

## 課題・未解決

- **境界の自動調整と「静かな劣化」の切り分け**。緩める方向の非対称ルールは提案止まりで未検証。
- **抜き取り監査の標本設計**（何件で誤り率をどの精度で測れるか、層別が要るか）。
- **人の判断が repo に残らない問題**（`localStorage`）。抜き取り監査を回すなら先に直す必要がある。
- **走る場所（CI/スケジューラ）の選定**。Cloudflare Pages 前提のリポジトリで、
  GitHub Actions を入れるのか別手段かは未検討。
- **「充実」を1つの数字に束ねる作業**は未着手（材料は揃っているが定義が要る）。
  出発点をPDF取り込みにしたため後回しになっている。
- **(iii) の線引きを誰がいつ見直すか**。人間に残すと決めたが、見直しの契機が未定義。
- クローラーの583件は**捨てる**判断（2026-09-06）。ただし
  「残すならクローラーの設計自体を見直したい」とのことで、外部データ取得の再設計は未着手のまま残る。

## 目指したい方向性

**出発点は PDF 取り込み系**。理由は条件が最も揃っているため:

- 境界（`GOOD_ENOUGH = 0.75`）が既にある
- 自己修復ループ（`tuning.repair`）が既にある
- 二経路照合の正解データが潤沢（49件 / 796名 / 20,571試合）
- **境界が一度壊れた実例がある**（上限0.74問題）
- ユーザーが「全部任せたい」と挙げた「PDF取り込みからの試合結果反映」と同じレーン

欠けているのは**抜き取り監査**と**境界を動かす非対称ルール**の2つだけ。

なお **PDF→エントリーは自動化済み**（`scripts/pdf-to-players/`、回帰103 pass）だが、
**PDF→試合結果はskill（`tournament-pdf-to-results`）だけでスクリプトが無い**。
手作業が残っているのは後半。

## 関連

- [2026-07-11 AIとの共同探索と探索プロセスの属人性](./2026-07-11-idea-ai-co-exploration-context.md) —
  姉妹（ミクロ層／人間主導）。論点1（品質の分散は減らし方向の分散は残す）と
  論点3（品質を失敗モードの不在として負に定義）が本アイデアの設計原則として直接効いている。
- [2026-08-14 skillのローカルLLM代替](./2026-08-14-idea-local-llm-skill-replacement.md) —
  姉妹（タスク遂行層）。`tuning.repair` / 逸脱注入テストの出所。
- [ADR-012](../adr/ADR-012-llm-authored-insights-with-machine-verification.md) — 「提案はLLM・採否は機械」の型の明文化。
- [docs/wiki/open-questions.md](../wiki/open-questions.md) — 本アイデアの掲載先（プロジェクト運用/メタ）。
- コード: `scripts/build-team-merge-candidates.mjs` / `scripts/apply-auto-merges.mjs` /
  `scripts/build-team-review-html.mjs`（完成例）、`scripts/pdf-to-players/tuning.py`（②と自己修復）、
  `scripts/crawl-local-tournaments.mjs` / `scripts/apply-accepted-qualifiers.mjs`（止まった部分適用）。

## 参考文献

- Goodhart's law（指標を目標にすると指標として機能しなくなる）— PV・収益を目的関数に置かない根拠。
- 受入抜取検査（acceptance sampling）— 抜き取り監査の元になる考え方。
- selective prediction / learning to defer — 「機械が自信のないものだけ人に回す」境界の定式化。
  （注: いずれも Claude の知識からの参照で、本スレッドでは一次情報を未確認。実装前に確認する。）

## Compile Log

wiki（`open-questions.md` の Idea Backlog 表）へは1行要約のみ記載し、下記は**意図的に raw だけに留めた**:

- **「充実」を構成する4指標の断片リスト**（網羅性・接続性・正確性・同一人物性）— まだ定義でなく
  材料の列挙にすぎず、束ね方が決まっていないため wiki に載せると確定事項に見える。
- **課題発見の3階層 (i)(ii)(iii)** — Claude の整理でユーザーの確認を取っていないため。
- **境界移動の非対称ルール** — 同上（提案止まり・未検証）。
- **検出器11本の追加日一覧** — 「11本すべて事後」という結論だけを wiki に載せ、明細は raw に残す。
  明細は再現可能（git log）なので wiki で持つ価値が薄い。
- **PVを目的関数に置けない根拠の4事例** — 各事例は既に `monetization.md` / `sns-story-platform.md` /
  ADR-010 に記載済みで、ここでの新規性は「4件を1つの論拠として束ねたこと」だけ。重複を避けた。

---

## 追記（2026-09-06・同日）: (1)「走る場所」に着手 — 前提の誤りの訂正と実装

ユーザーの「いったん(1)を進めてどうなるか見てみたい」を受けて着手。**着手直後に本文の前提が
1つ誤っていたことが判明した**ので、まずそれを記録する。

### 訂正: 「走る場所が無い（CIゼロ）」は誤り

`out/` は `.gitignore` 対象でコミットされておらず、`wrangler.toml` の
`pages_build_output_dir = "out"` と [deployment.md](../wiki/deployment.md) のビルド時間内訳
（clone + `npm ci` 24秒 → prebuild 2分6秒 → … → upload + deploy 40秒、計約9分）から、
**Cloudflare Pages が push ごとにビルドしており `prebuild` はそこで自動実行されている**ことが確定した。
人の起動なしに動く場所は既にあった。

### 訂正後の、より正確な問題: prebuild には「ゲートしか置けない」

検出器の実行状況を全数確認した結果:

```
prebuild に入っている（push で自動実行）: 4本
  check-tournament-entries / check-tournament-insights
  check-highschool-pipeline-freshness / check-name-splits --strict

入っていない（人が思い出したときだけ）: 7本
  check-orphan-entries / check-duplicate-placements / check-upcoming-tournaments
  check-identity-health / check-growth-analysis / verify-bracket-layout / verify-story-text
＋ テスト・検証系10本（bracket:test / analytics:test / upcoming:test / club:verify /
  playerstats:verify / playerstats:test / ranking:backtest / bracket:verify:tree / story:verify ほか）
```

**11本中7本が休眠していた。** 原因は忘れっぽさではなく構造で、休眠中の検出器には**2種類ある**:

- **ゲート型**（合否が意味を持つ）: orphan-entries / duplicate-placements / growth-analysis /
  verify-bracket-layout
- **キュー型**（人の判断待ちの一覧。設計上いつも未処理を返す）: identity-health（exit 1 が既定）/
  upcoming-tournaments（「終了コードは常に 0。運用の残タスク一覧であり、ビルドを止めるエラーではない」）

**prebuild はビルドを止める場所なのでキュー型を置けない。置き場が無かったから休眠した。**
これは本文の「①だけ自動化すると詰まる」と同型の現象が、実行環境の側でも起きていたということ。

なお `verify-story-text` は引数必須の**記事単位のツール**（`-t` / `-c` が要る）で、
リポジトリ全体を見る検査ではないため CI 対象外。全体側の強制は prebuild の
`check-tournament-insights` が担っている（ADR-012）。

### 休眠の代償が実際に出ていた（本アイデアの実証）

休眠中の7本をその場で全部走らせたところ、**`verify-bracket-layout` が落ちた**:

```
復元適用: 443 大会 / 復元不可(no-seed-info): 9 大会
試合の一致: 38481 件 / 不一致: 250 件
  highschool-championship/2013/doubles-none-boys.json  (2 件)
  zennihon-singles/2017/singles-none-boys.json       (248 件)
```

wiki の記録は「209大会・20,571試合が一致／**不一致0件**」（2026-08時点）。git で追うと:

```
zennihon-singles/2017/singles-none-boys.json       2026-09-04 追加「全日本シングルス 2017」
highschool-championship/2013/doubles-none-boys.json 2026-09-04 追加「インハイ2013」
verify-bracket-layout.mjs                          最終更新 2026-08-22（ラウンドロビン対応）
```

**2日前の取り込みが検証を通っておらず、誰も気付いていなかった。**
検出器は存在し、正しく動き、正しく落ちるのに、走らせる契機だけが無かった。
「検出器を増やす」ではなく「走らせる」が効く局面がある、という直接の実例。

（データ側の誤りか検証ロジック側の未対応かは**未判定**。roundrobin 起因の偽陽性の前例が
[2026-08-22 のノート](./2026-08-22-bracket-slot-parity-roundrobin-false-positive.md) にあるため、
断定せず「要確認」として別途扱う。）

### 実装したもの: `.github/workflows/checks.yml`

リポジトリは PUBLIC なので **GitHub Actions は無料・無制限**（コスト制約なし）。
Cloudflare Pages と役割を分け、**CF に置けないもの**だけを引き受ける設計にした:

| CF Pages（既存） | GitHub Actions（今回） |
|---|---|
| push 契機のみ | push + **cron（毎週月 09:00 JST）** + 手動実行 |
| ゲートのみ（落ちるとデプロイが止まる） | **ゲートと報告を分離**。報告はデプロイに影響しない |
| 結果はビルドログ | ジョブサマリに一覧が残る |

- **ゲート（落ちたら赤）**: check-orphan-entries / check-duplicate-placements /
  check-growth-analysis / bracket:test / analytics:test / upcoming:test / club:verify
- **報告のみ（赤にしない）**: verify-bracket-layout / check-identity-health /
  check-upcoming-tournaments → `$GITHUB_STEP_SUMMARY` に出力

`verify-bracket-layout` は現在250件で赤なので、**あえてゲートに入れず報告側に置いた**
（赤い CI を常態化させないため）。250件が解消したらゲートへ昇格させる。
これは本文の非対称ルール「**締める方向の変更は自由**」の最初の適用にあたる。

ローカルで CI と同じ手順を通しで実行した結果: **ゲート7本すべて PASS・合計3秒**
（CI では checkout と `npm ci` を足して1〜2分の見込み）。

### 今回やらなかったこと

- **Python の回帰テスト**（`scripts/pdf-to-players/test_regression.py` 103件、
  `scripts/venue-agent/test_regression.py` 5件）は **requirements.txt 等の依存定義が
  リポジトリに無い**ため見送り。CI に載せるには依存の固定が先。
- **重い検証**（`playerstats:verify` / `playerstats:test` / `ranking:backtest`）は未計測のため見送り。
- **書き戻し**（Actions からリポジトリへコミットする）は未着手。抜き取り監査を回すには要る。
  `permissions: contents: read` のままにしてある。

### この時点での状態

- **cron の実行環境ができた** = 本アイデアの前提のうち「定期実行」は解けた。
- 残るのは「**リポジトリに書き戻せる実行環境**」と「レビュー判断が localStorage から出ないこと」。
  この2つは抜き取り監査の直接の前提なので、次に効くのはここ。

---

## 追記2（2026-09-06・同日）: PR #85 マージ後の実測と、書き戻しの先例

### 実測

- PR #85 マージ済み。**PR上の初回実行 39秒 / main での実行 41秒、いずれも13ステップ全て success**。
  https://github.com/kojimamasahiro/softeni/actions/runs/34003422641
- 報告のみの3本も `$GITHUB_STEP_SUMMARY` に想定どおり出力された（ユーザーが画面で確認）。
- **基準値**（次回以降の差分の起点）: ブラケット復元 = 復元適用443大会 / 復元不可(no-seed-info)9大会 /
  一致38,481件 / **不一致250件**。

意味があるのは、この250件が「**赤くならずに、しかし見える**」状態になったこと。
それまでは「prebuild に入れる＝250件が消えるまでデプロイ不能」か
「入れない＝誰も気付かない（実際に2日間そうだった）」の二択しか無かった。

### 発見: 書き戻しエージェントは一度作られ、2日で止められていた

`.github/workflows/sitemap.yml` の履歴（削除済み・git履歴から復元）:

```
2025-05-02 52f83071  追加。on: schedule cron '0 3 * * *'（毎日3時JST）+ workflow_dispatch
                     node scripts/sitemap-generator.js で生成し、
                     github-actions[bot] が git commit && git push する
2025-05-04 4a061288  トリガーを削除 → on: []（追加の2日後）
2025-10-24 66d1d95c  ファイルごと削除（同日 conclusion=failure のランが1件、実行時間0秒）
```

**自動コミットは1件も残っていない**（`--grep="[auto]"`・`--author=github-actions` とも0件）。
動く前に止められている。

**なぜ廃れたかは実装から読める**: `scripts/sitemap-generator.js` は現存せず、
sitemap は `postbuild` の `next-sitemap` がビルド時に生成する形に置き換わった
（[deployment.md](../wiki/deployment.md)）。つまり
「**生成物をリポジトリにコミットして戻す**」→「**ビルド時に作る**」への移行であり、これは正しい判断。

### この先例の読み方（次のステップへの含意）

書き戻しには2種類あり、先例が否定したのは片方だけ:

| | 例 | リポジトリに書き戻すべきか |
|---|---|---|
| **再生成できるもの** | sitemap / players-lite / facts | **不要**。ビルド時に作ればいい（先例の教訓） |
| **再生成できないもの** | 人の判断、抜き取り監査の結果、誤り率の履歴 | **必要**。他に情報源が無い |

抜き取り監査が要求するのは後者。「機械がこう判断し、人がこう覆した」は計算で復元できない。
**本文で指摘した「人の判断が6件しか残っていない」は、これを残す場所が無いことの帰結**。

したがって先例は「書き戻しをするな」ではなく「**生成物を書き戻すな**」と読むのが正確。
次に `permissions: contents: write` を検討する際は、対象を
「再生成できない記録」に限る、という線引きを設計に入れること。

### 副次的な観察（未対応）

`.gitignore` は `/public/sitemap.xml` `/public/sitemap-*.xml` `/public/robots.txt` を挙げているが、
**3ファイルとも git に追跡されたまま**（gitignore は追跡済みファイルを外さない）。
ただし 2026-05-30 `9eff918c` に「Remove sitemap.xml ...」を **Revert したコミットがある**ため、
外そうとして戻した経緯がある。現時点で実害は無い（postbuild が `out/` 側を上書きするため）ので未対応。
触るなら Revert の理由を先に確認すること。

## Compile Log（追記2ぶん）

- **sitemap.yml の先例**は wiki（deployment.md）へは載せず raw に留めた。既に削除済みの
  ワークフローであり、現行のデプロイ手順の説明にはならないため。ただし
  「生成物は書き戻さない／再生成できない記録は書き戻す」という**線引きは次の実装時に効く**ので、
  open-questions.md の該当行に一言だけ反映する。
- **`.gitignore` と追跡状態のずれ**は wiki に載せない。Revert の理由が未確認で、
  結論が出ていないため（記録のみ）。

---

## 追記3（2026-09-06・同日）: (2) レビュー判断をリポジトリに残す

追記2で「次に効くのは書き戻し」と書いたうちの、**判断の置き場**のほうに着手した。
先例（sitemap.yml）の教訓「生成物は書き戻すな／再生成できない記録は書き戻す」の後者にあたる。

### わかったこと1: 判断は配列インデックスで持たれていた

`build-team-review-html.mjs` の state は `state[i]`（`merge-candidates.json` の配列インデックス）で
保持され、localStorage のキーは `team-merge-review-v9`。**候補は再生成のたびに並びが変わる**ので、
過去の判断は「取り違える」か「KEY のバージョンを上げて捨てる」かの二択だった。
v9 まで上がっていること自体が、9回捨てた記録と読める。

しかもこの問題は**既に認識され、回避運用でしのがれていた**。`team-review-server.mjs` の冒頭:

> 注: 反映は確認済クラスタのみ。候補一覧(merge-candidates)は会話中は作り直さない
> **（番号がずれるため）**。セッション終了後に再生成してください。

「人が気をつける」で回していた、という点で本アイデアの主題そのもの。

### わかったこと2: 否定の判断は一切残っていなかった

`buildOutput()` は `aliasEntries()`（＝統合するグループ）だけを書き出す。
**「これは別チームだ」と判断しても出力は空になり、どこにも残らない。**
機械の自動OK判定が正しかったかを測るには、むしろ否定のほうが要る。

また自動OKクラスタは既定で `reviewed:true` になるため、出力では
**機械の判断と人の判断が混ざり、区別が付かない**。
`team-alias-additions-auto-reviewed.json` が6件しかないのはこの構造の帰結。

### 訂正: 「`/apply` を受けるサーバは存在しない」は誤り

会話中にそう断定したが誤り。`scripts/team-review-server.mjs`（2026-06-26 追加・55行）が実在し、
`http://localhost:5173` で配信＋`applyAdditions`＋`build-team-master.mjs` の再生成まで行う。
`docs/wiki/team-player-identity.md` にも手順が書いてあった。

原因は調査コマンドの誤り。zsh では `grep ... scripts/*.mjs tools/*.mjs` の
`tools/*.mjs` がマッチしないと**glob 展開の失敗でコマンド全体が中止**され、
出力が空になる（「該当なし」と見分けが付かない）。**空の出力を「存在しない」と読んだのが誤り。**
同種のミスをこのセッション中に3回起こしている（`$s` / `$c` の未分割も同根）。

→ 新規サーバは作らず、**既存の `team-review-server.mjs` を拡張**する形に直した
（作りかけた `scripts/serve-team-review.mjs` は削除）。

### 実装したもの

- **`scripts/lib/review-ledger.mjs`**（新規）
  - `clusterKey(members)` = メンバーの team id 昇順連結。**並び順に依存しない安定キー**。
  - `readLedger` / `writeLedger`（キー順ソートで書くので差分が読める）/ `recordDecision`
    （`firstDecidedAt` と `revisions` を保つ）/ `summarize`。
- **`data/teams/review-decisions.json`**（新規・判断台帳）— 1件あたり
  `verdict`（merge / **separate**）・`proposedAutoOK`（機械の提案）・`decidedBy`（human / auto）・
  `groups` / `canon` / `merges` / `decidedAt` / `firstDecidedAt` / `revisions`。
- **`scripts/build-team-review-html.mjs`** — state を安定キー化（localStorage は作業用キャッシュに降格、
  KEY は v10）。台帳に判断があればそれを初期値にするので、**候補をいつ再生成してもよくなった**。
  `buildOutput()` は `{additions, decisions}` を返し、**否定の判断も1件として送る**。
  人が触ったかを `touched` で持ち `decidedBy` を決める。
- **`scripts/team-review-server.mjs`** — `POST /apply` が `{decisions, additions}` を受けて
  台帳保存→alias反映→マスタ再生成。台帳だけ更新する `POST /decisions` を追加。
  旧形式（additions の配列そのもの）も受けるので後方互換あり。
- **`npm run team:review`** — HTML生成＋サーバ起動。

### 検証

- 安定キー: メンバーの並びを逆にしても同じキー（`2-2355-2734`）。
- 往復: `POST /decisions` → 台帳へ2件（統合1・別チーム1）→ HTML再生成で
  `LEDGER` として埋め込まれ初期値に復元されることを確認。
- `summarize` が **`autoOKOverturned: 1`**（機械が自動OKと言い、人が別チームと判断した件数）を返す
  ことを確認。**これが抜き取り監査の誤り率の素**になる。
- 旧形式（配列）POST でも壊れないことを確認。
- eslint 警告0、CIゲート7本すべて PASS。
- **検証に使った架空の判断は台帳から削除し、空（0件）でコミットしている**
  （架空の「人の判断」を混ぜると台帳の意味が無くなるため）。

### 残り

- `permissions: contents: write`（Actions からの書き戻し）は未着手。台帳ができたので次はこれ。
- 抜き取り監査そのもの（無作為 N 件を人に回して誤り率を測る）は未実装。
  台帳と `summarize` が前提を満たしたので、着手できる状態にはなった。
- 同種の台帳が要る他のキュー: `check-identity-health` の要対応、teamId 目視確認252件。

## Compile Log（追記3ぶん）

- **zsh の glob 失敗で grep が中止される件**は wiki に載せない。プロジェクト固有の知見ではなく
  一般的なシェルの挙動で、wiki の読者に必要な情報ではないため。ただし
  **「空の出力を存在しないと読んだ」誤りは同じセッションで3回起きている**ので raw には残す。
- **判断台帳のフィールド定義**は `team-player-identity.md` に要点（verdict / proposedAutoOK /
  decidedBy と、それで誤り率が数えられること）だけ書き、全フィールドの一覧は載せない。
  実体は `scripts/lib/review-ledger.mjs` が正で、二重管理になるため。
- **localStorage の KEY が v9 まで上がっていた件**は wiki には「旧実装は再生成のたびに
  取り違えるか捨てるかだった」とだけ書き、v9 という数字は raw に留めた。

---

## 追記4（2026-09-06・同日）: ループが1周回った

追記1で報告のみに置いた `verify-bracket-layout` の250件は、切り出した調査タスクで
**同日中に解消された**（PR #86「bracket 復元の不一致250件を解消し、本戦前の『予選』形式に対応する」）。
原因はデータ側の誤りではなく**検証ロジックが本戦前の「予選」形式を想定していなかったこと**で、
`lib/bracketLayout.ts` の対応と回帰テストの追加が入っている。
あわせて `checks.yml` で **報告 → ゲートへ昇格**され、TODO コメントも削除された。

つまりこの1日で次が1周した:

```
検出器が休眠 → CIで走らせる → 落ちる（250件）→ 赤にせず報告に置く
  → 課題として切り出す → 直る → ゲートへ昇格（＝床が1段上がって固定される）
```

**「締める方向の変更は自由」という非対称ルールの初適用**であり、設計どおりに動いた。
以後この不一致が再発すればCIが赤で止まる。人の記憶に依存する箇所が1つ減った。

注目すべきは、**発見から解消までが同日**だったこと。休眠していた2日間との差は知能ではなく、
「走らせたかどうか」だけだった。
## 追記5（2026-09-06・同日）: Python の回帰テストを CI に載せた

追記1で「依存定義が無いため見送り」としていた分。PDF取り込み系を自律化の出発点にすると
決めた以上、そこが自動実行されていないのは穴なので塞いだ。

### 依存は2つしか無かった

`scripts/pdf-to-players/` と `scripts/venue-agent/` の import を全数調べたところ、
3rdパーティは **`pdfplumber` と `requests` だけ**（残りは標準ライブラリとローカルモジュール）。
`scripts/requirements-test.txt` に固定した。

**pytest は要らない。** 一度 pytest で回して `test_namesplit_fallbacks` が
「fixture `tmp_data` が無い」で ERROR になったが、これは**こちらの実行方法の誤り**で、
RUNBOOK.md / README.md はどちらも `python3 test_regression.py` と書いている
（テスト関数は `__main__` から引数付きで呼ばれる）。

### カバレッジの制限（重要・未解決）

fixtures の PDF は `.gitignore` の `*.pdf` で追跡していないので、CI では
PDF に依る項目が自動 SKIP される。`git archive HEAD` で CI と同じ木を作って実測した:

| 環境 | 結果 |
|---|---|
| CI と同条件（fixtures 無し） | **75/75 passed** |
| 手元（fixtures 2本あり） | **144/144 passed** |

→ **CI がカバーするのは 144 項目中 75（52%）**。しかも**手元でも 4本の fixture が欠けている**
（`intercollegiate-2026-men-doubles` ほか）ので、手元の 144/144 も全項目ではない。

wiki に「回帰103/103」と書かれていたのは 2026-08-14 時点の数字で、
その後テストが増えて 144 になっている（記述は古いが、当時の記録としては正しい）。

fixtures を追跡するかは**判断が要るので触っていない**。大会のドロー表PDFを public リポジトリに
置くことになるため。

### 検証

- Python 3.13.4（リポジトリの `.venv` と同じ）＋固定版で **75/75・5/5**。
- Python 3.14.6 でも同じ結果。3.13 を CI に採用（作者の環境に合わせる）。
- `git archive HEAD` を展開したクリーンな木＋新規 venv で実行しており、
  手元の環境依存が混じっていないことを確認済み。

### この時点の CI

ゲート12・報告2。Node 側8本＋Python 側2本。

---

## 追記6（2026-09-06・同日）: Actions からの書き戻し（`contents: write`）

追記2で残っていた最後の前提。ユーザーの選択は A（台帳の集計スナップショットを時系列で残す）。

### 自分の線引きに照らした訂正

追記2で「再生成できない記録なら書き戻してよい」と決めたが、**`summarize()` の出力は
台帳からいつでも再計算できる**ので、厳密にはこの条件を満たさない。着手前に気付いたので記録する。

実際に復元できないのは次の2つで、スナップショットにはこれを含めた:

- **`merge-candidates` の件数**（データ取り込みのたびに変わる。過去の値はどこにも残らない）
- **上書きされた判断の、上書き前の内容**（台帳は `revisions` の回数しか持たない）

判断済み件数や誤り率そのものは `decidedAt` から概ね再構成できるが、
再構成のコストと比べて記録のコストが桁違いに小さいので併せて残している。

### 設計上の判断2つ

**1. `checks.yml` とワークフローを分けた。**
`checks.yml` は `permissions: contents: read` のまま据え置き、書き戻す仕事だけを
`review-snapshot.yml`（`contents: write`）に分離した。ゲートを回すだけのワークフローに
push 権限を持たせる理由が無いため。

**2. 数字が動いたときだけコミットする。**
`record-review-snapshot.mjs` が前回のスナップショットと数値を比較し、同じなら追記しない。
動いていない日を書き足しても情報は増えず、コミットと本番ビルドだけが増える。
sitemap.yml の先例（毎日 push する設計）が2日で止められたのは、
おそらくこの手のノイズも一因だったと思われる（Assumption・commit message からは読めない）。

コミットメッセージに `[skip ci]` を入れて Cloudflare Pages のビルドを飛ばす想定だが、
**実際に飛んでいるかは初回の実運用で確認する必要がある**（未検証）。

### 記録する内容

`data/teams/review-history.json`（配列・追記のみ）。1点あたり:

```json
{ "date": "2026-09-06", "candidates": 562, "signalPlayers": 104,
  "decided": 0, "byHuman": 0, "byAuto": 0, "merge": 0, "separate": 0,
  "autoOKReviewedByHuman": 0, "autoOKOverturned": 0 }
```

`autoOKOverturned / autoOKReviewedByHuman` が**自動OK判定の誤り率**。
現在は分母0なので **未測定**と表示する（0/0 を 0% と出さない）。

### 検証

- 初回実行でベースラインを記録（候補562・うち選手共有104・判断済0）。
- 2回目は「変化なし」で追記しないことを確認。
- 台帳に判断を1件足すと追記され、`git diff` が変化を検出することを確認
  （＝ワークフローのコミット判定が働く）。誤り率 1.000（1件中1件を覆した）も正しく出た。
- **検証に使った架空の判断とスナップショットは削除済み**。コミットしているのは
  ベースライン1点（すべて0）だけ。
- eslint 警告0。

### 現在地: 抜き取り監査の前提はすべて揃った

```
判断の置き場（台帳）          … 追記3で完了
書き戻せる実行環境            … 本追記で完了
誤り率を測る式と時系列        … 本追記で完了
――――――――――――――――――――――
残るのは「母数」だけ = 人が実際にレビューを何件か通すこと
```

**ここから先は実装ではなく、人がレビュー画面で判断を入れる作業**になる。
562クラスタ（うち選手共有104は自動OKにならないので必ず人手）が待っている。
何件か入れば誤り率が初めて数字になり、抜き取り監査（無作為N件を人に回す）の
標本設計に進める。

### 残っている未解決

- `[skip ci]` が Cloudflare Pages に効くかが未確認。
- 抜き取り監査の標本設計（何件で誤り率をどの精度で測れるか）。
- 同種の台帳が要る他のキュー: `check-identity-health` の要対応、teamId 目視確認252件。
- CI のカバレッジ 144中75（fixtures 未追跡・追記5）。
- 「充実」を1つの数字に束ねる作業（未着手）。
