# アイデア: AIが自律的にサービスを改善する仕組み（検知・検証の全体委譲と自己修復）

## 状況

Idea Backlog。発散フェーズだが**方向は決着した**（2026-09-06）。
出発点となるドメイン（PDF取り込み系）と、成立条件（抜き取り監査＋非対称な境界移動）まで合意。
実装は未着手。

## 目的

サービス運用のうち**検知・検証の全体**をAIに委譲し、人間は方向の判断だけを持つ状態にしたい。
背景にあるのは「AIソロプレナー」的な関心と、「自律して目標を立てて内外のデータを取得して
タスクを完了するAIエージェントを駆使できるようになりたい」という関心。

既存の姉妹スレッド2本との関係:

- [AIとの共同探索と探索プロセスの属人性](./2026-07-11-idea-ai-co-exploration-context.md) —
  **人間が主でAIが従**の前提で探索の質を上げる話。本アイデアはその上位（人間が主導権を持たない層）。
- [skillのローカルLLM代替](./2026-08-14-idea-local-llm-skill-replacement.md) —
  **人間が与えたタスクをAIが遂行する**話。本アイデアはその上位（タスクを誰が決めるか）。

本アイデアは「AI自身が何をやるか決める」層で、バックログでは初。

## ユーザーが興味を持った点

原文（時系列）:

- 「AIエージェントなどを使って、AIが自律的にサービスを改善する仕組みを検討したい。いわゆる
  **AIソロプレナー**というものかもしれない。（中略）**自律して目標を立てて内外のデータを取得して
  タスクを完了するAIエージェント**が駆使できるようになることも興味がある」
- (a)サービスの自走 と (b)エージェントを駆使できること の関係について:
  「**(b)をすることで(a)になるのではないか。もしくは、(a)をするために(b)をするのではないか**」
- 任せたいことの正体:
  「**任せたいことはAIの自律性を向上さて "床" を高めて方向を人間が判断できるための土台づくり**」
  「**任せたいことは課題発見かもしれない**」
- 委譲の範囲:
  「**検知や検証の全体を任せていくというのはできないのかどうか。もちろん、最終判断は人間にしたい**」
- 「（検出器の穴を測る案は）**かなり狭い範囲だと感じ、それをすることで仕組み作りにつながる
  イメージがつかない**」（→ Claude の提案の位置づけ誤りを正した指摘。下記「経緯」参照）
- 境界（自動でよい範囲）の決め方について:
  「**自動化したい。ただ、そこにも自己修復できるようにして、間違いがないか、改善する余地は
  ないかを判定して、時には人に判断を任せつつ、自己修復されるようにしておきたい**」

## 想定運用 / なぜこのスコープか

### 「充実」を目的関数に置く（PV・収益は置かない）

ユーザーの見立て:「トップページの『ソフトテニスを「記録」としてたどれる場』が充実していくことで、
収益・PV・掲載データ量・手作業時間の削減というのは説明変数になるのか」。

方向は妥当だが、挙がった4つは**役割が3種類に分かれる**（Claude の整理）:

| 変数 | 役割 | 目的関数に置けるか |
|---|---|---|
| 手作業時間の削減 | 真の説明変数（上流の原因。容量が増える） | ○ |
| 掲載データ量・接続性・正確性 | 「充実」の構成要素そのもの（説明変数ではない） | ○（定義が要る） |
| PV・収益 | 下流の結果（遅行指標） | **×** |

PV・収益を自律エージェントの目的関数に置けない理由は、**本リポジトリに4件の実証がある**。
どれもPVが上がる選択肢を、ミッションに反するとして人間が退けたもの:

1. AdSense全面展開時、本文78文字の空ページにも広告が出る穴を見つけ、
   **本文400文字未満は出さないゲートを足して417枚を自ら除外**した
2. **優勝予想はやらない**（2026-07-31決定。2026-08-31に「事前Elo上位4組＝準決勝4組」と
   実測で当たることを確認してもなお見送り）
3. [ADR-010](../adr/ADR-010-retire-result-articles-consolidate-to-hub.md) で結果記事を廃止しハブへ統合（ページを減らす判断）
4. 選手一覧の姓頭文字分割ページを不採用（薄いページの量産になるため）

**PVを目的関数に持ったエージェントなら4件とも逆の判断をする**。しかも短期的にはどれもPVが
上がるので、指標の上では「改善」に見える（Goodhart）。

一方「記録としてたどれる」は機械で測れる概念で、**断片は既に測られている**が一度も束ねられていない:
網羅性（掲載大会数・年）/ 接続性（内部リンク本数 選手一覧97→1,917本、学校ページ間0本、
`check-orphan-entries` の孤立エントリー）/ 正確性（照合カバレッジ26.4%）/
同一人物として辿れるか（識別ヘルスの要対応シグナル72→42、別名残存4→0）。

### 委譲の単位は「機能の一部」ではなく「1ドメイン丸ごと」

理由は下記「わかっていること」の583件（部分適用が実際に止まった実証）。

## わかっていること

### 1. 完成例が既に1つある — チーム名寄せ

「検知・検証の全体委譲」は、本リポジトリで**既に1ドメインだけ完成している**。

```
① 候補を機械が出す         scripts/build-team-merge-candidates.mjs（コア一致／選手共有の2シグナル）
② 自動でよい分を機械が決める  scripts/apply-auto-merges.mjs
③ 残りを人が判断する道具を機械が作る  scripts/build-team-review-html.mjs
④ 判断を機械が反映する      scripts/apply-team-aliases.mjs
⑤ 起動                     ← ここだけ人
```

③は「チーム名だけでは判断できない」ことを前提に**選手名・年代・主な大会の文脈を添え、
1クリックで判断でき、確認済みはリストから消える**設計。人間は判断だけをする。

**②に「最終判断は人間」が思想でなくコードとして書かれている**のが要:

> 自動OK: 同一グループ内で「同一大会(大会id+年)」に表記が同居しなければ自動OK。
> **ただし signal:"players"（選手共有シグナル）のクラスタは常に自動OKにしない（人手レビュー専用）。**

→ **人間が全部を見なくても「最終判断は人間」は保てる。機械が判断してよい範囲を一度定義すれば、
その外側だけが人間に来る。**

### 2. 部分適用は実際に止まった — 外部データ取得クローラーの583件

```
data/local-sources/detected-documents.json
  総数 583件 / status: 全件 "new"
  ignored 0件 / accepted 0件 / appliedAt を持つもの 0件
  検出日 2026-05-24 〜 2026-06-12
```

`crawl:local-tournaments`（県連サイトを巡回してPDFを検出）と
`apply-accepted-qualifiers.mjs`（accepted を反映）は両方実装済みなのに、
**間の仕分けが人手だったため583件が3ヶ月放置され、accepted が1件も無いので反映側は
一度も仕事をしていない。クローラー自体も2026-06-12以降走っていない。**

→ **①だけ自動化すると人の可処分時間が律速になり、仕組みごと止まる。**
同じ兆候が teamId 目視確認252件にもある。

### 3. 「提案はLLM・採否は機械」の型が3回、独立に発明されている

- `scripts/pdf-to-players/tuning.py` の `repair()` — LLMが4段階の温度で列判定を提案 → 機械が採点 →
  外れた内訳をフィードバックして最大3回直させる。docstring に
  **「採否を決めるのは常に機械（`score_entries`）で、LLMの自己申告は使わない」**
- [ADR-012](../adr/ADR-012-llm-authored-insights-with-machine-verification.md) — 記事は機械照合を通ったものだけ公開（prebuild が強制）
- `npm run ranking:backtest` — 過去データで再生して採点

**自己修復ループの型は既にある。** 今回の要求は「この型を②（境界の決定）自身に適用する」こと。

### 4. ②は既に1箇所で自動化されていて、一度壊れている

`scripts/pdf-to-players/tuning.py`:

```python
GOOD_ENOUGH = 0.75   # これ未満ならLLMに聞く＝機械が自分で「自信がない」と判定する境界
if a.score >= 0.95:  # 早期打ち切り
score *= 0.6         # 退化解釈へのペナルティ
```

2026-08-14のインカレ対応時、**「存在しない項目を減点する設計だったため点数の上限が0.74になり、
目安0.75に到達不能」**という形でこの境界が壊れていた。
→ **境界が誤っていても症状は「なんとなく上手くいかない」としてしか出ない。
②を自動化するなら、境界が正しいかを測る仕組みが同時に要る。**

### 5. 学習材料の実測 — 人の判断は6件しかない

| 材料 | 件数 |
|---|---|
| `data/teams/team-alias-additions-auto-reviewed.json`（人が確定した判断） | **6件** |
| `data/teams/team-name-mappings.json` | 67件 |
| `scripts/pdf-to-players/fixtures/` | **PDF 2本**（うち回帰103アサーション） |

レビューHTMLは判断を `localStorage` に持つため、**押した判断の大半がブラウザ内で消えている**。

一方、**二経路照合による正解データは潤沢**:
PDF抽出 vs 人が作った `initialPlayer.js` = **49件全項目一致** /
インカレ姓名分割 vs 既存選手データ = **796名中793名一致(99.6%)、不一致3件は既存データ側の誤り** /
`bracketLayout` vs 全データ = **209大会 20,571試合一致・不一致0**。

→ **「人の判断を学ぶ」経路は今は成立しない（6件）。「独立した第二経路と突き合わせて
自分を採点する」経路なら今日から成立する。** 後者のほうが設計として優れてもいる——
前者は機械が人間の偏りに収束する（[2026-07-11 の論点1「均質化」](./2026-07-11-idea-ai-co-exploration-context.md)そのもの）。

### 6. 検出器11本はすべて事後に生まれている

`check-*` / `verify-*` の追加日を git で全数確認（2026-09-06時点）:

```
2026-05-19 check-growth-analysis      2026-08-02 check-orphan-entries
2026-06-26 check-identity-health      2026-08-04 check-highschool-pipeline-freshness
2026-07-19 check-tournament-entries   2026-08-26 check-duplicate-placements
2026-07-31 verify-bracket-layout      2026-08-26 check-upcoming-tournaments
2026-08-01 check-tournament-insights  2026-08-29 check-name-splits
2026-08-01 verify-story-text
```

**11本すべてが事後**。ソース冒頭のコメントが例外なく「何が壊れてから作ったか」で始まる。
8月だけで5本＝増加ペースが上がっている（rawノートも 6月10 → 7月35 → 8月65本）。
**人間の発見負荷が増え続けている。**

これは2026-07-11の論点3（**「探索の品質」は既知の失敗モードの不在として負に定義する**）を、
理論として書いた翌月に、11本の検出器として無自覚に実践していたことを意味する。

### 7. 本リポジトリの主要な失敗モードは「静かに間違う」

`check-tournament-entries.mjs` 冒頭:
**「統計エンジンはこれらを『相方不明』として黙って除外するため、表示だけ見ても気付けない」**。
`check-orphan-entries` の70試合消失、`check-duplicate-placements` の二重計上も同型。

### 8. 走る場所が無い

`.github/workflows` が存在せず CI ゼロ。全部ユーザーの Mac で手動 `npm run`。
**仕組みが人の起動を要求する限り、それは仕組みではない。**

## 設計の骨子（合意済み）

### 人間の判断を「キューの処理」ではなく「機械の抜き取り検査」に使う

機械が全件を処理し、**人は無作為に少数（例:20件）だけ見る**。得られるのは処理結果ではなく
**機械の誤り率**で、これが (a) 境界が正しいかの判定材料になり、(b) 不足していた人の判断ラベルにもなる。

**人の時間が件数に比例しなくなる（線形→定数）。** これが「全体を任せる」と
「最終判断は人間」が両立する形。

自己修復はここから自動的に出る:
**誤り率が許容を超えたら境界を自動で締める、下回り続けたら少しずつ緩める。**

### 境界の移動は非対称にする（Claude の提案・ユーザー未確認）

- **締める方向**（人に回る量が増える）→ 安全。自動でよい
- **緩める方向**（機械が決める量が増える）→ **独立した二経路照合で裏が取れたときだけ**

理由は「わかっていること 7」。緩めれば人の負荷は下がり、指標上は改善に見え、誤りは黙って通る。

### 課題発見の3階層（Claude の整理・ユーザー未確認）

ユーザーの「任せたいのは課題発見」は "床" と "方向" の境界上にあるため分解した:

- **(i) 規約違反の発見** — 決めたルールが守られていない箇所を探す。純粋に床。全部任せてよい
- **(ii) 検出器の穴の発見** — 「守っているつもりのルールが機械で守られていない」を探す。
  メタだが保証範囲の内側なので任せられる
- **(iii) 課題設定** — 何を問題とみなすか。任せると均質化が最も強く出る。**人間に残す**

(ii) には本リポジトリに1例だけ実績がある: 2026-08-14の**逸脱注入テスト**（インサイトに8種の逸脱を
注入し、照合器が落とせたのは2種だけ）。ここから「実効カバレッジ26.4%」「記事の主語が0/24で未照合」
という、人間が気付いていなかった課題が出た。

## 課題・未解決

- **境界の自動調整と「静かな劣化」の切り分け**。緩める方向の非対称ルールは提案止まりで未検証。
- **抜き取り監査の標本設計**（何件で誤り率をどの精度で測れるか、層別が要るか）。
- **人の判断が repo に残らない問題**（`localStorage`）。抜き取り監査を回すなら先に直す必要がある。
- **走る場所（CI/スケジューラ）の選定**。Cloudflare Pages 前提のリポジトリで、
  GitHub Actions を入れるのか別手段かは未検討。
- **「充実」を1つの数字に束ねる作業**は未着手（材料は揃っているが定義が要る）。
  出発点をPDF取り込みにしたため後回しになっている。
- **(iii) の線引きを誰がいつ見直すか**。人間に残すと決めたが、見直しの契機が未定義。
- クローラーの583件は**捨てる**判断（2026-09-06）。ただし
  「残すならクローラーの設計自体を見直したい」とのことで、外部データ取得の再設計は未着手のまま残る。

## 目指したい方向性

**出発点は PDF 取り込み系**。理由は条件が最も揃っているため:

- 境界（`GOOD_ENOUGH = 0.75`）が既にある
- 自己修復ループ（`tuning.repair`）が既にある
- 二経路照合の正解データが潤沢（49件 / 796名 / 20,571試合）
- **境界が一度壊れた実例がある**（上限0.74問題）
- ユーザーが「全部任せたい」と挙げた「PDF取り込みからの試合結果反映」と同じレーン

欠けているのは**抜き取り監査**と**境界を動かす非対称ルール**の2つだけ。

なお **PDF→エントリーは自動化済み**（`scripts/pdf-to-players/`、回帰103 pass）だが、
**PDF→試合結果はskill（`tournament-pdf-to-results`）だけでスクリプトが無い**。
手作業が残っているのは後半。

## 関連

- [2026-07-11 AIとの共同探索と探索プロセスの属人性](./2026-07-11-idea-ai-co-exploration-context.md) —
  姉妹（ミクロ層／人間主導）。論点1（品質の分散は減らし方向の分散は残す）と
  論点3（品質を失敗モードの不在として負に定義）が本アイデアの設計原則として直接効いている。
- [2026-08-14 skillのローカルLLM代替](./2026-08-14-idea-local-llm-skill-replacement.md) —
  姉妹（タスク遂行層）。`tuning.repair` / 逸脱注入テストの出所。
- [ADR-012](../adr/ADR-012-llm-authored-insights-with-machine-verification.md) — 「提案はLLM・採否は機械」の型の明文化。
- [docs/wiki/open-questions.md](../wiki/open-questions.md) — 本アイデアの掲載先（プロジェクト運用/メタ）。
- コード: `scripts/build-team-merge-candidates.mjs` / `scripts/apply-auto-merges.mjs` /
  `scripts/build-team-review-html.mjs`（完成例）、`scripts/pdf-to-players/tuning.py`（②と自己修復）、
  `scripts/crawl-local-tournaments.mjs` / `scripts/apply-accepted-qualifiers.mjs`（止まった部分適用）。

## 参考文献

- Goodhart's law（指標を目標にすると指標として機能しなくなる）— PV・収益を目的関数に置かない根拠。
- 受入抜取検査（acceptance sampling）— 抜き取り監査の元になる考え方。
- selective prediction / learning to defer — 「機械が自信のないものだけ人に回す」境界の定式化。
  （注: いずれも Claude の知識からの参照で、本スレッドでは一次情報を未確認。実装前に確認する。）

## Compile Log

wiki（`open-questions.md` の Idea Backlog 表）へは1行要約のみ記載し、下記は**意図的に raw だけに留めた**:

- **「充実」を構成する4指標の断片リスト**（網羅性・接続性・正確性・同一人物性）— まだ定義でなく
  材料の列挙にすぎず、束ね方が決まっていないため wiki に載せると確定事項に見える。
- **課題発見の3階層 (i)(ii)(iii)** — Claude の整理でユーザーの確認を取っていないため。
- **境界移動の非対称ルール** — 同上（提案止まり・未検証）。
- **検出器11本の追加日一覧** — 「11本すべて事後」という結論だけを wiki に載せ、明細は raw に残す。
  明細は再現可能（git log）なので wiki で持つ価値が薄い。
- **PVを目的関数に置けない根拠の4事例** — 各事例は既に `monetization.md` / `sns-story-platform.md` /
  ADR-010 に記載済みで、ここでの新規性は「4件を1つの論拠として束ねたこと」だけ。重複を避けた。

---

## 追記（2026-09-06・同日）: (1)「走る場所」に着手 — 前提の誤りの訂正と実装

ユーザーの「いったん(1)を進めてどうなるか見てみたい」を受けて着手。**着手直後に本文の前提が
1つ誤っていたことが判明した**ので、まずそれを記録する。

### 訂正: 「走る場所が無い（CIゼロ）」は誤り

`out/` は `.gitignore` 対象でコミットされておらず、`wrangler.toml` の
`pages_build_output_dir = "out"` と [deployment.md](../wiki/deployment.md) のビルド時間内訳
（clone + `npm ci` 24秒 → prebuild 2分6秒 → … → upload + deploy 40秒、計約9分）から、
**Cloudflare Pages が push ごとにビルドしており `prebuild` はそこで自動実行されている**ことが確定した。
人の起動なしに動く場所は既にあった。

### 訂正後の、より正確な問題: prebuild には「ゲートしか置けない」

検出器の実行状況を全数確認した結果:

```
prebuild に入っている（push で自動実行）: 4本
  check-tournament-entries / check-tournament-insights
  check-highschool-pipeline-freshness / check-name-splits --strict

入っていない（人が思い出したときだけ）: 7本
  check-orphan-entries / check-duplicate-placements / check-upcoming-tournaments
  check-identity-health / check-growth-analysis / verify-bracket-layout / verify-story-text
＋ テスト・検証系10本（bracket:test / analytics:test / upcoming:test / club:verify /
  playerstats:verify / playerstats:test / ranking:backtest / bracket:verify:tree / story:verify ほか）
```

**11本中7本が休眠していた。** 原因は忘れっぽさではなく構造で、休眠中の検出器には**2種類ある**:

- **ゲート型**（合否が意味を持つ）: orphan-entries / duplicate-placements / growth-analysis /
  verify-bracket-layout
- **キュー型**（人の判断待ちの一覧。設計上いつも未処理を返す）: identity-health（exit 1 が既定）/
  upcoming-tournaments（「終了コードは常に 0。運用の残タスク一覧であり、ビルドを止めるエラーではない」）

**prebuild はビルドを止める場所なのでキュー型を置けない。置き場が無かったから休眠した。**
これは本文の「①だけ自動化すると詰まる」と同型の現象が、実行環境の側でも起きていたということ。

なお `verify-story-text` は引数必須の**記事単位のツール**（`-t` / `-c` が要る）で、
リポジトリ全体を見る検査ではないため CI 対象外。全体側の強制は prebuild の
`check-tournament-insights` が担っている（ADR-012）。

### 休眠の代償が実際に出ていた（本アイデアの実証）

休眠中の7本をその場で全部走らせたところ、**`verify-bracket-layout` が落ちた**:

```
復元適用: 443 大会 / 復元不可(no-seed-info): 9 大会
試合の一致: 38481 件 / 不一致: 250 件
  highschool-championship/2013/doubles-none-boys.json  (2 件)
  zennihon-singles/2017/singles-none-boys.json       (248 件)
```

wiki の記録は「209大会・20,571試合が一致／**不一致0件**」（2026-08時点）。git で追うと:

```
zennihon-singles/2017/singles-none-boys.json       2026-09-04 追加「全日本シングルス 2017」
highschool-championship/2013/doubles-none-boys.json 2026-09-04 追加「インハイ2013」
verify-bracket-layout.mjs                          最終更新 2026-08-22（ラウンドロビン対応）
```

**2日前の取り込みが検証を通っておらず、誰も気付いていなかった。**
検出器は存在し、正しく動き、正しく落ちるのに、走らせる契機だけが無かった。
「検出器を増やす」ではなく「走らせる」が効く局面がある、という直接の実例。

（データ側の誤りか検証ロジック側の未対応かは**未判定**。roundrobin 起因の偽陽性の前例が
[2026-08-22 のノート](./2026-08-22-bracket-slot-parity-roundrobin-false-positive.md) にあるため、
断定せず「要確認」として別途扱う。）

### 実装したもの: `.github/workflows/checks.yml`

リポジトリは PUBLIC なので **GitHub Actions は無料・無制限**（コスト制約なし）。
Cloudflare Pages と役割を分け、**CF に置けないもの**だけを引き受ける設計にした:

| CF Pages（既存） | GitHub Actions（今回） |
|---|---|
| push 契機のみ | push + **cron（毎週月 09:00 JST）** + 手動実行 |
| ゲートのみ（落ちるとデプロイが止まる） | **ゲートと報告を分離**。報告はデプロイに影響しない |
| 結果はビルドログ | ジョブサマリに一覧が残る |

- **ゲート（落ちたら赤）**: check-orphan-entries / check-duplicate-placements /
  check-growth-analysis / bracket:test / analytics:test / upcoming:test / club:verify
- **報告のみ（赤にしない）**: verify-bracket-layout / check-identity-health /
  check-upcoming-tournaments → `$GITHUB_STEP_SUMMARY` に出力

`verify-bracket-layout` は現在250件で赤なので、**あえてゲートに入れず報告側に置いた**
（赤い CI を常態化させないため）。250件が解消したらゲートへ昇格させる。
これは本文の非対称ルール「**締める方向の変更は自由**」の最初の適用にあたる。

ローカルで CI と同じ手順を通しで実行した結果: **ゲート7本すべて PASS・合計3秒**
（CI では checkout と `npm ci` を足して1〜2分の見込み）。

### 今回やらなかったこと

- **Python の回帰テスト**（`scripts/pdf-to-players/test_regression.py` 103件、
  `scripts/venue-agent/test_regression.py` 5件）は **requirements.txt 等の依存定義が
  リポジトリに無い**ため見送り。CI に載せるには依存の固定が先。
- **重い検証**（`playerstats:verify` / `playerstats:test` / `ranking:backtest`）は未計測のため見送り。
- **書き戻し**（Actions からリポジトリへコミットする）は未着手。抜き取り監査を回すには要る。
  `permissions: contents: read` のままにしてある。

### この時点での状態

- **cron の実行環境ができた** = 本アイデアの前提のうち「定期実行」は解けた。
- 残るのは「**リポジトリに書き戻せる実行環境**」と「レビュー判断が localStorage から出ないこと」。
  この2つは抜き取り監査の直接の前提なので、次に効くのはここ。

---

## 追記2（2026-09-06・同日）: PR #85 マージ後の実測と、書き戻しの先例

### 実測

- PR #85 マージ済み。**PR上の初回実行 39秒 / main での実行 41秒、いずれも13ステップ全て success**。
  https://github.com/kojimamasahiro/softeni/actions/runs/34003422641
- 報告のみの3本も `$GITHUB_STEP_SUMMARY` に想定どおり出力された（ユーザーが画面で確認）。
- **基準値**（次回以降の差分の起点）: ブラケット復元 = 復元適用443大会 / 復元不可(no-seed-info)9大会 /
  一致38,481件 / **不一致250件**。

意味があるのは、この250件が「**赤くならずに、しかし見える**」状態になったこと。
それまでは「prebuild に入れる＝250件が消えるまでデプロイ不能」か
「入れない＝誰も気付かない（実際に2日間そうだった）」の二択しか無かった。

### 発見: 書き戻しエージェントは一度作られ、2日で止められていた

`.github/workflows/sitemap.yml` の履歴（削除済み・git履歴から復元）:

```
2025-05-02 52f83071  追加。on: schedule cron '0 3 * * *'（毎日3時JST）+ workflow_dispatch
                     node scripts/sitemap-generator.js で生成し、
                     github-actions[bot] が git commit && git push する
2025-05-04 4a061288  トリガーを削除 → on: []（追加の2日後）
2025-10-24 66d1d95c  ファイルごと削除（同日 conclusion=failure のランが1件、実行時間0秒）
```

**自動コミットは1件も残っていない**（`--grep="[auto]"`・`--author=github-actions` とも0件）。
動く前に止められている。

**なぜ廃れたかは実装から読める**: `scripts/sitemap-generator.js` は現存せず、
sitemap は `postbuild` の `next-sitemap` がビルド時に生成する形に置き換わった
（[deployment.md](../wiki/deployment.md)）。つまり
「**生成物をリポジトリにコミットして戻す**」→「**ビルド時に作る**」への移行であり、これは正しい判断。

### この先例の読み方（次のステップへの含意）

書き戻しには2種類あり、先例が否定したのは片方だけ:

| | 例 | リポジトリに書き戻すべきか |
|---|---|---|
| **再生成できるもの** | sitemap / players-lite / facts | **不要**。ビルド時に作ればいい（先例の教訓） |
| **再生成できないもの** | 人の判断、抜き取り監査の結果、誤り率の履歴 | **必要**。他に情報源が無い |

抜き取り監査が要求するのは後者。「機械がこう判断し、人がこう覆した」は計算で復元できない。
**本文で指摘した「人の判断が6件しか残っていない」は、これを残す場所が無いことの帰結**。

したがって先例は「書き戻しをするな」ではなく「**生成物を書き戻すな**」と読むのが正確。
次に `permissions: contents: write` を検討する際は、対象を
「再生成できない記録」に限る、という線引きを設計に入れること。

### 副次的な観察（未対応）

`.gitignore` は `/public/sitemap.xml` `/public/sitemap-*.xml` `/public/robots.txt` を挙げているが、
**3ファイルとも git に追跡されたまま**（gitignore は追跡済みファイルを外さない）。
ただし 2026-05-30 `9eff918c` に「Remove sitemap.xml ...」を **Revert したコミットがある**ため、
外そうとして戻した経緯がある。現時点で実害は無い（postbuild が `out/` 側を上書きするため）ので未対応。
触るなら Revert の理由を先に確認すること。

## Compile Log（追記2ぶん）

- **sitemap.yml の先例**は wiki（deployment.md）へは載せず raw に留めた。既に削除済みの
  ワークフローであり、現行のデプロイ手順の説明にはならないため。ただし
  「生成物は書き戻さない／再生成できない記録は書き戻す」という**線引きは次の実装時に効く**ので、
  open-questions.md の該当行に一言だけ反映する。
- **`.gitignore` と追跡状態のずれ**は wiki に載せない。Revert の理由が未確認で、
  結論が出ていないため（記録のみ）。

---

## 追記3（2026-09-06・同日）: (2) レビュー判断をリポジトリに残す

追記2で「次に効くのは書き戻し」と書いたうちの、**判断の置き場**のほうに着手した。
先例（sitemap.yml）の教訓「生成物は書き戻すな／再生成できない記録は書き戻す」の後者にあたる。

### わかったこと1: 判断は配列インデックスで持たれていた

`build-team-review-html.mjs` の state は `state[i]`（`merge-candidates.json` の配列インデックス）で
保持され、localStorage のキーは `team-merge-review-v9`。**候補は再生成のたびに並びが変わる**ので、
過去の判断は「取り違える」か「KEY のバージョンを上げて捨てる」かの二択だった。
v9 まで上がっていること自体が、9回捨てた記録と読める。

しかもこの問題は**既に認識され、回避運用でしのがれていた**。`team-review-server.mjs` の冒頭:

> 注: 反映は確認済クラスタのみ。候補一覧(merge-candidates)は会話中は作り直さない
> **（番号がずれるため）**。セッション終了後に再生成してください。

「人が気をつける」で回していた、という点で本アイデアの主題そのもの。

### わかったこと2: 否定の判断は一切残っていなかった

`buildOutput()` は `aliasEntries()`（＝統合するグループ）だけを書き出す。
**「これは別チームだ」と判断しても出力は空になり、どこにも残らない。**
機械の自動OK判定が正しかったかを測るには、むしろ否定のほうが要る。

また自動OKクラスタは既定で `reviewed:true` になるため、出力では
**機械の判断と人の判断が混ざり、区別が付かない**。
`team-alias-additions-auto-reviewed.json` が6件しかないのはこの構造の帰結。

### 訂正: 「`/apply` を受けるサーバは存在しない」は誤り

会話中にそう断定したが誤り。`scripts/team-review-server.mjs`（2026-06-26 追加・55行）が実在し、
`http://localhost:5173` で配信＋`applyAdditions`＋`build-team-master.mjs` の再生成まで行う。
`docs/wiki/team-player-identity.md` にも手順が書いてあった。

原因は調査コマンドの誤り。zsh では `grep ... scripts/*.mjs tools/*.mjs` の
`tools/*.mjs` がマッチしないと**glob 展開の失敗でコマンド全体が中止**され、
出力が空になる（「該当なし」と見分けが付かない）。**空の出力を「存在しない」と読んだのが誤り。**
同種のミスをこのセッション中に3回起こしている（`$s` / `$c` の未分割も同根）。

→ 新規サーバは作らず、**既存の `team-review-server.mjs` を拡張**する形に直した
（作りかけた `scripts/serve-team-review.mjs` は削除）。

### 実装したもの

- **`scripts/lib/review-ledger.mjs`**（新規）
  - `clusterKey(members)` = メンバーの team id 昇順連結。**並び順に依存しない安定キー**。
  - `readLedger` / `writeLedger`（キー順ソートで書くので差分が読める）/ `recordDecision`
    （`firstDecidedAt` と `revisions` を保つ）/ `summarize`。
- **`data/teams/review-decisions.json`**（新規・判断台帳）— 1件あたり
  `verdict`（merge / **separate**）・`proposedAutoOK`（機械の提案）・`decidedBy`（human / auto）・
  `groups` / `canon` / `merges` / `decidedAt` / `firstDecidedAt` / `revisions`。
- **`scripts/build-team-review-html.mjs`** — state を安定キー化（localStorage は作業用キャッシュに降格、
  KEY は v10）。台帳に判断があればそれを初期値にするので、**候補をいつ再生成してもよくなった**。
  `buildOutput()` は `{additions, decisions}` を返し、**否定の判断も1件として送る**。
  人が触ったかを `touched` で持ち `decidedBy` を決める。
- **`scripts/team-review-server.mjs`** — `POST /apply` が `{decisions, additions}` を受けて
  台帳保存→alias反映→マスタ再生成。台帳だけ更新する `POST /decisions` を追加。
  旧形式（additions の配列そのもの）も受けるので後方互換あり。
- **`npm run team:review`** — HTML生成＋サーバ起動。

### 検証

- 安定キー: メンバーの並びを逆にしても同じキー（`2-2355-2734`）。
- 往復: `POST /decisions` → 台帳へ2件（統合1・別チーム1）→ HTML再生成で
  `LEDGER` として埋め込まれ初期値に復元されることを確認。
- `summarize` が **`autoOKOverturned: 1`**（機械が自動OKと言い、人が別チームと判断した件数）を返す
  ことを確認。**これが抜き取り監査の誤り率の素**になる。
- 旧形式（配列）POST でも壊れないことを確認。
- eslint 警告0、CIゲート7本すべて PASS。
- **検証に使った架空の判断は台帳から削除し、空（0件）でコミットしている**
  （架空の「人の判断」を混ぜると台帳の意味が無くなるため）。

### 残り

- `permissions: contents: write`（Actions からの書き戻し）は未着手。台帳ができたので次はこれ。
- 抜き取り監査そのもの（無作為 N 件を人に回して誤り率を測る）は未実装。
  台帳と `summarize` が前提を満たしたので、着手できる状態にはなった。
- 同種の台帳が要る他のキュー: `check-identity-health` の要対応、teamId 目視確認252件。

## Compile Log（追記3ぶん）

- **zsh の glob 失敗で grep が中止される件**は wiki に載せない。プロジェクト固有の知見ではなく
  一般的なシェルの挙動で、wiki の読者に必要な情報ではないため。ただし
  **「空の出力を存在しないと読んだ」誤りは同じセッションで3回起きている**ので raw には残す。
- **判断台帳のフィールド定義**は `team-player-identity.md` に要点（verdict / proposedAutoOK /
  decidedBy と、それで誤り率が数えられること）だけ書き、全フィールドの一覧は載せない。
  実体は `scripts/lib/review-ledger.mjs` が正で、二重管理になるため。
- **localStorage の KEY が v9 まで上がっていた件**は wiki には「旧実装は再生成のたびに
  取り違えるか捨てるかだった」とだけ書き、v9 という数字は raw に留めた。

---

## 追記4（2026-09-06・同日）: ループが1周回った

追記1で報告のみに置いた `verify-bracket-layout` の250件は、切り出した調査タスクで
**同日中に解消された**（PR #86「bracket 復元の不一致250件を解消し、本戦前の『予選』形式に対応する」）。
原因はデータ側の誤りではなく**検証ロジックが本戦前の「予選」形式を想定していなかったこと**で、
`lib/bracketLayout.ts` の対応と回帰テストの追加が入っている。
あわせて `checks.yml` で **報告 → ゲートへ昇格**され、TODO コメントも削除された。

つまりこの1日で次が1周した:

```
検出器が休眠 → CIで走らせる → 落ちる（250件）→ 赤にせず報告に置く
  → 課題として切り出す → 直る → ゲートへ昇格（＝床が1段上がって固定される）
```

**「締める方向の変更は自由」という非対称ルールの初適用**であり、設計どおりに動いた。
以後この不一致が再発すればCIが赤で止まる。人の記憶に依存する箇所が1つ減った。

注目すべきは、**発見から解消までが同日**だったこと。休眠していた2日間との差は知能ではなく、
「走らせたかどうか」だけだった。
## 追記5（2026-09-06・同日）: Python の回帰テストを CI に載せた

追記1で「依存定義が無いため見送り」としていた分。PDF取り込み系を自律化の出発点にすると
決めた以上、そこが自動実行されていないのは穴なので塞いだ。

### 依存は2つしか無かった

`scripts/pdf-to-players/` と `scripts/venue-agent/` の import を全数調べたところ、
3rdパーティは **`pdfplumber` と `requests` だけ**（残りは標準ライブラリとローカルモジュール）。
`scripts/requirements-test.txt` に固定した。

**pytest は要らない。** 一度 pytest で回して `test_namesplit_fallbacks` が
「fixture `tmp_data` が無い」で ERROR になったが、これは**こちらの実行方法の誤り**で、
RUNBOOK.md / README.md はどちらも `python3 test_regression.py` と書いている
（テスト関数は `__main__` から引数付きで呼ばれる）。

### カバレッジの制限（重要・未解決）

fixtures の PDF は `.gitignore` の `*.pdf` で追跡していないので、CI では
PDF に依る項目が自動 SKIP される。`git archive HEAD` で CI と同じ木を作って実測した:

| 環境 | 結果 |
|---|---|
| CI と同条件（fixtures 無し） | **75/75 passed** |
| 手元（fixtures 2本あり） | **144/144 passed** |

→ **CI がカバーするのは 144 項目中 75（52%）**。しかも**手元でも 4本の fixture が欠けている**
（`intercollegiate-2026-men-doubles` ほか）ので、手元の 144/144 も全項目ではない。

wiki に「回帰103/103」と書かれていたのは 2026-08-14 時点の数字で、
その後テストが増えて 144 になっている（記述は古いが、当時の記録としては正しい）。

fixtures を追跡するかは**判断が要るので触っていない**。大会のドロー表PDFを public リポジトリに
置くことになるため。

### 検証

- Python 3.13.4（リポジトリの `.venv` と同じ）＋固定版で **75/75・5/5**。
- Python 3.14.6 でも同じ結果。3.13 を CI に採用（作者の環境に合わせる）。
- `git archive HEAD` を展開したクリーンな木＋新規 venv で実行しており、
  手元の環境依存が混じっていないことを確認済み。

### この時点の CI

ゲート12・報告2。Node 側8本＋Python 側2本。

---

## 追記6（2026-09-06・同日）: Actions からの書き戻し（`contents: write`）

追記2で残っていた最後の前提。ユーザーの選択は A（台帳の集計スナップショットを時系列で残す）。

### 自分の線引きに照らした訂正

追記2で「再生成できない記録なら書き戻してよい」と決めたが、**`summarize()` の出力は
台帳からいつでも再計算できる**ので、厳密にはこの条件を満たさない。着手前に気付いたので記録する。

実際に復元できないのは次の2つで、スナップショットにはこれを含めた:

- **`merge-candidates` の件数**（データ取り込みのたびに変わる。過去の値はどこにも残らない）
- **上書きされた判断の、上書き前の内容**（台帳は `revisions` の回数しか持たない）

判断済み件数や誤り率そのものは `decidedAt` から概ね再構成できるが、
再構成のコストと比べて記録のコストが桁違いに小さいので併せて残している。

### 設計上の判断2つ

**1. `checks.yml` とワークフローを分けた。**
`checks.yml` は `permissions: contents: read` のまま据え置き、書き戻す仕事だけを
`review-snapshot.yml`（`contents: write`）に分離した。ゲートを回すだけのワークフローに
push 権限を持たせる理由が無いため。

**2. 数字が動いたときだけコミットする。**
`record-review-snapshot.mjs` が前回のスナップショットと数値を比較し、同じなら追記しない。
動いていない日を書き足しても情報は増えず、コミットと本番ビルドだけが増える。
sitemap.yml の先例（毎日 push する設計）が2日で止められたのは、
おそらくこの手のノイズも一因だったと思われる（Assumption・commit message からは読めない）。

コミットメッセージに `[skip ci]` を入れて Cloudflare Pages のビルドを飛ばす想定だが、
**実際に飛んでいるかは初回の実運用で確認する必要がある**（未検証）。

### 記録する内容

`data/teams/review-history.json`（配列・追記のみ）。1点あたり:

```json
{ "date": "2026-09-06", "candidates": 562, "signalPlayers": 104,
  "decided": 0, "byHuman": 0, "byAuto": 0, "merge": 0, "separate": 0,
  "autoOKReviewedByHuman": 0, "autoOKOverturned": 0 }
```

`autoOKOverturned / autoOKReviewedByHuman` が**自動OK判定の誤り率**。
現在は分母0なので **未測定**と表示する（0/0 を 0% と出さない）。

### 検証

- 初回実行でベースラインを記録（候補562・うち選手共有104・判断済0）。
- 2回目は「変化なし」で追記しないことを確認。
- 台帳に判断を1件足すと追記され、`git diff` が変化を検出することを確認
  （＝ワークフローのコミット判定が働く）。誤り率 1.000（1件中1件を覆した）も正しく出た。
- **検証に使った架空の判断とスナップショットは削除済み**。コミットしているのは
  ベースライン1点（すべて0）だけ。
- eslint 警告0。

### 現在地: 抜き取り監査の前提はすべて揃った

```
判断の置き場（台帳）          … 追記3で完了
書き戻せる実行環境            … 本追記で完了
誤り率を測る式と時系列        … 本追記で完了
――――――――――――――――――――――
残るのは「母数」だけ = 人が実際にレビューを何件か通すこと
```

**ここから先は実装ではなく、人がレビュー画面で判断を入れる作業**になる。
562クラスタ（うち選手共有104は自動OKにならないので必ず人手）が待っている。
何件か入れば誤り率が初めて数字になり、抜き取り監査（無作為N件を人に回す）の
標本設計に進める。

### 残っている未解決

- `[skip ci]` が Cloudflare Pages に効くかが未確認。
- 抜き取り監査の標本設計（何件で誤り率をどの精度で測れるか）。
- 同種の台帳が要る他のキュー: `check-identity-health` の要対応、teamId 目視確認252件。
- CI のカバレッジ 144中75（fixtures 未追跡・追記5）。
- 「充実」を1つの数字に束ねる作業（未着手）。

---

## 追記7（2026-09-06・同日）: 台帳のキー設計が誤っていた（自己修正）

ユーザーが「zennihon-workers に中学校が入っているのはおかしい」と指摘し、その確認で
チームIDを追う過程で**追記3で作った台帳のキー設計が壊れている**ことが判明した。

### 何が誤りだったか

`clusterKey` を **メンバーの team id の昇順連結**にしていた。しかし
`scripts/build-team-master.mjs` は

```js
const id = teams.length + 1;
```

と**位置でIDを振る**。alias を反映して `build-team-master.mjs` が走るたび
（＝レビュー画面で「確認済を反映」を押すたび）にIDが総入れ替わりになる。

実測（ユーザーが実際にレビューを進めた後の状態で計測）:

| 計測 | 値 |
|---|---|
| 再生成前後で **IDが移動したチーム** | **5,520件中 5,220件**（同じIDのままは259件） |
| 候補を作り直した後、台帳426件のうち **idキーで引き当たる数** | **0件（0.0%）** |
| 同じく **名前キーで引き当たる数** | **305件**（候補383件中 79.6%。残りは統合されて候補から消えた分） |

つまり **「再生成のたびに判断を取り違えるか捨てるか」という、この台帳が直そうとした問題そのものを、
別の形で作り直していた**。追記3で「旧実装は配列インデックス保持だったので v9 まで上がった」と
書きながら、位置依存のIDに置き換えただけだった。

### なぜ見落としたか

追記3で「並び順を逆にしても同じキーになる」ことは検証したが、**IDそのものが振り直される可能性を
検証していない**。テストしたのは「同じ入力に対する安定性」で、「再生成をまたいだ安定性」ではなかった。
`build-team-master.mjs` の採番方法は読めば1行で分かる場所にあり、確認していれば防げた。

発見が偶然（ユーザーの別件の指摘）だった点も含めて、本アイデアの主題どおりの失敗。
**この種の「再生成をまたいだ同一性」は機械で検査できる**（候補を再生成してキーの一致率を測る）ので、
検出器にする候補になる。

### 修正

- `clusterKey` を**チーム名ベース**に変更（NFKC正準化＋空白除去して昇順連結）。
  名前は判断の対象そのものなので、これ以上に安定な識別子が無い。
  正準化を挟むことで `normalize-team-spacing` 等の揺れ吸収でキーが変わらない。
- `LEDGER_VERSION` を 2 に上げ、`migrateV1()` を追加。各判断は `members` にチーム名を
  持っているので、**読み込み時に自動で振り直せば1件も失われない**。
- 実データ426件（人18・自動408）を移行し、候補を再生成しても305件が引き継がれることを確認。

### 副次的な効果

wiki にあった「**レビュー中は候補を作り直さない（番号がずれるため）**」という回避運用が、
これで本当に不要になった。実際、いま候補を再生成すると **562件 → 383件**
（ユーザーの統合62件が効く）で、うち305件が判断済みとして畳まれ、**残り78件**になる。

### ついでに確認した本題（zennihon-workers の中学校）

**データは正しかった。** 該当は1件のみで `zennihon-workers/2022/doubles-over35-boys.json`
entryNo 98、種目は **over35（35歳以上）**。同じ大会には `入善高校教員`・`上尾高校OB`・
`尼崎高校OBクラブ` が並んでおり、**勤務先やOBクラブ名義での出場が普通にある**大会。
中学生は35歳以上になれない。

システム側も正しく分離している——`いわき市立泉中学校` はジャンル「成」（zennihon-workers のみ）、
`泉中学校` はジャンル「中」（全中・県対抗・全日本シングルス）で**別チーム扱い、マージ候補にも
上がっていない**。統合してはいけない。

## Compile Log（追記7ぶん）

- **キー設計の誤りと実測値**は wiki（team-player-identity.md）に反映した。
  「team id は使えない」は将来また同じ間違いをしうるので、理由と実測値ごと残す必要がある。
- **「なぜ見落としたか」の分析**は raw のみ。wiki は仕様を書く場所で、反省は載せない。
- **「再生成をまたいだ同一性の検査を検出器にできる」**という着想は、まだ実装も設計もしていないので
  wiki には載せない。検出器を作る段になったら書く。
- **zennihon-workers の中学校の件**は wiki に載せない。データが正しく、システムも正しく
  分離できており、変更すべきものが何も無いため（レビュー時の判断材料としては raw に残す）。

---

## 追記8（2026-09-06・同日）: ID のずれを検出器にした ＋ 誤り率が初めて出た

### きっかけ

ユーザーの「**白鴎大足利高校が zennihon-primaryschool で表示されてそう**」という違和感から。
調べると**生データに小学生大会の白鴎/白鷗は1件も無い**。壊れていたのは表示だった。

```
merge-candidates.json 「白鴎大足利高校」= id 825
そのときの teams.json  id 825 =「いすみジュニア」（小学生クラブ）
→ 画面には いすみジュニア の出場大会が「白鴎大足利高校」の名前で出ていた
```

追記7のIDバグの**表示面での現れ**。`build-team-master.mjs` が位置でIDを振るので、
「確認済を反映」のたびに `teams.json` は新IDになるが `merge-candidates.json` は旧IDのまま。
レビュー画面は文脈（選手名・年・出場大会）を**IDで**引くため、別チームの経歴が出る。

実測: **1,223メンバー中1,211がずれ、一致はわずか12**。

皮肉なことに、wiki の「**レビュー中は候補を作り直さない（番号がずれるため）**」という
回避運用が、このずれを固定していた。作り直さないから旧IDが残り続ける。

### なぜ人が見つけにくいか

データは壊れず、**表示だけが静かに嘘になる**。名前は正しいので一見おかしくない。
実際、発見の端緒は「小学生大会に高校が出ているのはおかしい」という**利用者の違和感**だった。
`check-tournament-entries.mjs` 冒頭の「表示だけ見ても気付けない」と同じ型。

機械なら一致率を数えるだけで確実に落ちる。

### 実装

- **`scripts/check-team-id-alignment.mjs`**（新規）— `merge-candidates.json` の各メンバーの
  `id` を `teams.json` で引き、名前が一致するかを見る。ずれがあれば exit 1。
  `team-context.json` が引けないメンバー数も併せて報告する（ずれの副作用）。
  `npm run check:team-id-alignment`。
- **根本原因も塞いだ** — `team-review-server.mjs` の `/apply` が、マスタ再生成だけでなく
  **候補とレビュー画面まで作り直す**ようにした。判断台帳がチーム名キー（追記7）になったので
  候補を作り直しても判断は失われない。応答に `remaining`（候補数・判断済・未判断）を載せ、
  画面に「ページを再読み込みしてください」と出す。
- CI へは**報告のみ**で入れた。**main にコミット済みのデータは1,211件ずれており**
  （レビュー作業の結果が未コミットのため）、いまゲートにすると即赤になる。
  整合した候補がコミットされたらゲートへ昇格させる（`verify-bracket-layout` と同じ運用）。

### 検証

- ずれた状態（807メンバー中707ずれ）で exit 1、候補を作り直すと 807/807 一致で exit 0。
- サーバの `/apply` を空リクエストで叩き、マスタ→候補→画面の再生成と
  `remaining: {clusters:378, done:373, todo:5}` を確認。
- 検証中に**ユーザーのレビューサーバが5173で稼働中**だったため、最初のテストリクエストが
  そちらへ届いた（`additions: []` なので alias は無変更・実害なし）。以後 `PORT=5199` で検証した。
  **稼働中のプロセスを確認せずにサーバを立てたのは手順の誤り。**

### 誤り率が初めて数字になった

ユーザーがレビューを進めた結果（台帳587件・人の判断133件）:

```
自動OKの誤り率: 0.429（人が見た 14 件中 6 件を覆した）
```

**機械が「自動OK」と判断したもののうち、人が実際に見た14件では6件が誤りだった。**
抜き取り監査が測ろうとしていた値がこれ。まだ標本14件なので幅は広いが、
**「自動OKの境界は緩すぎる」方向を強く示唆する**（追記6の設計でいえば「締める」判断）。

注意: この14件は無作為抽出ではなく、ユーザーが目についた順に見たもの。
**難しそうなものから見た可能性があり、真の誤り率より高く出ている恐れがある。**
抜き取り監査を実装するときは、ここを無作為抽出に変えることが要点になる。

### 残り

- 候補378件のうち**未判断は5件**まで減った。
- コミット済みデータの整合（→ ゲート昇格）。
- 抜き取り監査の無作為抽出（上記の偏りを消す）。
- 追記7で挙げた他の検出器候補（小学生大会に中学校/高校名・チーム名＝選手名）は未実装。

## Compile Log（追記8ぶん）

- **検出器の存在と直し方**は wiki（team-player-identity.md）へ反映する。運用手順に関わるため。
- **誤り率 0.429 と標本の偏りの注意**は raw のみ。標本14件で暫定値であり、
  wiki に載せると確定した数字として読まれるため。抜き取り監査を実装して
  無作為抽出になった時点で改めて書く。
- **稼働中サーバへ誤ってリクエストを送った件**は raw のみ（手順の反省で、仕様ではない）。

---

## 追記9（2026-09-06・同日）: 抜き取り監査を実装した

追記6で「前提はすべて揃った、残るのは母数だけ」と書いた、その母数が揃ったので実装した。

### 0.429 は使えない、という前提の確認

追記8で出た「自動OKの誤り率 0.429（14件中6件）」は**推定値として使えない**。
14件はユーザーが目についた順に見たもので、無作為ではない。難しそうなものから見ていれば
当然高く出る。**測るべきは「機械が決めて人が一度も見ていない」454件からの無作為標本**。

| 区分 | 件数 | 監査に使えるか |
|---|---|---|
| 機械が決めて人が見ていない（`decidedBy: auto`） | **454** | **これが母集団** |
| 人が見た（`decidedBy: human`） | 133 | 使えない（選び方が無作為でない） |

### 設計上の要点2つ

**1. 事前登録（引いてから判断する）。** `--draw` で標本を確定してファイルに記録し、
そのあとで人が判断する。順序を守らないと「都合のよい結果だけ採る」ことが構造的に可能になる。

**2. 引いた時点の機械の判定を保存する。** 台帳は人が判断すると上書きされ、
`revisions` の回数しか残らない。**機械が何と言っていたかが消える**ので、
標本ファイル側に `machineVerdict` として控える。これが無いと誤り率が計算できない。

### 実装

- **`scripts/audit-review-sample.mjs`**（新規・`npm run review:audit`）
  - `--draw N [--seed S]` … 母集団から無作為に N 件引き、`data/teams/review-audit.json` へ記録。
    乱数は mulberry32 で **seed を残すので同じ標本を引き直せる**。既に引いた分は除外する。
  - `--report [--threshold T]` … 判断済みの標本について誤り率と**95%信頼区間（Wilson）**を出す。
    標本が小さいときに正規近似より素直なので Wilson を使う。
    **誤り率の下限が閾値を超えたときだけ exit 1**（既定 0.05）。未測定は失敗にしない。
- **レビュー画面に「監査対象」フィルタ**を追加。標本だけを順に判断できる。
  進捗表示にも「監査対象 N件（未判断 M）」を出す。

### 締める／緩めるの非対称性を出力に埋め込んだ

追記6で決めた非対称ルール（締める方向は自由・緩める方向は独立した照合で裏を取る）を、
レポートの文言そのものに入れた:

```
判定: 誤り率の下限 4.7% が閾値 1.0% を超えている。
  → 自動OKの範囲を締めること（緩める方向の変更は独立した照合で裏を取るまで禁止）。
```

### 検証（模擬データ・台帳は退避して復元済み）

454件から20件を `--seed 20260906` で抽出。標本のうち12件を人が判断し2件を覆した状態を作ると:

```
機械の判定を人が覆した: 2 / 12
誤り率: 16.7%（95%信頼区間 4.7% 〜 44.8%）
判定: 誤り率の下限 4.7% は閾値 5.0% 以下。締める必要は無い。
  ただし上限は 44.8% で、標本 12 件では判断がつかない。緩めるなら標本を増やすこと。
```

**「12件では判断がつかない」と自分から言う**のが要点。点推定だけ出すと、
16.7% という数字が確からしく見えてしまう。閾値 0.01 にすると exit 1 で締める判定になることも確認した。

### 現在地

**第1回の標本20件を引いた状態**（`data/teams/review-audit.json`・seed 20260906）。
人がレビュー画面の「監査対象」で20件を判断すれば、**初めて信頼区間つきの誤り率が出る**。

n=20 で覆しが0なら 95%上限は約14%、1件なら約25%。**5%を切る主張をしたければ n≈60 は要る。**
まずは20件で桁を掴み、必要なら `--draw` を追記して足す設計にしてある。

### 残り

- 標本20件の判断（人の作業）。
- CI へ載せるかは保留。`--report` は「未判断なら 0」で通るので置いても無害だが、
  誤り率は人の作業が進んだときだけ動くので、週次の report-only が妥当と思われる（未実装）。
- 追記7で挙げた検出器2つ（小学生大会に中学校/高校名・チーム名＝選手名）は未実装のまま。

## Compile Log（追記9ぶん）

- **監査の手順（draw → 判断 → report）と2つの設計要点**は wiki（team-player-identity.md）へ反映する。
  運用手順であり、順序を間違えると測定が無意味になるため。
- **Wilson を選んだ理由・n と信頼区間の関係（n≈60）**は raw のみ。実装の根拠であって
  運用手順ではなく、wiki に置くと読み飛ばされる。
- **模擬データでの検証値（16.7% など）**は raw のみ。実測ではないので wiki に載せると誤解を招く。

---

## 追記10（2026-09-06・同日）: 監査が実データで結果を出し、機械の欠陥を1つ見つけた

追記9で引いた標本20件のうち14件をユーザーが判断した。**監査が初めて実データで動いた。**

```
機械の判定を人が覆した: 7 / 14
全体の誤り率: 50.0%（95%信頼区間 26.8% 〜 73.2%）
```

### レポートの判定が誤っていた（自己修正）

追記9で実装した `--report` は「誤り率の下限が閾値を超えたら**締めろ**」と出す設計だった。
しかし**覆された7件は全て `separate → merge`（見逃し）で、`merge → separate`（誤統合）は0件**。

| 向き | 件数 | 意味 |
|---|---|---|
| 誤統合 merge → separate | **0** | 別チームを1つにした。**データが壊れる方向** |
| 見逃し separate → merge | **7** | 統合すべきものを残した。安全側 |

締めても見逃しは減らない。むしろ増える。**判定を誤統合の率だけで行うように直した**。
見逃しは別枠で報告し、対処として「既定グループ分けの見直し」を指し、
**緩める方向の安全弁は据え置く**よう明記した。

追記9の実装は「誤りは1種類」という暗黙の前提を置いていた。実データが来るまで気付かなかった。

### 見逃しの原因: `defaultGroups` の接頭辞

覆された7件を調べると、機械的な原因が1つに収束した。

```
「大田原女子」      ジャンル[高]  → キー 'G:高'
「大田原女子高校」   ジャンル[]    → キー 'N:高'
```

**出場大会由来は `G:`、名前由来は `N:` と接頭辞が違う。** どちらも「高校」を意味するのに
文字列として別物なので、**片方だけジャンルを持つクラスタは必ず分割される**。
比布/比布中学校、川之石/川之石高校 も同型。

（`大宮中`・`安部` は `level()` が略称を拾えず `bare+i`＝必ず単独になる別の型。今回は未対応。）

### 影響の実測（修正前に測った）

| | 自動OK | 人手レビュー | **自動OKのうち実際に統合が起きるもの** |
|---|---|---|---|
| 現状 | 350 | 22 | **1** |
| 接頭辞を統一 | 350 | 22 | **60** |

**自動OK 350件のうち、実際に統合が起きていたのは1件だけだった。** 自動判定は99.7%空回りしていた。
「安全だから自動でよい」と判定した上で、グループ分けが全員を分けるので何も起きない、という状態。

そして**安全弁（同一大会での同居チェック）は一切動かない**——自動OK件数も人手レビュー件数も
変わらない。これは「緩める」変更ではなく、**自動OKの判定とグループ分けが矛盾していたバグの修正**。

### 修正

`defaultGroups` のキーを `memGenre(m) ?? level(m.name)` に統一し、段階が同じなら由来を問わず
同じグループにした。`autoOK` の同居チェックには触れていない。

### 現在地と注意

修正後、**次に「確認済を反映」を押すと60件が自動で統合される**。安全弁は同じだが、
実際に起きる統合の数は増えるので、**押す前に「自動OK」フィルタで何件か目視することを勧める**。
第2回の監査標本を引いて確かめてもよい。

### 残り

- 標本の残り6件（20件中14件が判断済み）。
- `level()` が略称を拾えない型（`大宮中`・`安部`）は未対応。
- 追記7の検出器2つ（小学生大会に中学校/高校名・チーム名＝選手名）は未実装のまま。

## Compile Log（追記10ぶん）

- **誤りの向きを分けて見る**という判定基準は wiki（team-player-identity.md）へ反映する。
  監査の読み方そのもので、間違えると逆の対処をするため。
- **`G:`/`N:` 接頭辞バグの詳細**は raw のみ。修正済みの実装内部の話で、
  wiki には「同じ段階なら同じグループになる」という現在の仕様だけあればよい。
- **「自動OKの99.7%が空回りしていた」という数字**は raw のみ。修正前の状態の記録であり、
  現在の仕様説明ではない。

---

## 追記11（2026-09-06・同日）: 「誤統合0件」は測っていなかった（層別抽出）

ユーザーが「**誤統合が0**」に着目したので、その0の中身を確かめた。**測れていなかった。**

```
抽出した80件の内訳（機械の判定別）
  機械が「統合」と判定 : 判断済  0 / 未判断 10  ← 誤統合を検出できるのはここだけ
  機械が「別チーム」    : 判断済 16 / 未判断 54  ← 見逃ししか検出できない
```

**誤統合は「機械が統合すると判定したもの」でしか起こり得ない。** 判断済み16件は全て
「別チーム」判定で、誤統合が**構造的に起こり得ない標本**だった。つまり 0/0。

にもかかわらず追記10のレポートは「誤統合は 0/16 件で、率の上限は 19.4%」と表示していた。
**検証していない安全性を、検証したかのように見せていた。** 追記10で分子を直したが、
分母が誤ったままだった。

### なぜこうなったか

母集団が偏っている。実測で **統合52 / 別チーム391**（約1:7.5）。
全体から無作為に20件引くと、期待される「統合」判定は2〜3件しかない。
4回引いて80件でも、実際に引けた「統合」判定は10件で、そのどれも判断されていなかった。

**無作為抽出は「母集団全体の誤り率」には正しいが、「稀だが危険な層の誤り率」には向かない。**
危険な層を測りたければ、その層から明示的に引く（層別抽出）必要がある。

### 修正

- `--draw N --stratum merge|separate` を追加。層を指定して引ける。
  引いた回には `stratum` を記録し、レポートの回ごとの内訳にも出す。
- **分母を層ごとに分けた**。誤統合は「統合」判定の標本数を分母に、
  見逃しは「別チーム」判定の標本数を分母にする。
- **分母0のときは率を出さず「未測定」と言う**。

```
判定: **誤統合は未測定**。
  機械が「統合」と判定した標本を人が一度も判断していないので、分母が0。
  「誤統合0件」は「起きていない」ではなく「**測っていない**」という意味。
  データを壊す方向を測るには、その層から明示的に引くこと:
    node scripts/audit-review-sample.mjs --draw 20 --stratum merge
```

### 層ごとの残量（実測）

| 層 | 母集団 | まだ引いていない |
|---|---|---|
| `merge`（機械が統合と判定） | 52 | 42 |
| `separate`（機械が別チームと判定） | 391 | 337 |

### この回で3回目の「実データが来るまで気付かなかった」

1. 追記10: 誤りの向きを区別していなかった（誤統合と見逃しを同じ「誤り」として数えた）
2. 追記11: 向きは分けたが**分母**を分けていなかった（起こり得ない標本を分母に入れた）
3. どちらも**ユーザーの指摘が端緒**（「50%は高すぎないか」「誤統合が0」）

設計は3回とも「もっともらしく動いて、間違った数字を自信をもって出す」形で壊れていた。
本アイデアの主題（静かに間違う）が、監査ツール自身に3回起きたことになる。

**教訓**: 率を出す実装は、分子だけでなく**分母が意味を持つか**を必ず検査する。
「0件」を報告するときは「0/0 ではないか」を確かめる。

### 残り

- `--stratum merge` で20件引き、判断して初めて誤統合が測れる（母集団52・未抽出42）。
  n=20 で0件なら95%上限は約14%、n=42 全部で0件でも約7%。**52件しか無いので、
  全数見ても「5%未満」とは言い切れない**。ここは標本設計の限界として記録しておく。
- 追記7の検出器2つは未実装のまま。

## Compile Log（追記11ぶん）

- **層別抽出の必要性と、分母0のときは未測定と言うこと**は wiki（team-player-identity.md）へ反映する。
  監査の読み方の根幹で、間違えると「安全だと確認した」と誤解するため。
- **母集団が52/391と偏っている実測値**は raw のみ。データが増えれば変わる数字で、
  wiki に書くと古くなる。
- **「3回とも実データが来るまで気付かなかった」という反省**は raw のみ。仕様ではない。

---

## 追記12（2026-09-06・同日）: 「誤統合100%」も測定の誤りだった（機械の判定が古い）

`--stratum merge` で標本が増え、**誤統合 3/3 = 100%（下限43.8%）**が出た。
データが壊れる方向なので重大——と思ったが、**また測定の誤りだった。**

### 覆された3件

```
水戸クラブ / 水戸スポーツ少年団      （茨城県）
韮山 / 韮山ジュニア                （静岡県）
大田原クラブ / 大田原 / 大田原ジュニア （栃木県）
```

**いまのロジックで計算し直すと3件とも `separate`** ＝ 人の判断と一致で、誤りは0件。
追記10の `defaultGroups` 修正の前後どちらでも `separate` になる。**機械は間違えていない。**

### 原因: `machineVerdict` を判断台帳から読んでいた

`--draw` は機械の判定を**台帳の `verdict` から**取っていた。ところが台帳の値は:

- **反映のたびに上書きされる**（この3件は `revisions` が 14〜15回）
- **IDがずれていた時期**（`memGenre` が別チームの経歴を引いていた）に書かれたものが混ざる

該当エントリの `firstDecidedAt` は 04:09 UTC で、ID整合を直す前。
つまり **既に直っているバグを、現在の誤り率として報告していた。**

### 修正

- グループ分けと自動OK判定を **`scripts/lib/team-grouping.mjs`** に切り出し、
  レビュー画面生成と監査の両方から使うようにした（実装が2箇所にあると必ずずれる）。
- `--draw` は機械の判定を**その場で計算する**。台帳は見ない。
  回に `verdictSource: 'computed'` を記録する。
- `--report` は `verdictSource` が `computed` でない回を**集計から除外**する。
  除外の基準は結果ではなく**取得方法**なので、都合のよい回を選ぶことにはならない。

### 除外した64件を捨てないための参考値と、その限界

人の判断自体は有効（人の答えは機械の値に依存しない）。そこで現在のロジックで
機械側を計算し直した参考値を出したところ **見逃し 1/45 = 2.2%** と激減した。
修正が効いた証拠に見えたが、**これも歪んでいた。**

```
突き合わせできた: 47 件（候補から消えていて照合不能: 17 件）
  照合不能17件のうち16件は人が「統合」と判断したもの
  → 統合されて候補から消えたので、**見逃しだった側だけが抜けている**

見逃し  1/45 = 2.2%   … 最良（消えた分を無視）
       17/61 = 27.9%  … 最悪（消えた分を全部見逃しに数える）
```

**真の値は 2.2%〜27.9% の間で、幅が広すぎて使えない。** 出力にこの範囲と理由を明記した。
修正の効果を知るには**修正後に新しく引き直すしかない**。

### 監査ツールの誤りはこれで4回目

1. 追記10: 誤りの**向き**を区別していなかった
2. 追記11: 向きは分けたが**分母**を分けていなかった（0/0 を「0件」と報告）
3. 追記12: 機械の判定が**古い値**だった（直ったバグを現在の誤りとして報告）
4. 追記12: 救済のための参考値に**生存者バイアス**（統合された側だけ消える）

いずれも「もっともらしく動いて、確信をもって間違った数字を出す」形。
**そして4回とも、気付いたのはユーザーが数字に違和感を持ったから**
（「50%は高すぎないか」「誤統合が0」「誤統合100%」）。

**測定器を作ると、測定器自身の誤りは測れない。** これは本アイデアの主題そのもので、
「機械が全件を処理し人は標本だけ見る」の設計は、
**人が数字を疑える形で出す**ことに依存している、という実地の結論になった。
点推定だけを出していたら4回とも通っていた。

### 残り

- **修正後の測り直し**（`--draw 20` と `--draw 20 --stratum merge`）。これが本番。
- 追記7の検出器2つは未実装のまま。

## Compile Log（追記12ぶん）

- **`machineVerdict` は台帳でなくその場で計算する**は wiki（team-player-identity.md）へ反映。
  監査の正しさの前提条件で、戻すと同じ誤りが再発するため。
- **4回の誤りの一覧と「測定器自身の誤りは測れない」という結論**は raw のみ。
  仕様ではなく、この探索から得た知見。ただし本アイデアの中心的な学びなので
  raw の目立つ位置に残す。
- **参考値の 2.2%〜27.9% という具体値**は raw のみ。標本が入れ替われば変わる数字。

---

## 追記13（2026-09-06・同日）: はじめて信頼できる測定が出て、私の修正が悪化させていたと分かった

追記12の修正後、`--draw` で引き直した第7回（機械の判定を抽出時に計算・事前登録）を
ユーザーが19件判断した。**今日はじめての、信頼できる測定**:

```
見逃し separate → merge :  0 / 11 =  0%   ← defaultGroups 修正が効いた
誤統合 merge → separate :  4 /  8 = 50%（下限21.5%）← **危険な方向**
```

### 覆された4件は、すべて私の修正が作った

```
常磐大 / 常磐大学高校              修正前 G:高 , N:高 → 分割   修正後 S:高 , S:高 → 統合
国府台 / 国府台高校                同上
岡山南 / 岡山南高校 / 岡山南高クラブ   G:高,N:高,G:成 → 分割   S:高,S:高,S:成 → 統合
北上ジュニア / 北上 / 北上中学校      G:小,G:中,N:中 → 分割   S:小,S:中,S:中 → 統合
```

追記10で「見逃し27.9%を0%にした」と報告した修正が、**同じ操作で誤統合0%→50%を作っていた**。
見逃しはデータを壊さないが誤統合は壊す。**悪い取引だった。**

### なぜ機械には決められないか

4件とも**片方が `genres` 空**（出場大会の裏付けが無い）で、名前の段階だけで同じグループにしていた。
ところが**正しく統合できた例（大田原女子 / 大田原女子高校、比布 / 比布中学校）も構造は完全に同一**。
手元の情報では区別できない。

**つまりこの型は「もっと賢く判定する」のではなく「機械が決めてはいけない」が正解。**
私が追記10で「バグを直した」と思ったものは、**安全な失敗（見逃し）を危険な失敗（誤統合）に
変換していただけ**で、能力は増えていなかった。

### 対応（案D）

`isAutoOK` に「**裏付けの無いメンバーが他のメンバーと同じグループに入るなら自動OKにしない**」を
追加し、人手レビューへ回す。

| | 自動OK | 人手レビュー | 自動OKで統合が起きる |
|---|---|---|---|
| 追記10の修正後 | 338 | 22 | 49 |
| **案D** | **292** | **68** | **3** |

誤統合4件はすべて人手へ回る。自動統合が49→3に戻るのは、
**49件のうち46件が「決めてはいけない型」だった**ということ。

より広い案C（`genres` 空のメンバーがいるクラスタは全部人手）も測ったが、
人手が126件に膨れる割に安全性は変わらないので採らなかった。

### あわせて重複実装を解消

`build-team-review-html.mjs` が持っていた `level` / `memGenre` / `defaultGroups` / `autoOK` を
`scripts/lib/team-grouping.mjs` に一本化した。監査が同じ判定を使う必要があり、
実装が2箇所にあると必ずずれる（実際、追記12で監査だけ古い判定を見ていた）。

### この日の総括

**機械側で見つかって直した欠陥: 3件**
（`defaultGroups` の接頭辞 → ただし後述のとおり誤り、IDのずれ、裏付けの無い自動統合）

**監査ツール側で見つかって直した欠陥: 5件**
（誤りの向き・分母・古い判定・生存者バイアス・引き直しの阻害）

**そして最大の発見は、追記10で「直した」と報告したものが実は悪化だったこと。**
それが分かったのは、監査を4回直して**ようやく信頼できる測定が1回できた**からだった。

測定器が壊れている間、私は自信をもって「99.7%空回りしていたのを直した」と報告していた。
実際には安全な失敗を危険な失敗に変えていた。**測定できるようになるまでは、
改善したかどうかは分からない**——という当たり前が、この日いちばん高くついた学びになった。

### 残り

- 案Dの適用後に**もう一度引き直して測る**（第8〜10回のmerge層41件が未判断のまま残っている）。
  案Dが効いたなら誤統合は下がるはず。
- 追記7の検出器2つは未実装のまま。

## Compile Log（追記13ぶん）

- **「裏付けの無い統合は機械が決めない」という方針**は wiki（team-player-identity.md）へ反映。
  現在の仕様であり、戻すと誤統合が再発するため。
- **案C/案Dの比較数値**は raw のみ。選定の根拠であって仕様ではない。
- **「追記10の修正は悪化だった」という経緯**は raw のみ。wiki は現在の仕様を書く場所。
  ただし本アイデアにとって中心的な学びなので raw には詳しく残す。

---

## 追記14（2026-09-06・同日）: 判断が反転し、#108 を戻した ／ 監査の前提の誤り

追記13で「#96 が誤統合を作った」と診断し、裏付けの無い統合を人手へ回す実装（#108）を入れた。
**その直後、ユーザーが同じ4件の判断を `separate` から `merge` へ改めた。**

```
北上ジュニア / 北上 / 北上中学校      separate → merge
岡山南 / 岡山南高校 / 岡山南高クラブ   separate → merge
常磐大 / 常磐大学高校               separate → merge
国府台 / 国府台高校                 separate → merge
```

理由は「最初の判断が誤りだった」（常磐大＝常磐大学高校、国府台＝国府台高校 なので統合が正しい）。
これで **誤統合 4/8 = 50% → 0/8 = 0%** になり、#108 の根拠が消えた。
安全弁だけを戻した（重複実装の一本化は有用なので残した）。自動OK 292→337・人手 68→22。

**同じ箇所で判断が2回反転している**（#96 で緩め → #108 で締め → 本追記で戻す）。
いずれも n が小さい段階で機械側を動かした結果で、**急ぎすぎだった。**

### より重い問題: 監査の前提が成り立っていなかった

抜き取り監査は「**人の判断が機械の判定と独立している**」ことを前提に誤り率を測る。
しかし **レビュー画面は機械の判定を初期状態として表示している**。人は機械の答えを見てから判断する。

さらに今回は、**監査が「誤統合50%」と報告した後に、その4件が再判断されて反転した**。
測定結果が判断に影響し、判断が測定結果を変える、という循環が実際に起きた。

監査ツールの誤りを5回直したが、**この前提の誤りには最後まで気付いていなかった**。
分子・分母・判定の鮮度・偏り・引き直しはすべて「計算の正しさ」の話で、
**測定の設計そのもの（独立性）は一度も点検していなかった。**

対処の候補（未実装・要検討）:

- レビュー画面で機械の初期グループ分けを見せずに判断してもらう「盲検モード」を監査対象だけに適用する。
- 判断を上書き可能なままにせず、監査標本については**最初の判断を確定として記録**する
  （いまは `revisions` が17回まで伸びており、いつの判断を測っているのかが曖昧）。
- 監査結果を人に見せる前に、その回の判断を締め切る。

### 現在地と、当面の方針

**機械側はしばらく触らない。** 根拠は merge層8件・上限32.4% でしかなく、
残り48件（第8〜11回）を判断すれば母集団49件のほぼ全数になり、推定でなく確定値になる。
それを見てから #96/#108 のどちらが正しいかを決める。

**この日、機械側を n<10 の数字で2回動かして2回とも戻した。** 標本が足りないうちは動かさない、
という運用規律のほうが、判定ロジックの改良より先に要る。

## Compile Log（追記14ぶん）

- **「監査は人の判断が独立であることを前提にしており、画面が機械の答えを見せている以上
  成り立たない」**は wiki（team-player-identity.md）へ反映する。監査の読み方の前提条件であり、
  知らずに数字だけ見ると誤る。
- **判断が2回反転した経緯と「n<10 で動かさない」規律**は raw のみ。仕様ではなく運用の教訓。
- **盲検モード等の対処案**は未実装・未検討なので wiki には載せない。

---

## 追記15（2026-09-06・同日）: 監査は構造的に成立していなかった（測定の設計そのものの欠陥）

追記14の後、ユーザーの「グループが変わらない場合も確認済みにする必要があるか」という
問いから、**監査が2つの理由で機能していない**ことが分かった。計算の誤り（5回直した）ではなく、
**測定の設計そのもの**の欠陥。

### 欠陥1: merge層は原理的に測れない

```
第7回  all     標本20 | 判断済19 | 画面に出る 0 | 候補から消えた  1
第8回  merge   標本 1 | 判断済 0 | 画面に出る 0 | 候補から消えた  1
第9回  merge   標本20 | 判断済 0 | 画面に出る 0 | 候補から消えた 20
第10回 merge   標本20 | 判断済 0 | 画面に出る 0 | 候補から消えた 20
第11回 merge   標本 7 | 判断済 0 | 画面に出る 0 | 候補から消えた  7
```

**merge層48件は1件も画面に出ない。** 理由:

```
機械が「統合する」と判定
  → 「確認済を反映」で alias が適用される
  → 表記が1つに統合される
  → merge-candidates から消える
  → レビュー画面に出ない ＝ 人が確認できない
```

**機械の統合判断は、人が見る前に適用されて消える。**
つまり**データが壊れる方向（誤統合）は、この設計では原理的に監査できない。**

追記11で「誤統合を測るには層を指定して引け」と直し、追記12で「機械の判定はその場で計算しろ」と
直したが、**引いた標本がそもそも人の目に届かない**という一段下の問題は見えていなかった。
「誤統合 0/8」の8件は、たまたま統合適用の前に引けた分でしかない。

### 欠陥2: 「機械に同意した」を記録できない

```js
decidedBy: s.touched ? 'human' : (AUTO[i] === true ? 'auto' : 'human')
```

`touched`（人が操作した）でなければ `auto` として記録される。
**「見て、機械の判定に同意した」は立派な人の判断**だが、いまの実装は
それと「誰も見ていない」を区別できない。グループを変える必要が無いとき、
人は「確認済にする」を押すしかなく、**同意を表明する自然な操作が無い。**

誤り率は「一致／不一致」の比なので、**一致を記録できない測定器は誤り率を測れない。**

### 欠陥3: 監査対象が畳まれて出る（実装の取りこぼし）

追記の #100 で「監査対象は開いて表示する」と直したはずが、実測では
画面内の監査対象58件すべてが畳まれて出ている。修正が効いていない箇所がある。

### 直し方（未実装）

- **(1) 監査標本は反映の対象から外す。** 機械が統合と判定しても適用を保留し、
  人が見るまで候補に残す。これをしないと誤統合は永久に測れない。
- **(2)「機械の判定に同意」ボタン。** グループを変えずに `touched` を立てられるようにする。
- **(3) 欠陥3 の実装を直す。**

### この日の総括（測定について）

| | 回数 |
|---|---|
| 監査ツールの**計算**の誤りを直した | 5回（向き・分母・鮮度・生存者バイアス・引き直し） |
| その結果で**機械側**を動かした | 2回（#96 緩め → #108 締め） |
| そのうち**戻した** | 2回 |
| **測定の設計**の欠陥に気付いた | 最後（追記14の独立性、追記15の3件） |

計算を5回直しても、**測っているものが人の目に届いていない**ことには気付かなかった。
分子・分母・鮮度・偏りはすべて「式が正しいか」の問題で、
**「そもそも観測できているか」は一度も点検していなかった。**

そして気付いたきっかけは毎回ユーザーの違和感で、今回は
「グループが変わらない場合も確認済みにする必要があるのか」という**操作についての素朴な問い**だった。
統計の話ではなかった。

**教訓**: 測定器を作ったら、まず「この標本は人の目に届くか」「一致は記録されるか」を確かめる。
率の計算はその後。

## Compile Log（追記15ぶん）

- **「機械の統合判断は人が見る前に適用されて消えるので誤統合は監査できない」**は
  wiki（team-player-identity.md）へ反映する。監査の結果を読む上での決定的な制約で、
  知らずに「誤統合0%」を見ると安全だと誤解する。
- **欠陥2・3 と直し方の3案**は raw のみ。未実装で、実装時に設計し直す可能性が高い。

---

## 追記16（2026-09-11・別セッション）: 書き戻し漏れの発見 — レビューは追記15の同日夜に完了していた

ユーザーから「残り48件の判断を進めたい」と依頼を受けたが、着手前に現状を確認したところ、
**この作業は既に完了しており、raw への書き戻しだけが漏れていた**ことが分かった。

### 現状確認でわかったこと

`data/teams/merge-candidates.json` を作り直し（`build-team-merge-candidates.mjs`）、
`scripts/lib/review-ledger.mjs` の `clusterKey` で `review-decisions.json` と突き合わせたところ、
**現在の候補358件（`signal:"players"` 19件を含む）は1件残らず判断済み**だった
（再生成前後で候補の中身は無変化・差分ゼロ）。

台帳の中身を集計すると、判断はすべて `decidedAt: "2026-09-06"`。
git log で該当日の夜のコミットを確認すると:

```
21:23  00defc2a  監査対象の「確認済にする」がリロードで消えるのを直す
21:36  3d5f25a5  更新（候補・台帳・alias・マスタを一括反映）
```

**追記15を書いた直後、同じ晩のうちにユーザー自身がレビューを最後まで終え、反映まで済ませていた。**
セッションが追記15で止まっていたため、この続きが raw に一度も記録されないまま今日まで残っていた。

### 欠陥3の実際の原因は、追記15の仮説と違っていた

追記15は欠陥3を「実装の取りこぼし」（`監査対象は畳まずに開いて表示する` の修正が効いていない）
と推測していたが、`00defc2a` のコミットメッセージによる実際の原因は別だった:

```
s.reviewed = true;  →  touch(i)  →  st(i)
```

という順で処理していたため、`reviewed` を立てた直後にまだ `touched` が false の段階で `st(i)`
（監査対象を「未確認」として開く補正）が走り、`reviewed` が false に戻されていた。
その後で `touched = true` が立つので、保存されるのは「未確認だが触った」という壊れた状態になり、
リロードすると確認済みが消えて畳まれて見える——**表示のロジックではなく、確認済みフラグを
セットする処理順序の不具合**だった。修正は「補正を `openIfAudit()` に切り出し、state の
初期化時にだけ適用する」。

### 現状のまとめ

| 欠陥（追記15） | 状態 |
|---|---|
| 欠陥1: merge層は原理的に測れない（機械の統合判断が人の目に届く前に適用されて消える） | **未解決**（wiki既記載） |
| 欠陥2:「機械の判定に同意」を記録する操作が無い | **未解決**（wiki未記載 → 今回追記） |
| 欠陥3: 監査対象が畳まれて出る | **解決済み**（`00defc2a`、原因は表示ロジックではなく確認済みフラグのセット順序） |
| レビュー本体（候補358件・うち players 19件） | **全件判断済み**（2026-09-06 21:36 時点、以降追加候補なし） |

欠陥1・2が残ったままなので、**「誤統合0%」は今も安全の根拠にならない**。母集団49件（merge層）が
「ほぼ全数の確定値になる」という追記14の見立ては、判断自体は全数終わったが、
**監査（＝機械の判定が正しかったかの検証）としては欠陥1により依然として測れていない**、
という状態で止まっている。

### 次にやるなら

ユーザーは今回「欠陥1・2の実装」「PDF取り込み系への横展開」ではなく「この書き戻し」を選んだ
（他の2択も提示済み）。したがって実装の着手はまだ先——次にこの raw を開く人（自分自身を含む）は、
上の表から欠陥1・2の実装に進むか、「充実」の定義作業に進むかを選ぶところから再開する。

## Compile Log（追記16ぶん）

- **欠陥3の実際の原因（フラグのセット順序）と修正内容**、**現状まとめの表**、
  **「誤統合0%は依然として安全の根拠にならない」の継続**は wiki（team-player-identity.md）へ反映する。
  欠陥1は既に反映済みだが、欠陥2（同意操作が無い）と欠陥3の解決は未反映だったため追加する。
- **書き戻し漏れが起きた経緯そのもの**（セッションが追記15で止まった理由の推測、等）は
  raw のみ。運用の教訓ではあるが、原因はセッション境界という偶発事情で、
  一般化して姉妹アイデア（[AIとの共同探索の属人性](./2026-07-11-idea-ai-co-exploration-context.md)）
  側に置くべき論点かもしれないが、今回はそこまで踏み込まず事実の記録に留めた。
- **ユーザーへの3択提示と選択結果**は raw のみ。次回再開時の「どこから始めるか」の参考情報であり、
  wiki に載せる仕様・現状ではない。
- **回数の総括表と「観測できているかを先に確かめる」という教訓**は raw のみ。運用の学び。

---

## 追記17（2026-09-11）: 欠陥1を実装で埋めた／欠陥2は誤診断だったと判明

ユーザーの「先に穴を実装で埋めたい」を受けて着手。実装前にコードを読み直したところ、
**欠陥2は実在しないバグだった**。着手を欠陥1の実装だけに絞った。

### 欠陥2の再診断: 「同意を記録できない」は誤りだった

追記15・追記16は「グループを変える必要が無いとき、同意を表明する操作が無い」と書いたが、
`build-team-review-html.mjs` の「確認済にする」ボタンのハンドラを読むと:

```js
b3.onclick=()=>{s.reviewed=true;touch(i);save();render();}
```

**`touch(i)` を呼んでおり、`s.touched` が立つ。** `buildOutput()` の

```js
decidedBy: s.touched ? 'human' : (AUTO[i] === true ? 'auto' : 'human')
```

と合わせると、グループを一切変えずに「確認済にする」を押しても `decidedBy:'human'` として
記録される。つまり**「見て、変更せずに同意した」は既に区別できている**。

`git log -S` で該当行の履歴を追うと、この `touch(i)` は `0b560ddb`
（2026-09-06 12:00、判断台帳を導入したコミットそのもの）から一貫して存在し、
追記15を書いた時点（同日21時台）でも既にこの状態だった。つまり**当時のコードを見誤っていた**。

念のため生成済みの `data/teams/team-merge-review.html`（実際に配信されるファイル）も確認し、
同じ実装であることを確認した。ソースと配信物の乖離も無かった。

**教訓**: この日（追記14・15）は「計算を5回直しても気付かなかった」という反省を書いたが、
**その反省を書いた直後の欠陥2の診断そのものが、コードを読まずに書かれた誤りだった**。
「実装を見ずに症状から原因を決め打ちしない」という、このスレッドが何度も学び直している
教訓をもう一度確認した形になる。

### 欠陥1: 実装で埋めた

真の原因を実装レベルで特定し直した。当時の設計では、抜き取り監査の母集団を
「台帳に `decidedBy:'auto'` として記録済みのもの」から引いていたが、**その記録自体が
「既に alias 適用された後」にしか作られない**（レビュー画面の一括反映は、台帳登録と
alias 適用を同じ操作で同時に行う）。つまり引ける時点で常に手遅れだった。

加えて調べ直すと、**`apply-auto-merges.mjs`（自動OKクラスタを一括適用する別スクリプト）は
判断台帳を一切参照も書き込みもしていなかった**——このスクリプト経由で適用された統合は、
台帳に痕跡すら残らず、抜き取り監査の母集団に永遠に入らない。これは追記15が書いた
「レビュー画面での消失」よりさらに直接的な欠陥1の実例で、当時は見落としていた。

実装した変更:

1. **`scripts/lib/review-ledger.mjs`**: `heldAuditKeys(rounds, decisions)` を追加。
   抜き取り監査で引かれたが、まだ `decidedBy:'human'` の記録が無いキーの集合を返す。
2. **`scripts/audit-review-sample.mjs` `--draw`**: 母集団を「台帳の既存記録」から
   「`merge-candidates.json` 上でいま自動OK判定されていて、まだ台帳に一切記録が無いもの」に
   変更（`isAutoOK`/`machineVerdict` を `team-grouping.mjs` からその場で計算）。
   引いた直後に `build-team-review-html.mjs` を自動で再生成し、レビュー画面の
   「監査対象」判定（`AUDIT` 集合）を即座に最新化するようにした（手動での再生成忘れを防ぐ）。
3. **`scripts/apply-auto-merges.mjs`**: 独自実装だった `defaultGroups`/`autoOK` を
   `scripts/lib/team-grouping.mjs` の共通実装に差し替え（レビュー画面・監査と判定基準がずれる
   余地を無くした）。適用前に `heldAuditKeys()` で監査保留中のクラスタを除外し、
   適用したクラスタは判断台帳へ `decidedBy:'auto'` として記録するようにした。
   **さらに実装中に見つけた別バグも直した**: 台帳に既に人の判断（例: 「別チーム」）が
   あるクラスタも、機械のisAutoOK判定だけを見て無条件に上書き適用していた
   （台帳を一切見ていなかったので当然の欠落だが、直す過程で気付いた）。
   「台帳に既存の判断があれば理由を問わず触らない」を先に確認するよう修正。

### 検証（本番データではなくscratchpadの合成データで）

本番の `data/teams/*.json` は既に全件判断済み（追記16）で「これから保留されるべき未決定の
自動OKクラスタ」が実在しないため、動作確認は scratchpad に切り出した合成データ
（3クラスタ: A/B/C、いずれも自動OK相当）で行った。本番データは一切変更していない。

1. `--draw 2 --stratum merge` で A・B を抽出 → `review-audit.json` に記録、
   レビューHTML再生成で `AUDIT` 集合に反映されることを確認。
2. `apply-auto-merges.mjs` 実行 → **A・B は保留されてalias未適用、Cのみ適用**
   （ログ: `自動OKクラスタ: 1 / 監査対象で保留: 2`）。Cは台帳に `decidedBy:'auto'` で記録。
3. 再度 `apply-auto-merges.mjs` 実行（冪等性の確認） → 0件適用（Cは判断済でスキップ、
   A・Bは保留のまま）。
4. Aを「人が別チームと判断」した状態を台帳に直接書き込み → 三度目の実行で
   **Aは統合されない**（「判断済でスキップ」に回る）ことを確認。これが今回追加で見つけた
   「人の判断を無条件上書きする」バグの直接的な再現・修正確認。
5. `--draw`で3件（A/B/C相当）を保留 → `apply-auto-merges.mjs`で全件保留を確認
   → 人の判断を模擬入力（2件は機械に同意=merge、1件は覆す=separate）→
   `--report` で **誤統合1/3・下限6.1%が閾値5%を超え、終了コード1**（＝「自動OKを締めよ」の
   判定）を確認。抜き取り監査の一連の流れ（引く→保留→人が判断→誤り率が出る→閾値判定）が
   末端まで動作することを確認した。

### 現状のまとめ（更新）

| 欠陥 | 状態 |
|---|---|
| 欠陥1: merge層は原理的に測れない | **実装で解決**（2026-09-11・上記） |
| 欠陥2: 「機械の判定に同意」を記録する操作が無い | **誤診断だったと判明**（実際は元から記録できていた） |
| 欠陥3: 監査対象が畳まれて出る | 解決済み（追記16参照） |

**残っている作業**: 本番データで実際に `--draw` を回し、抜き取り監査を初めて機能させること
（今回は合成データでの動作検証のみ）。また、レビュー画面からの一括反映
（`team-review-server.mjs` の `POST /apply`）は、保留の実効性をクライアント側の状態
（`openIfAudit()`・再生成されたレビューHTMLの `AUDIT` 集合）に委ねている。**サーバ側では
二重チェックしていない**——引いた直後にレビューHTMLを再生成しているので通常は問題ないが、
「引く前から開きっぱなしのタブ」で古い状態のまま一括反映を押した場合の防御は無い
（`additions` がクラスタキーを持たない配列形式のため、サーバ側で個別に弾くには
ペイロード形式の変更が要る。今回はスコープ外とし、既知の残課題として記録するに留めた）。

## Compile Log（追記17ぶん）

- **欠陥2は誤診断で、実際は最初から記録できていたこと**、**欠陥1の実装内容の要約**、
  **現状まとめの表**、**サーバ側の二重チェック無しという既知の残課題**は
  wiki（team-player-identity.md）へ反映する。特に欠陥2の訂正は、前回（追記16）に
  「未解決」として書き戻したばかりの内容を上書きする必要があるため優先度が高い。
- **検証をscratchpadの合成データで行った経緯・手順の詳細（A/B/C/擬似ID等）**は raw のみ。
  再現方法の記録として価値はあるが、wikiは「現状の仕様」を持つ場所であり、
  検証ログはそこに置くものではない。
- **`git log -S` で欠陥2の履歴を確認した手順そのもの**は raw のみ。誤診断だったと分かった
  経緯の記録であり、今後のwiki読者に必要な情報ではない。

---

## 追記18（2026-09-11）: 本番データで意図的テストを実施／欠陥1の実例を過去データに発見

ユーザーの依頼で、合成データではなく本番データに対して欠陥1の修正を意図的にテストした。

### 本番データでのテスト（実施・完全に復元済み）

`review-decisions.json` から実在する1件の判断（「北越」「北越高校」＝新潟県、人が2026-09-06に
「別チーム」と判断・25回改訂）を一時的に削除して「未判断」状態を人工的に作り、
一連の流れを確認した:

1. `--draw 1 --stratum merge` → 母集団1件（この1件のみ）から正しく抽出、
   `review-audit.json` に記録・レビューHTML再生成で「監査対象」バッジが即座に反映されることを確認。
2. `apply-auto-merges.mjs` 実行 → **保留され、alias は一切変更されなかった**
   （他の335件の既決定クラスタは正しくスキップ）。
3. 人の判断（元の「別チーム」）を復元 → `--report` が正しく「機械:merge → 人:separate」の
   覆しとして計上することを確認。
4. `git checkout` で `review-decisions.json`・`review-audit.json`・`team-merge-review.html` を
   完全に復元。`team-name-aliases.json` 等は最初から一切変更していない。

**副産物**: テストに使った「北越」「北越高校」について、ユーザーが選手データ（水澤奈央が
両方に登場、髙澤颯/高澤颯が異体字違いで同一人物の疑い）から「同じチームかもしれない」と
指摘。当時の「別チーム」判断（25回改訂）の根拠は今回のセッションからは分からず、
**実際に統合すべきかは別途レビュー画面で検討中**（本追記の時点で未確定・ユーザー預かり）。

### 発見: 過去の監査対象141件のうち58件が「機械判定のまま」だった

レビュー画面をユーザーが自分の環境で開いた際に「違うチームが表示された」という報告を受けて
調査したところ、`review-audit.json` に記録された過去の監査対象141件のうち
**58件が今も `decidedBy:"auto"` のまま**（人が最終確認した記録が無い）と判明した。

**この58件は現在の `merge-candidates.json` から全て消えている**——つまり
レビュー画面の「監査対象」フィルタを開いても、この58件はもう画面に出てこない
（現在の候補358件はすべて `decidedBy:"human"` または元から候補に残り続けている
「別チーム」判定で、todoは0）。**これは追記15が発見した欠陥1の実例そのもの**：
旧設計では監査対象は「台帳にdecidedBy:'auto'として記録済み＝既に適用済み」のものから
引いていたため、引いた時点で既に統合されて候補から消えていた。今回58件全てで
この経路を実際に踏んでいたことが裏付けられた。

58件をすべて突き合わせて調べた結果:

| 内訳 | 件数 | 内容 |
|---|---|---|
| 元のペア同士が同じcanonicalへ収束（＝統合された） | 44件 | 機械の自動OK判定通りに統合済み |
| 元のペアは統合されず、片方が無関係な表記ゆれで別のcanonicalに吸収された | 14件 | 統合はされていない。候補から消えたのは無関係な理由 |

44件のうち「スポ少（少年団・クラブ）」と「中学校」が統合されている2件
（東出雲スポ少↔東出雲中学校、南関ジュニア↔南関中学校）を選手名で確認したが、
**既に統合済みのため統合前の個別データが残っておらず、遡って正誤を検証する手段が無い**。
ジャンル・年度は不自然ではなかった（同じ町の少年団が中学生年代まで継続、という
このデータでよくあるパターン）が、確証ではない。

**結論**: 明確な誤りの証拠は無いが、**「一度統合すると検証不能になる」という欠陥1の本質的な
危険性を、この58件が実例として証明している**。今日の修正（追記17）により、今後同じ経路で
統合されるクラスタは**統合前に**監査対象として保留されるため、この種の「後から検証できない」
状態は今後発生しなくなる。過去の58件については遡って取り消す実益・根拠が無いため、
記録に残すのみで対応は行わない。

### 現状のまとめ（再更新）

- 欠陥1: 実装で解決済み（追記17）。**本番データでの意図的テストでも動作確認済み**（本追記）。
- 欠陥2: 誤診断（追記17）。
- 欠陥3: 解決済み（追記16）。
- **新規判明**: 過去の監査対象58件が「検証不能なまま統合済み」——欠陥1が実際に本番データへ
  与えていた実害の実例。遡及対応はしない（できない）。
- 本番での**新しい**（テスト用でない）`--draw` はまだ実施していない。次に大会データが
  取り込まれ未判断の自動OKクラスタが生じたとき、または任意のタイミングで手動実行できる。

## Compile Log（追記18ぶん）

- **本番データで欠陥1の修正が動作することを確認したこと（完全に復元済み）**、
  **過去の監査対象58件が検証不能なまま統合済みだったという発見とその内訳（44/14）**、
  **遡及対応はしないという判断**は wiki（team-player-identity.md）へ反映する。
  特に58件の発見は「誤統合0%」の読み方に関わる重要な事実（監査記録に載っていても
  実際には一度も人の目を通っていないケースが過去に存在した）。
- **「北越」が同じチームかもしれないという指摘と未確定の状態**は raw のみ。
  結論が出ていない個別データ判断であり、wikiに載せる仕様・現状ではない。
  結論が出たら別途レビュー画面での判断として記録する。
- **テストの具体的な手順（削除→draw→apply→復元のコマンド列）**は raw のみ。
  再現方法の記録であり、wikiは現状の仕様を持つ場所のため。

---

## 追記19（2026-09-11）: 新しい欠陥（欠陥4）を発見・実データを1回破損させ、復元した

追記18の調査中、ユーザーがレビュー画面で実際に1件だけ「確認済にする」→「確認済を反映」を
押したところ、意図せず**41件の`decidedBy`が`"human"`から`"auto"`へ巻き戻った**
（`verdict`・グループ分けなど実質的な判断内容は無変化。壊れたのは「人が確認した」という
記録だけ）。

### 原因: ブラウザの`localStorage`キャッシュがリポジトリの台帳と再同期されない

`build-team-review-html.mjs`の`st()`は、`state[k]`が**まだ無いときだけ**台帳
（`LEDGER`）から初期値を作る。逆に言えば、**一度でも`localStorage`にキーが乗ってしまえば、
その後リポジトリ側の台帳がどう更新されようと二度と参照されない**。

このユーザーの実ブラウザには、2026-09-06〜07当時（まだ多くのクラスタが`decidedBy:"auto"`
だった頃）のキャッシュが残っていたと見られる。今回「確認済を反映」を押すと
`buildOutput()`は**画面に読み込まれている358件全てを一括再送信する**設計（触っていない
クラスタも含む）ため、キャッシュされたままだった41件の古い`touched:false`状態が、
台帳側で既に`"human"`へ更新されていた値を上書きした。

**チーム統合の実体（`data/tournaments/team-name-aliases.json`）は一切変更されていない**
（差分ゼロ）。壊れたのは監査に使う「誰が判断したか」のメタデータのみだが、これは
まさに抜き取り監査の前提そのもの（`decidedBy`が正しく記録されていること）を壊す。

### 対応

ユーザーの了承を得て `git checkout` で `review-decisions.json` / `teams.json` /
`team-context.json` / `merge-candidates.json` / `team-merge-review.html` を
今朝の状態へ完全復元した（`team-name-aliases.json`は元から無変化）。
ユーザーが確認したかった1件も実質的な内容変化が無かったため、復元で失われたものは無い。
レビューサーバは停止した。ユーザーには実ブラウザの`localhost:5173`のサイトデータを
次回利用前にクリアするよう伝えた（クリアしない限り同じ巻き戻りが再発する）。

### 欠陥4として記録

| 欠陥 | 内容 | 状態 |
|---|---|---|
| 欠陥4（新規） | ブラウザのlocalStorageキャッシュが台帳と再同期されず、再送信時に`decidedBy`が巻き戻ることがある | **未修正**（今回は実データ側を復元して回避しただけ） |

根本修正の方向性（未実装・未検討）:
- `buildOutput()`が「今回のセッションで実際にtouchしたクラスタだけ」を送信するようにする
  （現状は表示中の全件を毎回無条件で送っている）。
- または`st()`の初期化を、キャッシュの有無に関わらず**台帳の`decidedBy`を常に信頼する**
  方向に倒す（キャッシュはgroups/canonのUI状態だけに使い、`decidedBy`の判定材料にしない）。
- いずれも欠陥1・2と同様「機械が全件を一括処理する設計と、個々の判断の出所を正しく
  区別する設計」がまだ両立できていない、という同じ根の問題。

## Compile Log（追記19ぶん）

- **欠陥4の発見内容（原因・実害の有無・対応）**と**根本修正の方向性3案**は
  wiki（team-player-identity.md）へ反映する。今後このツールを使う人が同じ事故を
  繰り返さないために必須の情報。
- **実際に何件目のどのクラスタが直接の原因だったか等の調査ログの細部**は raw のみ。
  再現性はあるが、wikiは現状の仕様・既知の課題を持つ場所であり、調査の逐次記録ではない。
