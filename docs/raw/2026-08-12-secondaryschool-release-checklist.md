# 中学カテゴリ（/secondaryschool）公開までのチェックリスト

作成 2026-08-12。仕様は [../wiki/secondaryschool.md](../wiki/secondaryschool.md)、
経緯は [2026-08-12-idea-juniorhigh-category-pages.md](./2026-08-12-idea-juniorhigh-category-pages.md)。

**このファイルは作業の進捗管理用。** 全部片付いたら破棄してよい（内容は wiki と raw に残っている）。
セッションをまたぐので、着手・完了のたびにチェックを更新すること。

## 現在のスコープ（2026-08-12 変更）

「収録チームの名鑑＋進路」に絞る。**県別ポイントは廃止**。
理由: 配点が Assumption（ユーザー未確定）で、かつ実装が全中・クラブ選手権プレを混ぜていたため
「47県が同じ枠数だから公平」という説明と食い違っていた（全中は県ごとの出場枠が20倍違う）。
順位づけをやめれば、残るものが全部「検算できる事実」になる。

---

## A. 方針変更の実行（公開前・必須）

- [x] **A-1. 県別ポイントを削除する**
  - `src/pages/secondaryschool/index.tsx` の「都道府県別の成績」節（上位10県のチップ）と
    `topPrefectures` prop
  - `src/pages/secondaryschool/[prefectureId]/index.tsx` の「都道府県対抗での◯◯県」節
  - `scripts/build-secondaryschool-index.mjs` の `PREF_POINTS` / `prefPoint()` / `rank` 採番 /
    `points` `entries` `best` フィールド
  - `lib/secondaryschool.ts` の `SecondarySchoolPrefecture` から `points` `entries` `best` `rank`
- [x] **A-2. description からポイント言及を外す**
  - 県ページの「ポイント◯◯で全国◯位」→ 収録チーム数ベースの文言へ
- [x] **A-3. 高校の学校ページに逆引きを追加する（81校）**
  - `lib/highschoolFeederSchools.ts` を追加し、高校の学校ページに「{高校名}の選手の出身中学」節を実装。新規URLなし
  - **実装で1点判明**: 高校の学校ページは男女別なので、進路レコードに `highschoolGender` が必要だった。
    `pathways.json` に追加済み（boys 85件 / girls 99件）。`mixed` は男女どちらのページにも出す
  - 動作確認: `昇陽`(girls) 12件・`昇陽`(boys) 0件と正しく分かれた。`岡崎城西`(boys) は3中学から4名
- [x] **A-4. `seo.md` の重複マップに中学カテゴリを追記**（#13 として追加）
  - プロジェクト規約（seo.md「新規ページ追加時の運用」）で必須
  - **`/teams/[teamId]` との重複は0件と確認済み**（中学280チームはどれも
    `team-name-mappings.json` に無いので `/teams/` 側のページが存在しない）
  - 制御手段は「重複なし・掲載閾値5で薄いページを抑制」と書けばよい
- [x] **A-5. docs を方針変更に合わせて更新**
  - `secondaryschool.md` の「都道府県ページ」節を書き換え（ポイントの記述を削除）
  - ポイント廃止の判断経緯を idea raw に追記

## B. 公開前の確認

- [ ] **B-1. `npm run build` の完走** — **C-2/C-3/C-5 の名寄せでページ数が 328→321 に変わったため再実行が必要**
- [ ] **B-2. 生成ページ数の再確認**（C-3/C-5 の統合で **チーム273＝321ページ** に変化。再ビルド時に確認）
- [x] **B-3. 内部リンクの確認**（`out/` に対する grep で実測）
  - サイドナビ「特集 > 中学」: 全ページに `href="/secondaryschool/"` あり
  - 全中の大会ハブ → `/secondaryschool/`: 1本
  - 高校の学校ページ → 中学チームページ: **88ページ**（男子44・女子44）に「出身中学」節
  - 中学ページへの内部リンクを含むHTML: 4,240ファイル
  - sitemap: 328件すべて掲載・欠落0・実ファイルの無いURL0
  - 中学ページに `robots` meta 無し（＝index対象。掲載閾値でそもそも薄いページを作らない方針どおり）

## C. データ品質（公開後でよいが放置しない）

- [x] **C-1. teamId のローマ字読み** → **2026-08-12 一通り確認済み**（override 23件）
  - 残り254件は「pykakasi の読みで問題なし」と判断した結果。無変更＝確認済みの意味
  - 検証: override に索引外の名前0・不正文字0・県内衝突0。`-2` が付くIDも0
  - 今後データが増えたときの手順: override を書く → `npm run secondaryschool:build`
    → `npm run secondaryschool:teamid-todo`（確認済みが除外されたリストが出る）
  - リストは [2026-08-12-secondaryschool-teamid-review.md](./2026-08-12-secondaryschool-teamid-review.md)
- [x] **C-2. `朝日`（愛知）/ `野木`（栃木）が学校かクラブか** → **選手名の一致で決着**
  - `朝日`（愛知・5件）の選手は `朝日中学校` の顔ぶれと完全一致（`朝日STC` とは重ならない）
  - `野木`（栃木）は**大会の系統で指すものが変わる**。中学校大会→`野木中学校`（鈴木大雅ほか）、
    **小学生大会→`野木クラブ`**（別符楓都・上倉穂乃実ほか）
  - 同名の別団体が他県にもあるため（`朝日` は兵庫・長野にも）alias の scope に
    **`prefecture` を追加**して解決（[ADR-013](../adr/ADR-013-scoped-team-name-aliases.md) 追記）
  - **`-2` の付いた teamId は全滅**。チーム 280→277、ページ 328→325
- [x] **C-3. 設置者接頭辞つき／なしの未統合ペア** → **2026-08-12 統合済み（4件）**
  - `羽須美中学校`←`邑南町立羽須美中学校`（選手5人共通）/ `名寄中学校`←`名寄市立名寄中学校` /
    `中広中学校`←`広島市立中広中学校` / `連島中学校`←`倉敷市立連島中学校`（選手4人共通）
  - **判定はすべて選手名の突き合わせ**。パターンは共通で、2026年度のブロック大会だけが
    設置者接頭辞つきの正式名称、それ以前は略称という出典差
  - canonical は**略称側**を採った。高校も `上田染谷丘`（長野県立…）のように設置者を落としており、
    URLも短くなる。設置者接頭辞つきの表記は alias として残る
  - 索引に残る設置者接頭辞つき14件は**略称版が存在しない**ので統合不要
  - 20文字超のIDは5件のみ（`北海道教育大学附属旭川中学校` など、名前自体が長いもの）
- [ ] **C-4. `函館市拠点校部活動（巴中学校）` の表記**
  - 括弧内が実際の学校名で外側が制度名。他の括弧書きのように機械的に落とせない。
    大会要項の確認が要る。現状2件で掲載閾値未満のためページは無い
- [x] **C-5. `四天王寺高校` が中学データに混入** → **2026-08-12 修正済み**
  - 当初「高校名の団体が中学データに混入」と報告したが**不正確だった**。四天王寺は中高一貫校で、
    `四天王寺高校` はインターハイ・ハイジャパ等に45件出ている**正規の高校チーム**
  - 実際の問題は「**全中2022の所属が誤って高校名で記録されていた**」1件（房野紗千・市川こいと）
  - `四天王寺中学校` へ寄せる alias を **中学校の大会・大阪府に限定**して追加。
    高校の大会に出ている `四天王寺高校` は無傷（45件のまま）
  - 中学側は2件で掲載閾値未満のためページは生成されない
- [ ] **C-6. 高校の地区大会に中学校名が4種混入**
  - `highschool-kinki-block/2026` の `日高中学校`(6)、`highschool-shikoku-block/2026` の
    `土佐中学校`(4)、`highschool-kyushu-block/2026` の `新宮中学校`(2)、
    `highschool-hokushinetsu-block/2026` の `柏崎ジュニア`(2)
  - **元データの時点でこの表記**（`git show HEAD:` で確認済み）。出典を当たらないと直せない

## D. 公開後

- [ ] **D-1. GSC でインデックス状況を確認**
- [ ] **D-2. 効果測定**（`{中学校名} ソフトテニス` `{県名} 中学 ソフトテニス` を拾えているか）

---

## 完了済み（参考・2026-08-12）

- 中学カテゴリの是非を実データで検討 → フル横展開を選択
- 名寄せ（[ADR-013](../adr/ADR-013-scoped-team-name-aliases.md) で alias に大会スコープを導入）。
  ヘルスチェックの要対応シグナル 72→40、別名残存 4→0
- 地域移行トラッカーを全中の大会ハブに実装（`npm run club:verify`）
- `/secondaryschool` の3階層（328ページ）と進路（184件/83チーム）を実装
- teamId の短縮（平均10文字）・長音を `ou` に統一・括弧書き8種の整理
