# 中学カテゴリ（/secondaryschool）公開までのチェックリスト

作成 2026-08-12。仕様は [../wiki/secondaryschool.md](../wiki/secondaryschool.md)、
経緯は [2026-08-12-idea-juniorhigh-category-pages.md](./2026-08-12-idea-juniorhigh-category-pages.md)。

**このファイルは作業の進捗管理用。** 全部片付いたら破棄してよい（内容は wiki と raw に残っている）。
セッションをまたぐので、着手・完了のたびにチェックを更新すること。

## 現在のスコープ（2026-08-12 変更）

「収録チームの名鑑＋進路」に絞る。**県別ポイントは廃止**。
理由: 配点が Assumption（ユーザー未確定）で、かつ実装が全中・クラブ選手権プレを混ぜていたため
「47県が同じ枠数だから公平」という説明と食い違っていた（全中は県ごとの出場枠が20倍違う）。
順位づけをやめれば、残るものが全部「検算できる事実」になる。

---

## A. 方針変更の実行（公開前・必須）

- [x] **A-1. 県別ポイントを削除する**
  - `src/pages/secondaryschool/index.tsx` の「都道府県別の成績」節（上位10県のチップ）と
    `topPrefectures` prop
  - `src/pages/secondaryschool/[prefectureId]/index.tsx` の「都道府県対抗での◯◯県」節
  - `scripts/build-secondaryschool-index.mjs` の `PREF_POINTS` / `prefPoint()` / `rank` 採番 /
    `points` `entries` `best` フィールド
  - `lib/secondaryschool.ts` の `SecondarySchoolPrefecture` から `points` `entries` `best` `rank`
- [x] **A-2. description からポイント言及を外す**
  - 県ページの「ポイント◯◯で全国◯位」→ 収録チーム数ベースの文言へ
- [x] **A-3. 高校の学校ページに逆引きを追加する（81校）**
  - `lib/highschoolFeederSchools.ts` を追加し、高校の学校ページに「{高校名}の選手の出身中学」節を実装。新規URLなし
  - **実装で1点判明**: 高校の学校ページは男女別なので、進路レコードに `highschoolGender` が必要だった。
    `pathways.json` に追加済み（boys 85件 / girls 99件）。`mixed` は男女どちらのページにも出す
  - 動作確認: `昇陽`(girls) 12件・`昇陽`(boys) 0件と正しく分かれた。`岡崎城西`(boys) は3中学から4名
- [x] **A-4. `seo.md` の重複マップに中学カテゴリを追記**（#13 として追加）
  - プロジェクト規約（seo.md「新規ページ追加時の運用」）で必須
  - **`/teams/[teamId]` との重複は0件と確認済み**（中学280チームはどれも
    `team-name-mappings.json` に無いので `/teams/` 側のページが存在しない）
  - 制御手段は「重複なし・掲載閾値5で薄いページを抑制」と書けばよい
- [x] **A-5. docs を方針変更に合わせて更新**
  - `secondaryschool.md` の「都道府県ページ」節を書き換え（ポイントの記述を削除）
  - ポイント廃止の判断経緯を idea raw に追記

## B. 公開前の確認

- [ ] **B-1. `npm run build` の完走** — **ページ数が 328→343 に変わったため再実行が必要**。
  ビルドはネットワークを使う生成ステップ（beta matches）を含むためサンドボックスでは完走できない
- [ ] **B-8. `npm run highschool:pipeline` の実行**（C-8 のスクリプト修正を反映する。
  `os.listdir` の順序が macOS と Linux で違うため生成物はサンドボックスから持ち込んでいない）
- [ ] **B-2. 生成ページ数の再確認**（チーム**293**＋県47＋入口1＋進路一覧2＝**343ページ**）
  - C-6 の修正ついでに**全中の団体戦データが未正規化だったことが判明**し、`--scope=all` で
    171箇所を正規化した（`加木屋` と `加木屋中学校` 等が別チームに割れていた）。
    これでチーム 273→**285**、進路 346→**367件**／112→**122チーム**に増えた
- [x] **B-4. 内部リンクとSEO要素の追加**（2026-08-12・ユーザー依頼）
  - **トップページ「属性別成績」に中学カテゴリのカード**を追加（高校と2カラムで並置）
  - **中学チームページの「進路」で高校名をリンク化**。逆引き（高校→中学）は実装済みだったが
    順方向がテキストのままだった。`getSchoolResolver()` で解決し **184/184 件すべてリンク可能**
  - **中学の県ページ ↔ 高校の県ページの相互リンク**（各94本）。`prefectureId` は47件すべて共通。
    高校側は中学ページの `teamCount > 0` を確認してから出す（デッドリンク防止）
  - **3階層すべてに `BreadcrumbList` JSON-LD**。高校は全階層に入れていたが中学は入口に構造化データが
    無く、県・チームも BreadcrumbList を持っていなかった
  - `docs/wiki/seo.md` #13 の内部リンク表と構造化データの記述を更新済み
- [x] **B-5. `/secondaryschool/pathways/` の新設**（2026-08-12・ユーザー選択）
  - きっかけは**入口ページの「中学から高校への進路」カードが `<div>` のままだった**こと。
    隣の「全中の結果」カードと同じ枠線・同じ見た目なのに押せず、リンクに見えて機能しない状態だった
  - 選択肢は「上位チームを並べる」「リンクでないと分かる見た目にする」「一覧ページを新設」の3案で、
    ユーザーは**一覧ページの新設**を選択
  - 最初は**中学起点**（この中学からどこへ進学したか）で作ったが、ユーザーの
    「高校に対して進学元となった中学一覧にしたい」を受けて**高校起点に作り替えた**
  - 作り替え前に「高校起点は薄い」と評価したが、**これは一面的だった**。
    グループ数だけ見ると 高校81 に対し8割が出身中学1つで薄く見えるが、
    実際に組んでみると**越境進学の集中**が見える。中学起点では1件ずつ別ページに散る情報:
    - 東北（宮城・男子）← 埼玉の上青木中学校から2名
    - 広島翔洋（広島・女子）← 愛知の朝日丘中学校2名＋埼玉の芝東中学校2名
    - 学法石川（福島・女子）← 岩手の黒石野中学校から2名
    - 朝日丘中学校（愛知）は岡崎城西・広島翔洋・三重の3校へ供給している
  - **184名中16名（9%）が県外**。ペア継続で採用された分がここに当たる（採用根拠が最も強い層）
  - 88グループ（高校×性別）を「出身中学の種類が多い順 → 人数順」で掲載。
    県外・同名の系列校にはラベルを付ける（系列校は184件中47件＝26%）
  - 高校の学校ページ88枚からこのページへ1本ずつ返す（既存の強いページから新カテゴリへ）
  - **総ページ数 321 → 322**
- [x] **B-6. 進路の採用条件を緩和**（2026-08-12・ユーザー提案 → [ADR-014](../adr/ADR-014-pathway-name-match.md)）
  - ユーザーの「縛りが厳しい。3年以内に同姓同名が高校に出てきたら同一人物にするのはどう？」から。
    さらに「中1で出て高3の可能性もあるから5年」と修正が入り **`MAX_YEAR_GAP = 5`**
  - **県一致・ペア継続のゲートを撤廃**。検証: ペア継続で同一人物が確定している96名に
    「氏名一致＋年差」だけを当てると **96/96 正解**。同姓同名の証拠がある氏名は全DBで29件だけ
  - 同姓同名の判定を「疑い」から**証拠ベース**（同一大会・同年・同種目に別チームで2人以上）へ。
    旧判定は 0件/2,956人 しか捕まえておらず、ゲート撤廃後の安全弁として機能しなかった
  - **進路 184→346件 / 中学 83→112チーム / 高校 88→111グループ / 複数中学から 16→41校**
  - **要監視**: 県またぎが 9%→51% に跳ねた。氏名一致のみの162件のうち**完全に単独が112件**あり、
    誤マッチが混じっている可能性がある。`basis` を残してあるので後から絞り込める
  - 各ページの注記を「県一致かペア継続のみ掲載」→「同姓同名の別人が含まれる可能性」に書き換え
- [x] **B-7. 進路一覧を男女別URLに分割**（2026-08-12・ユーザー依頼）
  - `/secondaryschool/pathways/` 1枚 → `/secondaryschool/pathways/boys/` と `/girls/` の2枚
  - 判断材料: **中学112チームのうち男女両方に出るのは22だけ**（45ずつが片側のみ）。
    高校は男女で完全に別なので、分割は「同じ内容を薄く割る」のではなく
    **もともと別の内容を正しく分ける**ことになる。`/highschool/boys/` と `/girls/` と同じ構造
  - 既存の `HighschoolGenderToggle`（Link ベース）をそのまま使用。JS不要
  - グループ見出しの「男子/女子」表記はページ全体で固定になったので**削除**
  - 高校の学校ページからのリンクを**同じ性別のページ**へ向けた
  - **入口ページのカードは1枚のリンクで男子へ送る**（2026-08-13 ユーザー指示）。
    隣の「全中の結果」カードと同じUIに揃えるため。女子へはリンク先の性別トグルで移動できる。
    中学チームページは男女2本のまま（こちらは男女をまとめたページなので）
  - 規模: 男子56校171名（複数中学から19校）/ 女子55校175名（同22校）。リンク未解決0
  - **未公開なので移行コストゼロ**。決めるならこのタイミングが最適だった
  - **総ページ数 322 → 323**
- [x] **B-3. 内部リンクの確認**（`out/` に対する grep で実測）
  - サイドナビ「特集 > 中学」: 全ページに `href="/secondaryschool/"` あり
  - 全中の大会ハブ → `/secondaryschool/`: 1本
  - 高校の学校ページ → 中学チームページ: **88ページ**（男子44・女子44）に「出身中学」節
  - 中学ページへの内部リンクを含むHTML: 4,240ファイル
  - sitemap: 328件すべて掲載・欠落0・実ファイルの無いURL0
  - 中学ページに `robots` meta 無し（＝index対象。掲載閾値でそもそも薄いページを作らない方針どおり）

## C. データ品質（公開後でよいが放置しない）

- [x] **C-1. teamId のローマ字読み** → **2026-08-12 一通り確認済み**（override 23件）
  - 残り254件は「pykakasi の読みで問題なし」と判断した結果。無変更＝確認済みの意味
  - 検証: override に索引外の名前0・不正文字0・県内衝突0。`-2` が付くIDも0
  - 今後データが増えたときの手順: override を書く → `npm run secondaryschool:build`
    → `npm run secondaryschool:teamid-todo`（確認済みが除外されたリストが出る）
  - リストは [2026-08-12-secondaryschool-teamid-review.md](./2026-08-12-secondaryschool-teamid-review.md)
- [x] **C-2. `朝日`（愛知）/ `野木`（栃木）が学校かクラブか** → **選手名の一致で決着**
  - `朝日`（愛知・5件）の選手は `朝日中学校` の顔ぶれと完全一致（`朝日STC` とは重ならない）
  - `野木`（栃木）は**大会の系統で指すものが変わる**。中学校大会→`野木中学校`（鈴木大雅ほか）、
    **小学生大会→`野木クラブ`**（別符楓都・上倉穂乃実ほか）
  - 同名の別団体が他県にもあるため（`朝日` は兵庫・長野にも）alias の scope に
    **`prefecture` を追加**して解決（[ADR-013](../adr/ADR-013-scoped-team-name-aliases.md) 追記）
  - **`-2` の付いた teamId は全滅**。チーム 280→277、ページ 328→325
- [x] **C-3. 設置者接頭辞つき／なしの未統合ペア** → **2026-08-12 統合済み（4件）**
  - `羽須美中学校`←`邑南町立羽須美中学校`（選手5人共通）/ `名寄中学校`←`名寄市立名寄中学校` /
    `中広中学校`←`広島市立中広中学校` / `連島中学校`←`倉敷市立連島中学校`（選手4人共通）
  - **判定はすべて選手名の突き合わせ**。パターンは共通で、2026年度のブロック大会だけが
    設置者接頭辞つきの正式名称、それ以前は略称という出典差
  - canonical は**略称側**を採った。高校も `上田染谷丘`（長野県立…）のように設置者を落としており、
    URLも短くなる。設置者接頭辞つきの表記は alias として残る
  - 索引に残る設置者接頭辞つき14件は**略称版が存在しない**ので統合不要
  - 20文字超のIDは5件のみ（`北海道教育大学附属旭川中学校` など、名前自体が長いもの）
- [ ] **C-4. `函館市拠点校部活動（巴中学校）` の表記**
  - 括弧内が実際の学校名で外側が制度名。他の括弧書きのように機械的に落とせない。
    大会要項の確認が要る。現状2件で掲載閾値未満のためページは無い
- [x] **C-5. `四天王寺高校` が中学データに混入** → **2026-08-12 修正済み**
  - 当初「高校名の団体が中学データに混入」と報告したが**不正確だった**。四天王寺は中高一貫校で、
    `四天王寺高校` はインターハイ・ハイジャパ等に45件出ている**正規の高校チーム**
  - 実際の問題は「**全中2022の所属が誤って高校名で記録されていた**」1件（房野紗千・市川こいと）
  - `四天王寺中学校` へ寄せる alias を **中学校の大会・大阪府に限定**して追加。
    高校の大会に出ている `四天王寺高校` は無傷（45件のまま）
  - 中学側は2件で掲載閾値未満のためページは生成されない
- [x] **C-6. 高校の地区大会の4種は「中学校の混入」ではなく〈高校名の誤記〉だった** → **2026-08-13 修正済み**
  - **当初「中学校が高校大会に混入」と書いたのは誤り**（C-8 の検証中に判明）。
    選手名を他大会と突き合わせると、**同じ選手が他では高校に所属している**:
    - `日高中学校` の山下結萌・山岡美愛 → 他で **`日高`**（和歌山県立日高高校）
    - `新宮中学校` の村上恵理 → 他で **`新宮高校`**
    - `柏崎ジュニア` の飯田愛菜 → 他で **`柏崎`**
    - `土佐中学校` は**選手4名が姓のみで裏付けが取れなかった**が、同ファイルの高知県の
      他チームが全て高校であること・中高一貫校であること・他3件が誤記だったことから
      ユーザー判断で `土佐` として適用
  - 修正は [ADR-013](../adr/ADR-013-scoped-team-name-aliases.md) の scope つき alias。
    **`tournamentPrefix`（当該ブロック大会）× `prefecture`** で限定。
    canonical は既存の高校側の表記に合わせた（`日高` `柏崎` `土佐` は素の名前、`新宮高校` は
    全日本シングルスでの表記。大会ごとに流儀が違うので既存に寄せる）
  - **落とし穴: 逆向きの alias と往復して収束しなくなった。**
    `土佐中学校 ← 土佐` が**スコープ無し**だったため、
    高校ブロックで `土佐中学校`→`土佐` に直した直後に `土佐`→`土佐中学校` へ戻され、
    `normalize-team-names.mjs` が**何度流しても毎回12箇所を置換し続ける**状態になった。
    `土佐中学校` `新宮中学校` の側に `generation: junior` を足して解消
    （`日高中学校` `柏崎ジュニア` には既に付いていた）
  - **教訓**: 素の略称を中学校へ寄せる alias は**必ず scope を付ける**。
    付いていないものが1つでもあると、逆向きの修正を入れたときに静かに振動する
  - 結果: 高校の大会に残る中学校名・ジュニア名は **0件**。
    `日高`/`日高中学校`/`日高ジュニアクラブ`、`新宮高校`/`新宮中学校` が世代ごとに正しく分離
- [ ] **C-9. `normalize-team-names.mjs` は1回の実行では収束しない**
  - alias に連鎖があるため（`上宮学園中` → `上宮学園` → `上宮学園中学校`）、
    1回流すと**半分だけ正規化された状態**になる。実測で収束に3回かかった
  - canonical でも alias でもある名前が **17件**あるのが原因
  - 当面の運用は「**変化が0になるまで繰り返し流す**」。
    スクリプト側を不動点まで回すループにするか、連鎖を平坦化するかは未判断

- [x] **C-8. 高校パイプラインに中学・小学のデータが混入していた** → **2026-08-13 修正済み**
  - 発覚のきっかけ: C-5 で `四天王寺高校`→`四天王寺中学校` に直したあと
    `npm run highschool:pipeline` を回すと、大阪の高校 summary に中学校として現れた
  - **私の変更が原因ではなく、以前からある構造的な漏れ**。高校 summary 8,092件のうち
    **242件（55校）が中学・小学の大会由来**だった（`primaryschool-championship` まで含む）
  - 経路は2つあり、両方直した:
    1. **層1**: `03list/summary.py` の `normalize_school_name()` が「高校」と一緒に
       **「中学校」「中等教育学校」も落として**から高校名として照合していた。
       `四天王寺中学校`→`四天王寺` で高校に一致する。ADR-013 で JS 側に入れた
       「中高一貫校は世代で分ける」対策が、この Python パイプラインだけ抜けていた
    2. **層2**: `02result/extract.py` が `index.json` の全大会を対象にしていた。
       `栃木` `砺波` `豊浦` のように**接尾辞なしで高校名と完全一致する**中学校名が39種あり、
       層1だけでは消えないため `EXCLUDED_TOURNAMENT_IDS` で中学・小学専用の大会14件を除外
  - **全日本選手権・全日本シングルス・東西日本は除外していない**。高体連の高校生が正当に出ており、
    「この高校の選手が全日本でベスト8」は高校ページに出すべき情報。
    `zennihon-junior` も残した（u17/u20 は高校生。u14 で高校名に一致するのは**実測0件**）
  - 検証（リポジトリを汚さないよう `/tmp` のコピーでパイプラインを実行）:
    **総エントリ 8,092 → 7,773（−319）／ 中学小学由来 242 → 0 ／ 増えた行 0**
    - 内訳: 中学小学専用大会 242件 ＋ **全日本系に出ていた中学生 77件**
      （うち75件は playerIds が `◯◯中学校`。`昭和学院中学校` が全日本選手権で
      `昭和学院`（高校）に計上されていた等）
    - 残り2件は C-6 由来（上記）
  - **要実行**: `npm run highschool:pipeline` をユーザー環境で回すこと。
    `os.listdir` の順序が macOS と Linux で違うため、サンドボックスの生成物は反映していない

- [ ] **C-7. 選手名の表記ゆれ（同じ姓・同じ所属で名が前方一致）27件**
  - 発見の経緯: `水木洸太`(全中2022の1件) / `水木洸`(他19件) が同一人物なのに氏名が割れ、
    進路の追跡から漏れていた。**`洸` が正**とユーザー確認 → 2026-08-13 修正済み
    （`id` / `firstName` / `entries[].playerIds` の3箇所を直接置換。整形を保つためピンポイント置換）
  - 修正の副次効果: **ペア継続の検出が 96→98 に増えた**（水木・松田ペアが東北高校でも一致）
  - 全データを走査して同型を27件検出。一覧と分類は
    [2026-08-13-player-name-variants-review.md](./2026-08-13-player-name-variants-review.md)
  - **機械的に判断できる6件も 2026-08-13 修正済み**（異体字セレクタ1・所属名の混入2・
    世代マーカー`小/中/高`の混入3）。**27件 → 21件**
  - 残り21件は `水木` と同型で**どちらが正かは出典を当たるしかない**。
    多数決も長短も決め手にならない（水木は**長いほう**が誤りだった）
  - **別件も解決**: 同一ファイル内の `id` 重複2件（`zennihon-championship` の 2022・2023）は、
    **リタイア＋選手変更で同じ選手が2エントリーに出た**のが原因とユーザーが特定
    （entryNo 58 / 24 がリタイア側。`matches` に `retired: true` あり）。
    同姓同名ではなく同一人物なので、`participants` の重複行を畳んで解消。
    `playerIds` は id 文字列・`matches`/`results` は entryNo 参照で添字依存が無いため安全。
    全ファイルで id重複0・参照切れ0 を確認
  - 中学カテゴリ固有の問題ではなくサイト全体の話。公開のブロッカーではない

- [x] **C-10. 「その他のチーム」の正体を調べて2つ直した**（2026-08-13・ユーザーの疑問から）
  - きっかけは「その他のチーム」とはどういうグループか、という質問。
    調べると `unknown` 63件は**性質の違う3種類の寄せ集め**だった
  - **(a) 県名そのもの 8件 → 索引から除外（修正済み）**。
    `群馬`(32件)・`茨城`/`埼玉`/`山梨`/`神奈川`/`千葉`/`東京`/`栃木`(各16件)。
    出典はすべて `secondaryschool-kanto-block` の**個人戦**で、`participants[].team` に
    学校名ではなく都県名が入っている（同じ大会の団体戦は `野木中学校` と正しい）。
    **学校名は復元できない**: 選手名で他大会と照合できるのは144名中41名（28%）で、
    しかも `皆野ジュニア` `箕郷スポ少` のような**小学生時代のクラブ**に当たる。
    判定は「チーム名 === その参加者の都道府県の短縮名」と狭くしてあり、
    `群馬中央中学校` のような実在校は残る。**出典を当たり直せば復活できる**
  - **(b) 同じデータ内に「◯◯中学校」が別にある 16件** → 名寄せの取りこぼし。未対応
  - **(c) 名前だけでは判定できない 39件** → `男塾`(福岡26)・`旭川啓北`(北海道9) など。
    設計どおりの「わからない」。実在確認が要る
- [x] **C-11. 県ページの 中学校/地域クラブ/その他 の3見出しをやめた**（2026-08-13・ユーザー判断）
  - 「そもそもクラブと中学を分けなくてもいいのでは」という問いから、
    クラブ判定を使っている機能を全部たどった。**2系統しかない**:
    - **地域移行トラッカー**（全中ハブの1節）… 分類そのものが主題。**残す**
    - **中学カテゴリの表示4箇所**… いずれも分類が主題ではない
  - やめたのは「分類を**構造**として使っている2箇所」だけ:
    県ページの3見出し → 1リスト（出場回数順）＋小さなラベル、
    入口ページの `中学校130・地域クラブ116` → `収録293チーム` のみ
  - チームページと進路一覧の**ラベル用途は残した**（分かる時だけ添える形なので問題ない）
  - `SecondarySchoolPrefecture` から `schoolCount` `clubCount` を削除
  - **地域移行トラッカーと `npm run club:verify` は無傷**

## D. 公開後

- [ ] **D-1. GSC でインデックス状況を確認**
- [ ] **D-2. 効果測定**（`{中学校名} ソフトテニス` `{県名} 中学 ソフトテニス` を拾えているか）

---

## 完了済み（参考・2026-08-12）

- 中学カテゴリの是非を実データで検討 → フル横展開を選択
- 名寄せ（[ADR-013](../adr/ADR-013-scoped-team-name-aliases.md) で alias に大会スコープを導入）。
  ヘルスチェックの要対応シグナル 72→40、別名残存 4→0
- 地域移行トラッカーを全中の大会ハブに実装（`npm run club:verify`）
- `/secondaryschool` の3階層（328ページ）と進路（184件/83チーム）を実装
- teamId の短縮（平均10文字）・長音を `ou` に統一・括弧書き8種の整理
