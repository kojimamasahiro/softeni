# アイデア: 中学カテゴリの公開ページ（/highschool 型の横展開）

## 状況

Idea Backlog。**方針決定済み・実装前**（2026-08-12）。ユーザーが実データ集計を踏まえたうえで
「高校型フル横展開」を選択。ただし**無条件の全校ページ化ではなく、掲載閾値と名寄せ層を前提とする**
条件付きの採用（後述「採用にあたっての前提条件」）。

なお本アイデアは、2026-08-08 の
[全中ブロック大会の掲載](./2026-08-08-idea-zenchu-block-tournament-data.md)で合意した
「中学専用カテゴリは作らず通常の大会として登録する」という**当時の判断を上書きする**。
当時はブロック大会1件のためだけに新カテゴリを作るのがスコープ過大という理由だったが、
その後9ブロック分の実データが投入され（結果行1,064、校/団体534）、土台が変わった。

## 目的

高校カテゴリでは `/highschool/` 配下の都道府県ページ・学校ページ・強豪校ランキング・歴代大会ページが
「インターハイ」「{県名} 高校 ソフトテニス」「{学校名} ソフトテニス」系のロングテールを面で拾えている
（`seo.md` #3・#7・#10・#11）。同じ型を中学カテゴリへ横展開し、「全中」「全国中学校ソフトテニス大会」
「{県名} 中学 ソフトテニス」「{中学校名} ソフトテニス」を受けたい。

## ユーザーが興味を持った点

- 「高校のページのように中学を作成した方がいいか検討したい。高校ではSEOでインターハイなどのワードを
  拾えるようになった。また、大会結果も充実していると思える」（2026-08-12）
- 同時に懸念も自ら提起: 「中学はクラブなどもあり安定していなかったり、大会結果がまだ不十分であったり
  都道府県ごとのまとまりがうまくいかないかもしれない」
- 実データ集計（下記）で「フル横展開は非推奨・ランキング型が現実的」と提示したうえで、
  **ユーザーは「高校型フル横展開」を選択**。したがって本アイデアは「やるかどうか」ではなく
  「どう薄さを回避して実装するか」のフェーズにある。

## わかっていること（2026-08-12 実データ集計）

対象は中学の収録12大会（全中 `secondaryschool-championship` 4年分、9ブロック大会 2026年度、
都道府県対抗全日本中学生大会 2年分、全日本中学生クラブ選手権プレ 1年分）。
高校は3大会（IH 6年分、ハイジャパ 5年分、選抜 6年分）。

| 指標 | 中学 | 高校 |
|---|---|---|
| 結果行（延べ） | 2,758 | 6,160 |
| 校/団体名（生の表記） | 1,049 | 553 |
| 出場延べの中央値 | **2** | 6 |
| p25 / p75 | 2 / 5 | 2 / 21 |
| 出場5未満の割合 | **72.3%** | 47.2% |
| 出場15未満の割合 | 94.3% | 69.1% |
| 都道府県判明率 | 100% | 100% |
| 同名で複数県にまたがる率 | 0.6% | 0.5% |
| 県あたり校数の中央値 | 20 | 10 |

**要点: ページ候補は約1.9倍、データ量は0.45倍。1校あたりの厚みが約1/3。**
無条件に高校型3階層を作ると7割以上が `/teams/[teamId]` の noindex 閾値（5件）未満になる。
これは 2026-08-11 の
[成年カテゴリ検討](./2026-08-11-idea-general-category-prefecture-pages.md)で
「57.3%が5試合未満だから高校型フル移植は避けるべき」と結論した数字より悪い。

### ユーザーの懸念3点の検証結果

1. **「都道府県ごとのまとまり」→ 懸念は当たらない（想定外に良好）**。
   都道府県判明率100%、47都道府県すべてにデータあり、同名で複数県にまたがるのは0.6%（高校0.5%と同水準）。
   `team-player-identity.md` の「学校は県が安定する」が中学でも成立している。
   都道府県軸のページ化を妨げる要因は無い。

2. **「クラブなどもあり安定しない」→ 実在するが致命傷ではない、ただし構造的に増える**。
   名前に「中学校/中/中等」を含む率は大会ごとに大きく違う:

   | 大会 | 延べ | 校/団体 | 「中学校」名の率 |
   |---|---|---|---|
   | 全中 | 1,074 | 282 | 49.9% |
   | ブロック大会（9地区） | 1,836 | 534 | 17.4% |
   | 都道府県対抗全日本中学生 | 1,535 | 543 | 16.9% |
   | 全日本中学生クラブ選手権プレ | 172 | 70 | **0.0%** |

   例: `静内クラブ` `苫小牧光洋Wings` `iNexus` `veeoma.hc` `浜頓別ソフトテニス少年団` `筑西STC`。
   出場5回以上に絞ってもクラブ系と見られる名前が95件ある（`鉢盛クラブ` `能美Jr.STARS`
   `奈良LEGENDS` `新潟UCHINO` `GifuTed` 等）。
   **2025年度から地域クラブ活動所属の生徒の全中参加が正式に認められた**ため、この比率は今後増える。
   「学校」という単位を前提にした高校型の設計をそのまま持ち込めない構造的理由がここにある。

3. **「大会結果が不十分」→ 半分当たり。ただし真のボトルネックは別にある = 表記ゆれ**。
   全大会を正規化（括弧内除去・「◯◯県立」等の設置者接頭辞除去・「中学校/中/中等教育学校」語尾除去）した結果:

   - 正規化後キー 957 に対し、**同一県内で複数の表記を持つキーが95件（9.9%）**
   - 同じ処理を高校に当てると **0.5%（556中3件）**。**中学は約20倍**
   - 例: `昇陽` / `昇陽中学校`、`名寄` / `名寄市立名寄中学校`、`羽須美` / `邑南町立羽須美中学校`、
     `早来` / `早来学園`、`上宮` / `上宮学園`、`奈良育英` / `奈良育英中`
   - 原因は出典差。**全中の公式結果は略称表記、ブロック大会のPDFは正式名称表記**。
     高校は出典がどれも正式名称で揃っているため問題化していない。

   名寄せ層を作らないまま学校ページを生成すると、同一校が2ページに分裂し、
   内部リンクも被リンクも成績集計も割れる。**これがフル横展開の最大の技術的前提条件。**

### 掲載閾値ごとのページ規模（名寄せ後）

名寄せ後のユニーク校/団体は 959。閾値別の規模は以下。
「学校ページ実数」は男女別ページを別URLとした場合の概算。

| 掲載閾値（出場延べ） | 対象校 | 学校ページ実数 | 県あたり中央値 | 0件の県 |
|---|---|---|---|---|
| 制限なし | 959 | ~1,156 | 19 | 0 |
| 3以上 | 463 | ~654 | 9 | 0 |
| **5以上** | **283** | **~435** | **5** | **0** |
| 10以上 | 119 | ~198 | 3 | 2 |
| 15以上 | 63 | ~109 | 2 | 12 |

**閾値5以上が最有力**。283校/435ページは高校（553校）と同オーダーで、
47都道府県すべてに掲載校が残るため都道府県ページが空にならない。
`/teams/[teamId]` の既存 noindex 閾値（5件）とも整合する。

### 外部環境: 全中は2027年度から30%規模縮小（ソフトテニスは残留）

日本中体連は2027年度から全中を19競技→11競技に削減し、参加者数・開催経費とも30%減を目標としている。
**ソフトテニスは削減対象外で存続**するが、規模は縮小方向。器が消えるリスクは低いが、
年あたりの結果行が今後増える見込みは薄いため、**データの厚みは過去年度の遡及投入で稼ぐしかない**。

## 採用にあたっての前提条件（これを満たさずに着手すると薄いページ量産になる）

1. ~~**中学校名の名寄せ層を先に作る**~~ → **2026-08-12 実施済み**（下記「名寄せを実施」の節）。
   既存パイプラインを使い、alias 表に大会スコープを足して解消した（[ADR-013](../adr/ADR-013-scoped-team-name-aliases.md)）。
   以下は着手前の記述:
   表記ゆれ9.9%を解消しないと学校ページが分裂する。
   高校の `data/highschool/prefectures/*/summary.json` と `getSchoolResolver()` に相当する
   「正規化キー →（正式名, 県, id）」のマスタが要る。95件は手作業でも潰せる規模。
2. **掲載閾値を設ける**（推奨5以上）。閾値未満の校は都道府県ページに名前だけ出し、
   個別ページを作らない（`players/index.tsx` の count>=5 規約と同型）。
3. **「学校」ではなく「チーム」として設計する**。クラブ・スポ少・地域クラブが今後増えるため、
   URL・見出し・description に「高校」「学校」相当の語をハードコードしない。
4. **高校カテゴリのロジックを共有せず複製する**。高校側は地区大会を
   ランキング・卒業生集計・都道府県ページに統合しない方針を維持しており
   （[ページ構成決定](./2026-07-22-highschool-block-tournament-page-structure.md)）、
   中学データが高校の集計に混入しないことをallowlistで担保する。

## 追記（2026-08-12・「高校とは違う形」の候補を実データで検証）

「高校とはまた違ったページにするとしたらどういったことができるか」という問いを受け、
中学データが高校データと**構造的にどう違うか**を実測した。結論から言うと、
**中学は高校と『主役の単位』が違う**。高校の設計をそのまま持ち込む必然性は最初から無かった。

### 起点になった発見: ダブルスペアの所属一致率

| 大会 | ダブルスペア数 | 所属が異なるペア |
|---|---|---|
| 高校インターハイ | 3,792 | **0.00%** |
| 全中 | 512 | **0.00%** |
| ブロック大会（9地区） | 772 | **18.65%** |
| 都道府県対抗全日本中学生 | 576 | **38.89%** |

高校が「学校を主役にする」設計で正しかったのは、混成ペアが0.00%だからだった
（`sns-story-platform.md`「◯◯高が3連覇と書ける」の根拠）。中学でこれが成立するのは**全中だけ**で、
ブロック大会と県対抗では学校が単位として壊れる。県対抗は県選抜なので構造上当然だが、
ブロック大会の18.65%は地域クラブ・スポ少混在の影響と見られる（未検証）。

### 候補1: 進路の接続（中学→高校）— 最有力

氏名照合で**中学と高校の両方に出現する選手が547人**（中学収録2,956人の18.5%）。

- 中学最終出現年→高校初出現年の差: 1年261件 / 2年224件 / 3年58件 / 4年4件。進学として自然な分布
- **県が一致する（信頼度が高い）のは301件（55.0%）**。残り246件は越境進学と誤マッチの混在
- **同姓同名リスクは実測でほぼゼロ**: 中学側で「同一年に3チーム以上へ出現する氏名」= 確実に別人が
  混ざっている証拠となるケースは **0件 / 2,956人（0.00%）**。中学の氏名空間はきれいで、
  `2026-08-01-idea-news-fact-based-story-categories.md` の「名寄せは氏名のみが正解」と整合する
- 例: `名寄`→`旭川東`、`羽須美`→`松江工`、`就実`(中)→`就実`(高)
- **さらに強い証拠として、中学で組んだペアがそのまま高校でも同じペアという例が48組**。
  `井手遥菜・横山葵: 奈良育英 → 奈良育英`、`竹田羽花・野間結菜: 昇陽 → 昇陽`、
  `中村悠人・福田喜大: 上宮学園 → 上宮` のように**系列校（中高一貫）への内部進学**が目立つ一方、
  `佐々木南帆・義達心陽: 緑陽 → とわの森三愛`、`櫻井亜朱・藤原妃花: 下吉田 → 笛吹` のような
  外部進学もある。ペア単位の一致は誤マッチ率が実質ゼロという既存実測
  （`news-context-blocks.md`、IH2026で名前セット重複0件）があるため、**最も安全な採用単位**

**なぜこれが差別化になるか**: 中学と高校の両方を名寄せ済みで持っているサイトにしか作れない。
検索意図も高校と別物で、読者は「これから進路を選ぶ中学生と保護者」。
さらに**既存の高校ページも強くなる**（「この高校はどの中学から選手が来ているか」の逆引きを
高校の学校ページに追加できる）ため、新規カテゴリの投資が既存資産に還流する。

### 候補2: 都道府県対抗を県ページの主役にする

県対抗は2024・2025とも**47都道府県が完全に揃い、各県ダブルス6ペア＋シングルス2名で固定**
（男女それぞれ doubles 288名・144結果、singles 96名・96結果）。結果ラベルも
優勝1/準優勝1/ベスト4 2/ベスト8 4/5回戦敗退8/… と欠損なく揃う。
**サイト内で最も規則正しいデータセット。**

高校の都道府県ページは強豪県と過疎県で厚みの差が大きく、強豪校ランキングも出場機会の偏りを含む。
対して県対抗は**全県が同じ枠数で同じ土俵に立つ唯一の大会**なので、
出場数バイアスなしに「県の強さ」を比較できる。これは高校側では原理的に作れないページ。

### 候補3: 部活動の地域移行トラッカー

全中に出場する「学校名でない団体」の推移（名前の正規表現による概算）:

| 年度 | 全団体 | クラブ的な名前 | 比率 |
|---|---|---|---|
| 2022 | 98 | 1 | 1.0% |
| 2023 | 104 | 8 | 7.7% |
| 2024 | 99 | 15 | 15.2% |
| 2025 | 106 | **35** | **33.0%** |

3年で1%→33%。2025年度から地域クラブ活動所属生徒の全中参加が正式解禁された影響が数字に出ている。
例: `三重高クラブ` `MASTER` `新潟UCHINO` `GifuTed` `奈良LEGENDS` `王寺ユースクラブ` `国分寺JSC`。

**報道はされているが競技単位で数字にしている先行例が見当たらない領域**で、
「部活動 地域移行」「地域クラブ活動」という、ソフトテニス系サイトが誰も取りに行っていない
キーワード群に届く。毎年更新される定点観測なので記事が古びないのも利点。

### 候補4: 選手中心の設計 → **単独では成立しないと判明**

当初「中学生は卒業後も高校でサイトに出続けるので、選手を主役にする方が構造に合う」と考えたが、
実測で否定された。**中学収録データのみで出場5回以上に達する選手は 4人 / 2,956人（0.1%）、
中央値1回、最大5回**。中学は在籍3年＋収録年数が浅い（全中4年・ブロック1年）ため、
選手ページの既存基準（`count>=5`）を中学単独では満たせない。

ただしこれは**候補1を否定するものではなく、むしろ補強する**。読み替えると
「中学データは単独で選手ページを作る材料にはならないが、**既存の選手ページ（高校以降で厚みがある選手）に
中学時代の経歴を足す材料としては効く**」。547件の追跡可能選手がまさにその対象になる。

### この追記から見えた実装順序への示唆

候補1が本命で、候補4はその一部として吸収される。候補2は候補1・3の受け皿として
県ページを作るときに効く。候補3は独立して先行実装できる（既存ページへの追加で済み、
新規URLも1枚で足りる可能性が高い）。

なお本追記の集計中に**データ品質の問題を1件発見**: 中学の大会データに `四天王寺高校` という
高校名の団体が混入している（`市川こいと・房野紗千` のペアが中学側で `四天王寺高校` 所属）。
中高一貫校の表記が高校名に寄っている可能性が高く、名寄せ層を作る際に要確認。

## 追記（2026-08-12・候補3「地域移行トラッカー」を実装）

ユーザーの指示で候補3を先行実装した。置き場所は**全中の大会ハブに節を追加**（新規URLなし、
ADR-010の「ハブに集約」方針と整合）、集計対象は**全中のみ**（2026-08-12 ユーザー決定）。

### 実装

- `lib/clubTransition.ts`（新規）— 分類器 `classifyTeamAffiliation` と年度別集計 `getClubTransition`。
  対象大会は allowlist（`secondaryschool-championship` のみ）。年度が2つ未満なら null を返す
- `src/components/Tournament/ClubTransitionSection.tsx`（新規）— 表示。棒はCSSの矩形（絵文字禁止ルール準拠）
- `src/pages/tournaments/[generation]/[tournamentId]/index.tsx` — `clubTransition` prop を追加し節を差し込み
- `scripts/verify-club-transition.ts` + `npm run club:verify`（新規）— 分類の固定テストと全件目視出力

### 分類方針を「下限カウント」に変えた経緯（重要）

当初の集計（本ファイル上部の候補3の表）は正規表現による概算で、2022年度98団体中74件が
判定不能だった。この状態で「中学校を含まない＝クラブ」と推定すると**トレンドが表記ゆれの
アーティファクトになる**ことに実装中に気づいた。2022年度の出典が略称表記のみだったためで、
実際には `名寄`（名寄市立名寄中学校）`湊山`（米子市立湊山中学校）はいずれも学校である。

そこで**クラブと断定できる積極的な証拠がある場合のみ club とする下限カウント**に変更し、
判定できないものは `unknown` として学校にもクラブにも数えないことにした。
さらに検算をもとに規則を2度改良した:

1. 末尾 `中` を学校マーカーに追加（`人吉第一中` `砧南中` など38件）。
   全データで末尾 `中` の名前にカタカナ・ラテン文字の混入は0件と確認
2. ラテン判定を区切り記号除去後に行うよう変更（`M's` `N.N` を取りこぼしていた）
3. カタカナ3連を club に追加（`スマイリー` `レペゼン千葉` `ネクサス兵庫` `レグルス鹿島`）。
   学校マーカー持ちでカタカナ3連を含むのは `苫小牧市立ウトナイ中学校` の1件のみで、
   学校マーカーが先に効くため誤判定にならないことを確認

**確定値**（改良後）:

| 年度 | 出場団体 | 地域クラブ（下限） | 比率 | 判定不能 |
|---|---|---|---|---|
| 2022 | 98 | 1 | 1.0% | 80 |
| 2023 | 104 | 10 | 9.6% | 22 |
| 2024 | 99 | 21 | 21.2% | 11 |
| 2025 | 106 | 39 | 36.8% | 21 |

制度（2023年度から特例）とデータの変わり目が一致することを、検証スクリプトの
アサーション（「制度変更前の最大クラブ比率 < 変更後の最小クラブ比率」「比率は単調増加」）
として固定してある。データが増えてこれが崩れたら気づける。

### 検証結果

- `npm run club:verify`: 固定テスト38件すべて通過、単調増加・制度境界のアサーションも通過
- `npx tsc --noEmit`: エラー0
- `eslint`: エラー0（警告はimport resolverのネイティブバインディング読み込み失敗のみ＝環境要因）
- コンポーネントを実データで `renderToStaticMarkup` して描画確認、props が JSON シリアライズ可能なこと、
  絵文字が含まれないことを確認
- allowlist の効きを確認（高校IH・全日本・県対抗・ブロック・全小はいずれも null ＝節が出ない）
- **未実施: `npm run build` の完走**。この作業環境ではビルドが時間制限内に終わらなかった。
  型・lint・描画は個別に通っているが、本番ビルドでの目視確認は残タスク

### 判明したデータ品質の問題

中学の大会データに `四天王寺高校` という高校名の団体が混入している（2022年度・大阪府）。
中高一貫校の表記が高校名に寄ったものと見られる。学校であることは確かなのでクラブ側に
倒さないよう `高校` を学校マーカーに含めて回避したが、**名寄せ層を作る際に要修正**。

## 追記（2026-08-12・名寄せを実施。前提条件(a)は解消）

「採用にあたっての前提条件」の (a) 名寄せ層の構築に着手した。結論から言うと
**新しい層を作る必要はなかった**。既存の名寄せパイプライン
（`team-name-aliases.json` ＋ `build-team-merge-candidates.mjs` ＋ レビューHTML ＋
`normalize-team-names.mjs`、運用は `team-player-identity.md`）がそのまま使えた。
ただし**そのまま流すと壊れる**ことが判明し、alias 表に大会スコープを足す設計変更が必要になった
（[ADR-013](../adr/ADR-013-scoped-team-name-aliases.md)）。

### 判明した3分類（当初「95件の表記ゆれ」と見ていたものの内訳）

中学大会に出るチームでコア一致するグループは**213件**あり、単純マージしてよいのは一部だけだった。

| 種別 | 件数 | 中身 | 対応 |
|---|---|---|---|
| A1 単純な表記ゆれ | 170 | `ＢＬＡＳＴ`/`BLAST`、`西郷第一`/`西郷第一中学校` | マージ |
| A2 学校と同地名クラブ | 19 | `今市ジュニア`/`今市中学校`、`野木クラブ`/`野木中学校` | **マージしない** |
| B 中高一貫 | 24 | `昇陽`（高108/中41）と `昇陽中学校`（中43） | **世代で分ける** |

**A2 は地域移行トラッカー（候補3）の前提そのもの**。学校とは別に存在する地域クラブを
地名が同じというだけで統合すると、自分で作った機能の土台を壊す。

**B はマージではなく分割の問題**だった。素の `昇陽` は既に高校108件＋中学41件を含む融合状態で、
「マージするな」では足りない。該当は `昇陽` `京都光華` `就実` `明徳義塾` `明豊` `昭和学院` `札幌大谷` の7校。

### `--scope=all` がそのままでは流せなかった

dry-run で 525箇所の置換が出たが、内訳を名前レベルで見ると**高校データを壊す置換が混ざっていた**:

- `日高 → 日高中学校`（インターハイ・和歌山県2件＝和歌山県立日高高校）
- `柏崎 → 柏崎ジュニア`（インターハイ・新潟県2件＝柏崎高校）

同型の危険を全件洗い出すと**17件**。alias 表が文脈を持たないのが原因で、
これが ADR-013（`scope` の導入）の直接の動機になった。

### 中高一貫の分割で気づいた落とし穴: `zennihon-junior`

「generationId が junior なら中学校」という規則では**不十分**だった。
7校は `zennihon-junior`（全日本ジュニア選手権）にも出場するが、実測すると
**u17 / u20 のみで u14 は0件**、つまり高校生を指している。

- 中学校の大会（全中・ブロック・県対抗）: **107件** → 中学校へ寄せる
- 全日本ジュニア u17/u20: **36件** → 高校のまま維持

このため `scope` は `generation` だけでなく `tournamentPrefix` も持つ設計にした。

### 自動マージ（`apply-auto-merges.mjs`）が作った3種類の誤り

`--dry-run` が無く実行＝即反映なので注意。出力を検証して以下を修正した:

1. **canonical が略称になる（77件）**。`増田 ← 増田中学校` のように最頻出表記が選ばれる。
   ユーザー決定（canonical＝正式名称）に従い全件反転した
2. **A2 の誤マージ（1件）**。`野木中学校 ← 野木STC` は地域クラブを学校へ統合していた。巻き戻し
3. **意図的に統合しないと決めていたものの統合（1件）**。`府中STC ← 府中クラブ` は
   `team-player-identity.md` に「2対2の同数で別団体の可能性が残るため意図的に統合していない」と
   明記されているものだった。巻き戻し

さらに反転した77件を再検査し、素の略称が中学以外の大会にも出る**17件に `generation: ["junior"]` を付与**した。

### 実施結果

| 指標 | 前 | 後 |
|---|---|---|
| ヘルスチェック 要対応シグナル合計 | 72 | **42** |
| 別名のまま残る出場 | 4 | **0** |
| 自動OK可の未統合候補 | 29 | **3** |
| 全中の団体分類「判定不能」2022/2023/2024/2025 | 80/22/11/21 | **57/8/0/2** |
| alias 表の canonical 数 | 598 | 632（うち scope 付き27） |

置換は最終的に **58ファイル / 1,575箇所**。2回目の実行が0件で冪等性も確認済み。
**地域移行トラッカーのクラブ数（1/10/21/39）は前後で変化なし**で、名寄せが
トラッカーの結論を動かしていないことも確認した（判定不能が減った分は学校側に寄った）。

### 副産物: 高校の地区大会データに中学校名が混入している（未修正）

高校の大会に中学校名・クラブ名が入っている箇所を4種見つけた。
**いずれも今回の書き換えではなく元データの時点でこの表記**であることを `git show HEAD:` で確認済み。

- `highschool-kinki-block/2026`: `日高中学校`（団体戦・個人戦、計6件）
- `highschool-shikoku-block/2026`: `土佐中学校`（4件）
- `highschool-kyushu-block/2026`: `新宮中学校`（2件）
- `highschool-hokushinetsu-block/2026`: `柏崎ジュニア`（2件）

要項PDFの転記時の誤りか、中高一貫校の表記が中学側に寄ったものと思われる。
出典を当たらないと直せないため今回は触っていない。

## 追記（2026-08-12・名寄せ後にページ規模を再測定。ブロック大会の扱いが決着）

名寄せが済んだので「掲載閾値ごとのページ規模」を測り直した。測定は
`node scripts/measure-juniorhigh-scale.mjs`（**今回からスクリプトを保存した**。
以前の集計はワンショットで再現性が無かった）。

**上部の「掲載閾値ごとのページ規模」の表は名寄せ前の概算。正はこちら。**

### ブロック大会を含めるかで結論が変わる

| 閾値5のとき | A) ブロックなし | B) ブロック込み | 参考: 高校3大会 |
|---|---|---|---|
| 対象団体 | 158 | **283** | 289 |
| ページ数（男女別URL想定） | 238 | **432** | 409 |
| 県あたり中央値 | 3 | **5** | 6 |
| 0件の県 | 0 | 0 | 0 |
| 最も薄い県 | 京都1・宮崎1・山梨1・沖縄1 | 大阪2・大分2 | 宮城3・奈良3 |

**B（ブロック込み）が高校とほぼ同規模になる**（283対289団体、432対409ページ）。
A では県あたり中央値3で、京都・宮崎・山梨・沖縄が1団体しか残らず**都道府県ページが成立しない**。
したがって**ブロック大会は含める**。

「2026年度1年分のブロックだけで水増しされるのでは」という懸念は実測で否定された:

- 閾値5を満たす283団体のうち **ブロックのみ48件（17%）/ 全国のみ58件（20%）/ 両方177件（63%）**
- 収録年数の分布は 1年58 / 2年79 / 3年108 / 4年25 / 5年13。**1年しか無いのは20%**

つまりブロック大会は主に「既に全国大会に出ている団体を厚くする」方向に効いている。

### ただしブロック大会は集計に使えない（構造的な偏り）

ブロック大会に出場した団体数を県別に数えると、**北海道65 に対し福岡4**（中央値10）。

- 多い順: 北海道65 / 群馬24 / 山梨16 / 静岡16 / 千葉15
- 少ない順: 福岡4 / 奈良6 / 鳥取6 / 佐賀6 / 山形7

これは実力差ではなく**ブロック構成の差**。北海道は単独で1ブロックなので予選通過団体が全部出るが、
福岡は九州7県で1ブロックを分け合う。16倍の差は大会構造のアーティファクトでしかない。

**結論: ブロック大会は「ページを作るかどうか（掲載閾値）」には使い、
「ランキング・県別比較」には使わない。**

これは高校側の「地区大会をカテゴリ機能に統合しない」決定
（[ページ構成決定](./2026-07-22-highschool-block-tournament-page-structure.md)、理由は
「ブロックごとのPDF入手状況のばらつきが全国横断集計を歪める」）**と矛盾しない**。
むしろ同じ原理をより細かく適用したもので、高校は「集計にも掲載判定にも使わない」、
中学は「集計には使わないが掲載判定には使う」。高校は6年×3大会あるので掲載判定に地区大会が要らないが、
中学は全中4年しか無く、地区大会を外すと団体数が55%に落ちるという差による。

### クラブが約4割 —「学校ページ」とは呼べないことが確定

閾値5・ブロック込みの283団体の内訳は **学校117 / クラブ116 / 判定不能50**。
判定不能がすべて学校だとしても**クラブは41%**。
前提条件(c)「『学校』ではなく『チーム』として設計する」は、これで推測ではなく実測に裏付けられた。

参考に同じ物差しを高校に当てると 学校22 / クラブ2 / 判定不能265 で、
判定不能の大半は「中学校」等の接尾辞を持たない高校名（`三重` `東北` 等）。
**高校にはクラブが実質存在しない**のと対照的。

### この測定で確定したこと

1. 掲載閾値は **5**（`/teams/[teamId]` の noindex 閾値と整合、47県すべて非空）
2. 対象大会は **全中＋県対抗＋クラブ選手権プレ＋9ブロック大会**
3. 規模は **283団体 / 約432ページ**（高校289団体・409ページと同等）
4. URL・見出し・description に「学校」を前提とした語を使わない（クラブ41%）
5. ランキングを作るならブロック大会を除外する

## 追記（2026-08-12・実装完了。URLは /secondaryschool、階層は中学向けに再設計）

ユーザーの指示は「URLは `/secondaryschool/`、階層は中学でやりやすい形にして、特に高校に合わせなくていい」。
そこで高校の3階層をそのまま持ち込まず、実測に基づいて設計し直した。仕様は
[secondaryschool.md](../wiki/secondaryschool.md) に集約したので、ここには**なぜそうしたか**だけ残す。

### 高校と変えた4点と、その根拠

1. **性別を URL に入れない**（高校は `/highschool/[gender]/...`）。
   実測: 閾値5の283チームを男女別ページにすると432ページで**32%が5件未満**、
   1ページにまとめると283ページで**0%**。中学は1チームあたり中央値8件しかなく分割に耐えない。
   男女とも収録があるのは53%だけで、分けても半分は片側が空になる
2. **「学校」ではなく「チーム」**。閾値5の内訳が 学校117 / クラブ116 / 判定不能50 で**クラブが41%**。
   高校に同じ物差しを当てるとクラブは2件しかない
3. **大会軸のページを作らない**（高校は `/highschool/tournaments/[tournament]`）。
   全中は4年分しかなく歴代表が組めないうえ、既存の大会ハブに地域移行トラッカーを実装済みで、
   新設すると ADR-010 が避けた重複を自分で作ることになる。ハブ側から `/secondaryschool/` へ導線を張った
4. **ランキングページを作らない**。ブロック大会は配点に使えず（北海道65対福岡4）、
   全中＋県対抗だけでは薄い。県別ポイントは都道府県ページの中に置いた

結果、**331ページ**（入口1＋県47＋チーム283）。高校の529ページより小さくまとまった。

### 都道府県ページの主役を「県の成績」にした

候補2（都道府県対抗を県ページの主役に）をここで実装した。県対抗は47都道府県が同じ枠数で出場するため、
出場機会の偏りなく県を比較できる。実際にポイント化すると愛知44.5〜沖縄9.6の分布になった。
**高校側には対応する大会が無いので原理的に作れないページ**で、横展開ではなく中学固有の価値になる。

ただし**開催県は出場枠が他県の倍あり有利**（集計中に `appear` が19/20/21/40 と割れて発覚）。
注記も含めて未対応で、課題に残した。

### 進路（候補1）は最初から入れた

`scripts/build-secondaryschool-pathways.mjs`。採用条件は「ペア継続 or 県一致」で、
**県をまたぐ単独一致は捨てた**（追跡候補560件中、採用323・県またぎ不採用224・同姓同名0）。
掲載チームに紐づいたのは183件／82チーム。越境進学を取りこぼすが、
誤った進学先を出すほうが害が大きい。ページにも限界を注記した。

実際に描いてみると `昇陽中学校` に12件の進路が並び、すべて系列の `昇陽`（高校）だった。
これは名寄せで中高一貫を分離しておいたから正しく出た結果で、
分離していなければ同一チーム扱いになって進路として検出できていない。

### 検証

- `npx tsc --noEmit` エラー0 / `eslint` エラー0
- **331ページすべてを `renderToStaticMarkup` で描画確認**（props の JSON シリアライズ可能性も全件チェック）。
  `getStaticPaths` の件数は 県47・チーム283 で索引と一致
- 絵文字の混入なし（AGENTS.md の UI 表記ルール）
- **未実施: `npm run build` の完走**。この環境では時間内に終わらない

### 追記: teamId を短縮した（ユーザー指摘）

「chuugakkou は不要な気がするがどうか」「クラブも club のほうがいい？」という指摘を受けて修正した。
どちらもその通りで、高校側も `to_romaji` で「高等学校/高校」を落としてから変換していた。

落とすもの: 括弧書き / 設置者接頭辞（`江別市立…`）/ 競技名（`ソフトテニス`）/ 学校種別接尾辞。
英語綴りへ寄せるもの: クラブ・ジュニア・ユース・スポーツ・センター・チーム。
**平均10文字**になった（`ebetsushiritsuebetsudaiichichuugakkou` → `ebetsudaiichi`、`mkkurabu` → `mkclub`）。

固有名詞のカタカナ（`日野フレンズ` `レペゼン千葉` `ガナドール福島`）は**あえてローマ字のまま**にした。
団体が選んだ名前の英語綴りを推測すると誤るため（`ガナドール`は ganador かもしれないが断定できない）。

### 副産物: スラッグ衝突が名寄せの取りこぼしを検出した

短縮した結果、県内で14件が衝突した。調べると**設置者接頭辞のせいで名寄せ候補に上がっていない
グループが46件**あった。既存の `teamCore()` は接尾辞しか落とさないため、
`倉敷市立連島中学校` と `連島中学校` が別チームのままだった。

**ただし接頭辞を落として機械的にマージするのは危険**で、46件の中には別の学校が混ざっている:

- `岩見沢市立光陵中学校` と `伊達市立光陵中学校`（別の市）
- `東松山市立松山中学校` と `松山市立松山中学校`（別の学校）
- `北九州市立大学` と `東京都立大学`（完全に別）

既存パイプラインが接頭辞を落としていないのは正しい判断だった。本物の取りこぼしは約30件で、
人手レビューが要る。今回は**衝突時に短縮前の名前でローマ字化し直す**ことで URL の正しさだけ担保し、
名寄せ自体には手を付けていない（URLが長いまま残った5件がその印になっている）。

### teamId のローマ字が未検証（要対応）

pykakasi の読みは誤ることがある（`昇陽`→`noboruyou`、`鉢盛`→`hachijou`）。
高校側も同じ問題があり `team_id_map.json` に30件の手動上書きを持っている。
中学も `data/secondaryschool/team-id-overrides.json` を用意したが**283件の目視確認は未実施**。
URL は一度公開すると変えにくいので、公開前に確認したい。

## 課題・未解決

- **URL設計未確定**。`/juniorhigh/` か `/secondaryschool/` か。`/highschool/[gender]/[prefectureId]/[teamId]`
  と対称にするか、クラブ混在を踏まえて別構造にするか。
- ~~**ブロック大会をカテゴリに統合するかどうか**~~ → **2026-08-12 決着**（上の「ページ規模を再測定」節）。
  **掲載判定には使う・集計には使わない**。全中＋都道府県対抗のみだと閾値5の達成は158団体で
  （ブロック込み283・高校289）、京都・宮崎・山梨・沖縄が1団体しか残らず都道府県ページが成立しないため。
  高校の「統合しない」決定とは矛盾しない。高校は6年×3大会あるので掲載判定に地区大会が不要なだけで、
  「集計を歪めるから使わない」という原理は中学でもそのまま維持している
- **ランキングを作るなら配点をどうするか**。高校は3大会（IH/ハイジャパ/選抜）で年度重みをかけている。
  中学は全中4年分＋都道府県対抗2年分と年数が浅く、年度重み 1.0/0.8/0.6/0.3 がほぼ効かない。
  加えて **2026-08-12 の測定でブロック大会は配点に使えないと確定**した（県別の出場団体数が
  北海道65対福岡4で、実力差ではなくブロック構成の差が出る）。実質「全中＋都道府県対抗」だけで
  配点する必要があり、配点対象がさらに薄くなる。
- **高校カテゴリとのキーワード共食い**。「{県名} ソフトテニス」という広いクエリで
  高校の都道府県ページと競合しうる（成年カテゴリ検討でも同じ論点が出ている）。
- **過去年度の遡及投入コスト**。厚みを稼ぐ本命だが、ブロック大会は2026年度分しかなく、
  全中も2022年度以降のみ。PDF入手可否が未調査。
- 都道府県対抗全日本中学生大会（960行、中学最大のデータ源）は**構造上もともと県単位**で、
  県ページと相性が良い。この大会を県ページの主役に据える設計は未検討。

## 目指したい方向性

フル横展開を選択済み。ただし2026-08-12の追記により、**「高校型の器を作る」ことと
「高校には作れない中身を入れる」ことは別問題**だと整理できた。器はフル横展開でよいが、
中身の主役は高校（＝学校）と同じである必要はない。

コード着手前に済ませられる調査が3つ:

1. ~~**名寄せ層の構築**~~ → **2026-08-12 完了**（ADR-013）。残る `四天王寺高校` の混入と、
   高校地区大会への中学校名混入4種は出典確認が要るため未修正
2. ~~**閾値5・ブロック大会込み/なしでのページ規模の確定**~~ → **2026-08-12 完了**。
   閾値5・ブロック込みで **283団体 / 約432ページ**（高校289団体・409ページと同等）。
   測定は `node scripts/measure-juniorhigh-scale.mjs` で再現できる
3. **進路接続（候補1）の採用単位の確定**（ペア単位48組から始めるか、県一致301件まで広げるか）

なお候補3（地域移行トラッカー）は上記に依存せず先行実装できる。

## 関連

- [../wiki/highschool.md](../wiki/highschool.md) — 横展開元の高校カテゴリ仕様
- [../wiki/data-model.md](../wiki/data-model.md) Idea Backlog — 2026-08-08「中学専用カテゴリは作らない」決定（本アイデアが上書き）
- [2026-08-08-idea-zenchu-block-tournament-data.md](./2026-08-08-idea-zenchu-block-tournament-data.md) — 全中ブロック大会の掲載（データ供給元）
- [2026-08-11-idea-general-category-prefecture-pages.md](./2026-08-11-idea-general-category-prefecture-pages.md) — 成年カテゴリでの同型検討（データ深度で棄却した先例）
- [2026-07-22-highschool-block-tournament-page-structure.md](./2026-07-22-highschool-block-tournament-page-structure.md) — 地区大会をカテゴリ機能へ統合しない先例
- [../wiki/team-player-identity.md](../wiki/team-player-identity.md) — チームの都道府県データの信頼性
- [../wiki/seo.md](../wiki/seo.md) #3・#7・#10・#11 — 高校カテゴリのSEO設計

## 参考文献

- [中体連の全国大会 2027年度から規模縮小 19競技から9競技を除外](https://www.okinawatimes.co.jp/articles/-/1374824)
- [全国中学校体育大会、水泳や体操除外へ 27年度以降（日本経済新聞）](https://www.nikkei.com/article/DGXZQOUE0829X0Y4A600C2000000/)
- [中学生ソフトテニス大会の出場資格｜中体連2026年度ルール完全解説](https://hitonova.com/guide/chutairen-softtennis-national/) — 地域クラブ参加の解禁
- [第57回 全国中学校ソフトテニス大会](https://softtennis.zenchuu.jp/)

## Compile Log

- 2026-08-12 作成。集計はワンショットのPythonスクリプトで実施し未保存。再現する場合は
  `data/tournaments/details/{tid}/{year}/*.json` の `participants[].team` / `.prefecture` を
  上記12大会分について集計すればよい（`temp` などの非年度ディレクトリを除外する必要がある）。
- 提示した3案のうち「新規URLなし・既存強化」「ランキング型の小さいページ群」はユーザーが選ばなかったため
  本ファイルには選択肢として記録するに留め、詳細設計は展開していない。フル横展開が
  前提条件（名寄せ・閾値）で行き詰まった場合の退避先として残す。
- 2026-08-12 追記（「高校とは違う形」の4候補）。ユーザーは4候補すべてを掘り下げ対象に選択。
  候補4（選手中心の設計）は実測で単独成立しないと判明したが、否定の根拠ごと残す価値があるため
  削除せず「棄却の理由つき」で記録した。集計は全てワンショットのPythonスクリプトで未保存。
  再現手順は上記の各表の注記どおり `participants` / `entries.playerIds` を突き合わせればよい。
- 2026-08-12 実装完了。docs への write-back は **新規に `docs/wiki/secondaryschool.md` を作成**し、
  `public-pages.md` のルーティング節と `index.md` からクロスリンクした（AGENTS.md の新規wikiページ規約）。
  意図的に wiki へ載せなかったもの:
  - 候補1（進路）の実装以外の3候補の扱い。候補2は都道府県ページとして実装したので
    `secondaryschool.md` に溶かし、候補3は既に `public-pages.md` にあり、候補4は棄却済み。
    「候補1〜4」という番号自体は raw だけの整理軸なので wiki には持ち込まない
  - 331ページ全件の描画確認という検証手順。再現するなら同じ ts-node ワンライナーを書けばよく、
    スクリプトとして残すほどの再利用性が無いと判断した（測定スクリプトとは違い一度きりの確認のため）
- 2026-08-12 ページ規模の再測定。測定コードは `scripts/measure-juniorhigh-scale.mjs` として保存した
  （本ファイル冒頭の集計はワンショットで未保存だったため、同じ数字を出し直せない状態だった。
  同じ轍を踏まないようにするのが目的）。wiki へは載せていない——実装に着手する段階で
  `public-pages.md` のルーティング節に確定仕様として書けばよく、いまは idea 段階のため。
  なお本ファイル上部の「掲載閾値ごとのページ規模」の表は**名寄せ前の概算**で、
  再測定の節が正。削除せず残してあるのは、名寄せ前後でどれだけ数字が動くかの記録として意味があるため。
- 2026-08-12 名寄せ実施の追記。docs への write-back は
  [ADR-013](../adr/ADR-013-scoped-team-name-aliases.md)（`scope` の設計判断）と
  `team-player-identity.md`「alias の大会スコープ」節（運用手順）に分けた。
  意図的に wiki へ載せなかったもの:
  - 77件の canonical 反転の全リスト・17件の scope 付与の全リストは raw にも載せていない
    （`git diff data/tournaments/team-name-aliases.json` で追える。冗長なため）
  - A2（学校と同地名クラブ）の35グループの個別判断は未着手なので wiki に出さない
  - 高校地区大会への中学校名混入4種は、出典未確認のため wiki の仕様ページには書かず
    raw の「副産物」節に留めた。修正時に `tournament-venue-data` / 要項PDFを当たること
- クラブ比率（候補3）の判定は**チーム名の正規表現による概算**であり、厳密な定義ではない。
  「クラブ」「STC」「少年団」「Jr」「英字3文字以上」等を含む名前を機械的に拾っている。
  実装時は判定方法の確定が要る（この限界は本文中にも明記済み）。
  → **2026-08-12 の実装追記で確定した**（下限カウント方式）。上表の概算値
  （2022:1 / 2023:8 or 9 / 2024:15 or 17 / 2025:35〜38）は経緯として残すが、
  **正は実装追記の確定値（1 / 10 / 21 / 39）**。混同しないこと。
- 2026-08-12 実装追記（候補3）。docs/wiki への write-back は
  `public-pages.md`「学校部活動と地域クラブの内訳」節に集約した。
  意図的に wiki へ載せなかったもの:
  - 分類規則を改良した3段階の経緯（末尾`中`→ラテン区切り→カタカナ）は raw に留めた。
    wiki は現在の規則だけ載せれば足り、途中経過は再現に不要なため
  - 候補1・2・4 の実装計画は未着手なので wiki には出さない（idea段階のまま）
  - `seo.md` への追記は見送った。既存 #8 の「DB由来の差別化」方針の実例が増えただけで、
    重複マップに新しい競合が生まれたわけではないため。GSC で効果が見えた時点で再検討する

## 追記（2026-08-12・`matsuyama-2` の指摘から名寄せ漏れを18件修正）

ユーザーが teamId レビューリストの `| 松山 | 埼玉県 | matsuyama-2 |` を見て「これはいいのか」と指摘。
**バグだった**。`松山`（15件）と `松山中学校`（16件）が別チームのまま残っており、
同じスラッグになって `-2` が付いていた。同じ東松山市立松山中学校である。

### なぜ中高一貫の分離で漏れたか

先の対応（7校）は「`X` と `X中学校` が**両方 alias 表の canonical である**」ものだけを拾っていた。
`松山中学校` は canonical だが素の `松山` は canonical でなく、データ上の生表記でしかないため
スキャンから外れていた。**alias 表を起点にしたのが誤りで、実データを起点にすべきだった。**

実データから引き直すと18件あった。

### 「この県では高校に出ていない」は「全国で高校に出ていない」ではない

18件のうち11件は素の名前が中学の大会にしか出ておらず、scope 無しで alias に入れた。
その結果 **`向陽中学校` が高校大会に20件、`高田中学校` が34件、`高瀬中学校` が17件**現れた。
和歌山県立向陽高校・岩手県立高田高校などが書き換わったためで、
**県単位で「高校に出ていない」を確認しても、他県の同名高校は防げない**。

`git checkout -- data/tournaments/details` で巻き戻し、18件すべてを
`tournamentPrefix: ['secondaryschool-', 'zennihon-secondaryschool']` に限定して再適用した。
高校側に残る中学校名は元データ由来の4件（`土佐中学校` `新宮中学校` `日高中学校` `柏崎ジュニア`）のみで、
これは着手前と同じ状態。

### スラッグ衝突が検出器として機能した

`-2` が付いた7件はすべて未統合ペアだった。内訳と対応:

- 5件は全角半角の揺れ（`ＵＴＣ`/`UTC` ほか）→ `normalize-team-spacing.mjs --scope=all` で解消
- 1件はアポストロフィの揺れ（`M’ｓ` U+2019 と `M's` U+0027。NFKC では畳まれない）→ alias 追加
- 2件は `朝日`（愛知）と `野木`（栃木）で、同県に `朝日STC` `野木クラブ` があり
  素の名前が学校とクラブのどちらか判別できない → **意図的に未統合のまま残した**

**この副作用は設計として残す価値がある。** 「県内でスラッグが衝突する ＝ 名寄せの取りこぼし」
という検出器が無料で手に入っており、`-2` が付いているチームを見れば要確認箇所が分かる。

### 最終的な数字

チーム 283 → **280**（3件が統合された）、総ページ 331 → **328**。
学校125 / クラブ114 / 判定不能41。進路は 183件/82チーム → **184件/83チーム**。
ヘルスチェックの要対応シグナルは 42 → **40**。

## 追記（2026-08-12・teamId の override 22件と長音の流儀決定）

ユーザーが `team-id-overrides.json` に22件を記入。JSON の末尾にカンマが残っていたので修正した上で反映した。
`昇陽中学校`→`shouyou`、`明徳義塾中学校`→`meitokugijuku` など、pykakasi の誤読が主要チームで解消された。

### 長音は `ou` を残す（高校に合わせる）

ユーザーの記入は**長音を省く流儀**だった（`shoyo` `kyotokoka` `meiho`）。`Tokyo` `Kyoto` と同じで
URL としてはこちらのほうが読みやすい。一方 pykakasi が生成した残り258件は `ou` を残すため、
サイト内で2つの流儀が混在する状態になっていた（未override 258件のうち47件が `ou`/`uu` を含み、
`東京`→`toukyou` のような例もあった）。

「高校に合わせて」というユーザー判断で **`ou` を残す側に統一**し、22件を書き換えた
（`shoyo`→`shouyou`、`kyotokoka`→`kyoutokouka` ほか13件）。

**ただし高校側も一枚岩ではなかった。** 判断材料として調べた結果:

- 自動生成765件のうち **39%が `ou`/`uu` を含む**（pykakasi 由来なので当然）
- **手動上書き30件は混在**。`soyo`（相洋）`hoyatomo`（早鞆）`okayamasozan`（岡山操山）
  `shohei`（昌平）`honan`（豊南）は長音省略、`nishijouyou`（西城陽）`kousei`（甲西）
  `kagoshimashou`（鹿児島商）は長音あり

つまり「高校の流儀」は**機械生成の `ou` が実質的な多数派**というだけで、明文化された規約ではない。
中学側はその多数派に合わせた。

**例外を1つ残した**: `相洋中学校` は `souyou` ではなく **`soyo`**。高校側の `team_id_map.json` が
相洋を `soyo` としており、同じ学校名で高校 `soyo` / 中学 `souyou` と割れるのを避けたため。
同名の学校が高校側にある場合は `team_id_map.json` を先に見る、という運用を wiki に明記した。

### 副産物: タイポを1件発見

`新潟UCHINO` が `nigatauchino` になっていた（新潟＝にいがた なので `niigata`）。
長音の統一とあわせて修正した。

## 追記（2026-08-12・チーム名の括弧書きを整理）

ユーザーが `岩見沢市立光陵中学校(拠)` を見て「そもそも（拠）が不要」と指摘。その通りで、
**`(拠)` は拠点校であることの注記であってチーム名の一部ではない**。スラッグの問題ではなくデータの問題だった。

全データで括弧書きを含むチーム名を洗い出すと**8種・延べ19件**あり、2系統に分かれた。

**A) 制度上の注記（3件）** — 落とす:

- `岩見沢市立光陵中学校(拠)` → `岩見沢市立光陵中学校`（素の表記が既に7件ある）
- `札幌市立栄中学校（拠）` → `札幌市立栄中学校`（4件）
- `安曇川（拠）` → `安曇川中学校`。括弧を外した「安曇川」はデータに無く、正式名の
  `安曇川中学校`（12件）が正なので、そちらへ寄せた

**B) 別称の併記（4件）** — 括弧内を落とす:

- `ＫＳＴＣ（熊野クラブ）` → `ＫＳＴＣ`（29件）
- `松阪ジュニアクラブ（ＭＪＳ）` → `松阪ジュニアクラブ`（20件）
- `京都第二赤十字病院（日赤ＳＴ）` → `京都第二赤十字病院`（12件）
- `小郡連盟（小郡クラブ）` → `小郡連盟`

いずれも alias 表に登録して `normalize-team-names.mjs --scope=all` で反映した。
`岩見沢市立光陵中学校` は統合されてスラッグ衝突が1件減り、`teamId` の最長も40→38文字になった。

### 保留した1件

`函館市拠点校部活動（巴中学校）`（2件）だけは**落とすと意味が壊れる**ので触っていない。
括弧内の「巴中学校」が実際の学校で、外側は制度名（函館市の拠点校部活動）。
括弧を外すと「函館市拠点校部活動」という団体名になってしまう。
これは部活動の地域移行にともなう新しい参加形態そのもので、
どう表記するのが正しいかは出典（大会要項）を当たらないと決められない。
なお2件なので掲載閾値5に届かず、ページは生成されていない。

### この種の問題の見つけ方

括弧書きは `data/tournaments/details/**` の `participants[].team` を
`/[（(].*?[）)]/` で grep すれば全件出る。8種しかないので目視で分類できる。
新しい大会データを入れたときに一度走らせるとよい。

## 追記（2026-08-12・県別ポイントを廃止し、高校への逆引きを追加）

### 発端: 実装が説明と食い違っていた

「次に何をすべきか」を整理する過程で県別ポイントの数字を見直したところ、
**公開ページの主張と実装が合っていなかった**。ページには
「都道府県対抗は47都道府県がすべて同じ枠数で出場するため公平に比べられます」と書いていたが、
実際のポイントは**全中・都道府県対抗・クラブ選手権プレの3大会合算**だった。

県あたりの出場延べを大会別に測ると:

| 配点に使っていた大会 | 県あたり出場延べ | 最大/最小 |
|---|---|---|
| 都道府県対抗のみ | 19〜40 | 2.1倍 |
| **全中のみ** | **2〜40** | **20倍** |
| クラブ選手権プレのみ | 2〜20（**26県しか出ていない**） | 10倍 |
| 実装（3大会合算） | 20〜68 | 3.4倍 |

都道府県対抗単体でも完全な同数ではなく、**開催県の三重だけ40**（他県は19〜21）だった。

### 判断: ポイントを廃止し、順位づけをしないカテゴリにする

ユーザーと選択肢を検討した結果（(a)県対抗のみに絞る / (b)説明を書き換える / (c)廃止）、
**廃止**を選んだ。決め手は「そもそも中学カテゴリは
『どういったチームがあるか＋そのチームの逆引き』というシンプルな機能でよいのでは」というユーザーの判断。

これは**削除がほとんど**で作り直しは発生しなかった（入口1節・県ページ1節・生成スクリプトの配点）。
結果として:

- 配点という Assumption（ユーザー未確定の初期値）を公開せずに済む
- 開催県バイアスも大会ごとの枠数差も、順位を付けないなら問題が発生しない
- **残るものが全部「検算できる事実」になる**（どのチームが収録されているか／成績は何か／誰がどこへ進学したか）

失ったのは「県対抗は47県が同じ土俵」という中学固有の切り口。
アイデアとしては筋が良かったが、**実装がその主張に追いついていなかった**。
データは残っているので「順位づけせず実績だけ列挙する」形で戻せる
（ベスト8以上の実績がある県は30/47・1県あたり中央値2件）。

### 高校への逆引きを追加

同じ進路データを高校名で引き直し、`/highschool/[gender]/[prefectureId]/[teamId]` に
「{高校名}の選手の出身中学」節を追加した（`lib/highschoolFeederSchools.ts`）。**新規URLなし。**

実装で1点気づいた: **高校の学校ページは男女別なので、進路レコードに性別が必要だった**。
`pathways.json` に `highschoolGender` を追加し、`mixed` は男女どちらのページにも出す
（highschool.md の既存規約に合わせた）。実測で boys 85件 / girls 99件。

動作確認: `昇陽`(girls) は12件・`昇陽`(boys) は0件と正しく分かれた。
`岡崎城西`(boys) は3中学から4名で、複数の中学から集まっている高校のほうが読み物として面白い。

### 検討したが採らなかった案

- **逆引きだけにして中学カテゴリを捨てる**: 逆引きが付くのは575校中81校（14%）で
  1校あたり中央値1件。単体の施策としては小さく、328ページ分の受け皿と
  「{中学校名} ソフトテニス」クエリを失う。何より読者像が逆（高校を見ている人 vs 進路を選ぶ中学生）
- **県別ポイントを都道府県対抗のみに絞る**: 説明と実装は一致するが、配点の Assumption は残る
- **順位づけせず県対抗の実績だけ列挙**: 公平性の問題は消えるが17県が空になる。将来の選択肢として保留

## 追記（2026-08-12・設置者接頭辞の統合と「四天王寺高校」の正体）

### C-3: 設置者接頭辞つき／なしの4ペアを統合

teamId が長いまま残っていたのが目印になり、選手名の突き合わせで4件とも同一校と確定した。

| 統合 | 共通する選手 |
|---|---|
| `羽須美中学校` ← `邑南町立羽須美中学校` | **5人**（上口華弥・岡崎紗奈・川村優菜・栗原拓真・濱本凛乃） |
| `連島中学校` ← `倉敷市立連島中学校` | **4人**（小野暉介・石原星依・竹田陽希・首藤優空） |
| `名寄中学校` ← `名寄市立名寄中学校` | 後藤聖和 |
| `中広中学校` ← `広島市立中広中学校` | 栗本瀬菜（`広澤幸来`/`廣澤幸来` は異体字で同一人物） |

**パターンは4件とも同じ**で、2026年度のブロック大会だけが設置者接頭辞つきの正式名称、
それ以前（全中・県対抗）は略称という**出典差**だった。

canonical は**略称側**を採った。`team-player-identity.md` の「canonical は正式名称」規約と
一見ずれるが、高校側も `上田染谷丘`（長野県立上田染谷丘高校）のように設置者を落としており、
「よく使われる名前」を canonical にする運用に揃えた。URLも短くなる。

索引に残る設置者接頭辞つき14件は**略称版が存在しない**ので統合不要
（`羅臼町立知床未来中学校`→`shiretokomirai` のように teamId は既に短い）。

### C-5: 「四天王寺高校の混入」は自分の誤報だった

当初「中学の大会データに高校名の団体が混入している」と報告したが**不正確だった**。
実際に調べると `四天王寺高校` はインターハイ・ハイスクールジャパンカップ・全日本選手権などに
**45件出ている正規の高校チーム**（大阪の強豪）で、中学カテゴリには一切出ていない。

本当の問題は「**全中2022の所属が誤って高校名で記録されていた**」1件（房野紗千・市川こいと）。
四天王寺は中高一貫校なので、この2人の中学時代の所属は `四天王寺中学校` が正しい。

`四天王寺中学校 ← 四天王寺高校` を **`tournamentPrefix`（中学校の大会）× `prefecture`（大阪府）**
に限定して追加した。高校の大会の45件は無傷。中学側は2件で掲載閾値未満なのでページは生成されない。

**教訓**: 「Aの文脈にBの名前がある」を見たとき、Bが本当にどこの所属なのかを先に確認すべきだった。
名前の形（`〜高校`）だけで「混入」と決めつけると、実際は「所属記録の誤り」という別の問題を
見落とす。今回は scope 機構があったので、高校側を壊さずに1件だけ直せた。

### 結果

チーム 277→**273**、ページ 325→**321**。
20文字超の teamId は5件のみ（`北海道教育大学附属旭川中学校` など名前自体が長いもの）。
高校側は無傷（`四天王寺高校` 45件のまま、他の中学校名混入も元データ由来の4種で変化なし）。
